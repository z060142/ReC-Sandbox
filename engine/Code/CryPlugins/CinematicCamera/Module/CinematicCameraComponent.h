#pragma once#include <CryEntitySystem/IEntityComponent.h>#include <CrySchematyc/MathTypes.h>#include <CrySchematyc/Reflection/TypeDesc.h>#include <CrySchematyc/Env/IEnvRegistrar.h>#include <CrySchematyc/ResourceTypes.h>// CSharedString: the reflected string type behind the two .cube path properties (Look / Output).#include <CrySchematyc/Utils/SharedString.h>#include <Cry3DEngine/I3DEngine.h>#include <vector>#include "../Interface/ICinematicCameraOptics.h"// CCameraComponent is in CryDefaultEntities/Module ??forward-declared here,// included fully in the .cpp (include path set in CMakeLists.txt).namespace Cry { namespace DefaultComponents { class CCameraComponent; } }// ---------------------------------------------------------------------------// Sensor size presets// ---------------------------------------------------------------------------enum class ESensorPreset : uint32{	FullFrame35mm = 0,  // 36 x 24 mm	APSC,               // 23.6 x 15.7 mm	MicroFourThirds,    // 17.3 x 13 mm	Custom};static void ReflectType(Schematyc::CTypeDesc<ESensorPreset>& desc){	desc.SetGUID("{C3A1B2D4-9E5F-4C8A-B1D0-6E7F2A3C4B5D}"_cry_guid);	desc.SetLabel("Sensor Preset");	desc.SetDefaultValue(ESensorPreset::FullFrame35mm);	desc.AddConstant(ESensorPreset::FullFrame35mm,   "FullFrame35mm",   "Full Frame 35mm");	desc.AddConstant(ESensorPreset::APSC,            "APSC",            "APS-C");	desc.AddConstant(ESensorPreset::MicroFourThirds, "MicroFourThirds", "Micro Four Thirds");	desc.AddConstant(ESensorPreset::Custom,          "Custom",          "Custom");}// ---------------------------------------------------------------------------// Focal length detents//// The engine takes a continuous FOV, so "steps" are produced by quantising the focal// length that feeds the optics; the stored property still holds whatever the fader sent.// ---------------------------------------------------------------------------enum class EFocalStepMode : uint32{	Continuous = 0, // No quantisation.	PrimeSet,       // Snap to a classic prime lens series (12 ??600 mm).	Geometric       // Snap to N equal steps per doubling of focal length.};static void ReflectType(Schematyc::CTypeDesc<EFocalStepMode>& desc){	desc.SetGUID("{7D4E9A16-2C58-4B31-8F6A-0B9E4C1D7A32}"_cry_guid);	desc.SetLabel("Focal Step Mode");	desc.SetDefaultValue(EFocalStepMode::PrimeSet);	desc.AddConstant(EFocalStepMode::Continuous, "Continuous", "Continuous");	desc.AddConstant(EFocalStepMode::PrimeSet,   "PrimeSet",   "Prime Lens Set");	desc.AddConstant(EFocalStepMode::Geometric,  "Geometric",  "Geometric Steps");}// ---------------------------------------------------------------------------// Exposure model// ---------------------------------------------------------------------------enum class EExposureMode : uint32{	//! Writes the scene-key auto-compensation strength. Aperture cannot affect brightness	//! here: the engine scales that term by how far the scene is from the reference key,	//! so its sign flips between bright and dark scenes.	AutoCompensation = 0,	//! Locks EV Min and EV Max to a single EV100 computed from aperture, shutter, ISO and	//! EV compensation, which is what makes the f-number actually change brightness.	Manual,	//! Keeps the scene's own adaptation window and shifts it by the camera's relative	//! stops. Day and night both expose sensibly (the engine's night is artist-lit, only a	//! few stops darker than day), while the lens still visibly biases the image.	AutoBiased};static void ReflectType(Schematyc::CTypeDesc<EExposureMode>& desc){	desc.SetGUID("{1E8B5C74-6A29-4D03-B5F1-8C2D7E6A0B94}"_cry_guid);	desc.SetLabel("Exposure Mode");	desc.SetDefaultValue(EExposureMode::AutoBiased);	desc.AddConstant(EExposureMode::AutoCompensation, "AutoCompensation", "Auto Compensation");	desc.AddConstant(EExposureMode::Manual,           "Manual",           "Manual (physical)");	desc.AddConstant(EExposureMode::AutoBiased,       "AutoBiased",       "Auto + Bias (game)");}// ---------------------------------------------------------------------------// Metering pattern// ---------------------------------------------------------------------------// WHERE in the frame the camera's AUTO mode looks. Scene-referred path only: it is a weight on// the metering histogram's accumulation (decisions/s8-metering.md approach A), and the three are// the patterns every real body offers, at the sizes the camera industry settled on.enum class EMeteringMode : uint32{	//! The whole frame, uniformly. The A/B reference against the shipped whole-frame log-mean -	//! and outdoors, where the sky is half the picture and is not the subject, the pattern that	//! most often gets it wrong.	Average = 0,	//! A smooth radial falloff: full sensitivity at the centre, a quarter of it in the corners.	//! The classic answer to the sky problem and the default.	CenterWeighted,	//! A soft-edged disc 20 % of frame height across, about 1.8 % of frame area. Put it on a grey	//! card and the card is mid grey, whatever else is in the picture.	Spot};static void ReflectType(Schematyc::CTypeDesc<EMeteringMode>& desc){	desc.SetGUID("{7A4E2D18-9C63-4B57-8E20-51D3A9F6C742}"_cry_guid);	desc.SetLabel("Metering Mode");	desc.SetDefaultValue(EMeteringMode::CenterWeighted);	desc.AddConstant(EMeteringMode::Average,        "Average",        "Average");	desc.AddConstant(EMeteringMode::CenterWeighted, "CenterWeighted", "Centre-weighted");	desc.AddConstant(EMeteringMode::Spot,           "Spot",           "Spot");}// ---------------------------------------------------------------------------// Diffraction streak model// ---------------------------------------------------------------------------// Two models of the SAME physics - light bending around the iris opening - and they are// exclusive. Analytic paints N line pairs with an exponential falloff and an f-number ramp that// was tuned by eye; it cannot show what a real aperture does. Wave computes the pupil's actual// Fraunhofer diffraction pattern, |FFT(pupil)|^2, and convolves the frame's highlights with it,// so the blade count, the blade curvature, a shape mask and the spherical aberration all reach// the sunstar by themselves - and stopping down lengthens the rays with no ramp at all, because// the pattern's size on the sensor is lambda * N.enum class EStreakMode : uint32{	Analytic = 0, //!< The original line pairs. Kept unchanged as an A/B reference and a fallback.	Wave          //!< Wave optics: the FFT of the pupil as the glare point-spread function.};static void ReflectType(Schematyc::CTypeDesc<EStreakMode>& desc){	desc.SetGUID("{4C1A7B93-8E52-4D60-A7F4-2B93D6E1C085}"_cry_guid);	desc.SetLabel("Streak Mode");	desc.SetDefaultValue(EStreakMode::Wave);	desc.AddConstant(EStreakMode::Analytic, "Analytic", "Analytic (line pairs)");	desc.AddConstant(EStreakMode::Wave,     "Wave",     "Wave optics (FFT of the pupil)");}// ---------------------------------------------------------------------------// CCinematicCameraComponent//// Creates a sibling CCameraComponent that handles view/camera-manager integration// (so the entity appears in the Sandbox Camera Entity menu and the editor renders// through it with the correct frustum). This component drives://   - FOV  via focal-length + sensor-size optics formula ??written to CCameraComponent//   - DOF  via Dof_User_* post-effect params//   - EV   via E3DPARAM_HDR_EYEADAPTATION_PARAMS, applied in the plugin's//           UpdateBeforeFinalizeCamera() hook (runs after TOD::Update so it can't//           be overwritten by the TOD system in the same frame)// ---------------------------------------------------------------------------struct SCineBodyParams{	inline bool operator==(const SCineBodyParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineBodyParams>& desc)	{		desc.SetGUID("{1B7E60A4-3D52-4C18-9A6F-E20B84C15D71}"_cry_guid);		desc.SetLabel("Camera Body");		desc.AddMember(&SCineBodyParams::slot,			'slot', "Slot", "Camera Slot",			"1-9: the number this camera answers to. 'ccam_slot N' in the console (bind it to a "			"key) makes it the active camera, and a Phone Tracker set to follow the active camera "			"comes along with it. 0 = no slot.", 0);		desc.AddMember(&SCineBodyParams::bActivateOnGameStart,			'aogs', "ActivateOnGameStart", "Activate On Game Start",			"Automatically make this the active camera when gameplay starts. Leave off when "			"Schematyc decides which camera is active - with several cameras in the level, "			"auto activation lets the last one initialised steal the view from an explicit "			"Activate call.", false);		desc.AddMember(&SCineBodyParams::nearPlane,			'near', "NearPlane", "Near Plane (m)", nullptr, 0.25f);		desc.AddMember(&SCineBodyParams::farPlane,			'far_', "FarPlane", "Far Plane (m)", nullptr, 1024.f);		desc.AddMember(&SCineBodyParams::sensorPreset,			'sens', "SensorPreset", "Sensor Preset",			"Reference sensor size used to compute FOV and circle of confusion.",			ESensorPreset::FullFrame35mm);		desc.AddMember(&SCineBodyParams::customSensorWidth,			'cswt', "CustomSensorWidth", "Custom Sensor Width (mm)",			"Width in mm ??only used when Sensor Preset is Custom.", 36.0f);		desc.AddMember(&SCineBodyParams::iso,			'iso_', "ISO", "ISO",			"Sensitivity. Drives Manual/AutoBiased exposure and the film grain curve.", 400);		desc.AddMember(&SCineBodyParams::shutterDenominator,			'shut', "ShutterDenominator", "Shutter (1/x s)",			"50 is roughly a 180 degree shutter at 25 fps. Drives exposure, the motion blur shutter and the grain light term.", 50);	}	Schematyc::Range<0, 9, 0, 9, int> slot = 0;	bool bActivateOnGameStart = false;	Schematyc::Range<0, 32768> nearPlane = 0.25f;	Schematyc::Range<0, 32768> farPlane = 1024.f;	ESensorPreset sensorPreset = ESensorPreset::FullFrame35mm;	Schematyc::Range<1, 200> customSensorWidth = 36.f;	Schematyc::Range<25, 25600, 50, 6400, int> iso = 400;	Schematyc::Range<1, 8000, 1, 250, int> shutterDenominator = 50;};struct SCineLensParams{	// bokehShapeTex holds a string, so only the POD prefix in front of it can be memcmp'd.	inline bool operator==(const SCineLensParams& rhs) const	{		return 0 == memcmp(this, &rhs, offsetof(SCineLensParams, bokehShapeTex))		       && bokehShapeTex.value == rhs.bokehShapeTex.value;	}	static void ReflectType(Schematyc::CTypeDesc<SCineLensParams>& desc)	{		desc.SetGUID("{2C8F71B5-4E63-4D29-AB70-F31C95D26E82}"_cry_guid);		desc.SetLabel("Lens");		desc.AddMember(&SCineLensParams::focalLength,			'focl', "FocalLength", "Focal Length (mm)",			"Shorter = wider angle. Drives the vertical field of view.", 50.0f);		desc.AddMember(&SCineLensParams::focalStepMode,			'fstm', "FocalStepMode", "Focal Step Mode",			"Quantise the focal length so zooming has detents instead of sliding freely.",			EFocalStepMode::PrimeSet);		desc.AddMember(&SCineLensParams::focalStepsPerDoubling,			'fstn', "FocalStepsPerDoubling", "Steps Per Doubling",			"Detents between one focal length and twice that ??only used by Geometric.", 6);		desc.AddMember(&SCineLensParams::focalStepTime,			'fstt', "FocalStepTime", "Focal Step Time",			"Seconds the zoom motor takes to settle on a new detent, instead of snapping. 0 snaps instantly.", 0.25f);		desc.AddMember(&SCineLensParams::aperture,			'aptr', "Aperture", "Aperture (f-number)",			"Lower f-number = shallower depth of field.", 5.6f);		desc.AddMember(&SCineLensParams::apertureBlades,			'blad', "ApertureBlades", "Aperture Blades",			"Iris blade count. Shapes the bokeh: round when wide open, this polygon when stopped down.", 9);		desc.AddMember(&SCineLensParams::bladeCurvature,			'bcrv', "BladeCurvature", "Blade Curvature",			"How curved the iris blades are, which decides how round the opening is at a given "			"aperture. 0 = perfectly straight blades: the exact polygon at every f-stop, edges "			"cut crisply, so a 3-blade iris stays a clean sharp triangle. 1 = fully curved "			"blades: a true circle wide open, and the polygon reappearing gradually as you stop "			"down (half way one stop down, three quarters two stops down), with a slightly "			"softer outline. At an ordinary working aperture this is the fat-rounded-triangle "			"versus sharp-triangle control. The silhouette is always the same convex polygon - "			"curvature decides how far its edges bulge and how hard they are cut, it never "			"stretches the corners. Real lenses sit in between, and low blade counts show it "			"most - at 9 blades the polygon is nearly round anyway.", 0.4f);		desc.AddMember(&SCineLensParams::transmission,			'trns', "Transmission", "Transmission",			"Fraction of light the glass actually passes; defines the T-stop as T = f / sqrt(transmission). "			"Exposure and grain follow T, while depth of field, bokeh and streaks stay on the geometric f-number. "			"1 = ideal glass (T equals f), modern prime ~0.9, zoom ~0.8, vintage ~0.75.", 1.0f);		desc.AddMember(&SCineLensParams::ndFilterStops,			'ndf_', "NDFilterStops", "ND Filter (stops)",			"Neutral density in front of the lens: darkens by whole stops without touching depth of field or motion blur - how a real crew holds wide apertures in daylight.", 0);		desc.AddMember(&SCineLensParams::focusDistance,			'fdst', "FocusDistance", "Focus Distance (m)",			"Distance to the sharp focus plane.", 5.0f);		desc.AddMember(&SCineLensParams::vignetteAmount,			'vgna', "VignetteAmount", "Vignette Amount",			"RESIDUAL edge falloff - the part that is not mechanical: glass transmission and sensor "			"microlenses. 0 = none, 1 = the engine's stock vignette map, 2 = deepened. The barrel's "			"own light loss is Cat-eye's, and is applied separately.", 0.0f);		desc.AddMember(&SCineLensParams::vignetteApertureLink,			'vgnl', "VignetteApertureLink", "Vignette Aperture Link",			"How much the RESIDUAL falloff above follows the iris: 0 = fixed amount, 1 = strongest "			"wide open and gone by f/5.6. This is a shortcut kept for authoring convenience - the "			"real mechanical vignetting, with the cat-eye bokeh that comes with it, is Cat-eye.", 0.0f);		desc.AddMember(&SCineLensParams::distortion,			'dist', "Distortion", "Distortion",			"Radial lens distortion: positive = barrel, negative = pincushion / wide-angle correction. "			"A fixed per-lens character, deliberately not tied to focal length.", 0.0f);		desc.AddMember(&SCineLensParams::distortionFocalLink,			'dstl', "DistortionFocalLink", "Distortion Focal Link",			"Optional zoom characteristic: positive values add barrel at short focals and pincushion "			"at long ones (pivot 35mm), the way typical zoom lenses behave. 0 = prime-like fixed distortion.", 0.0f);		desc.AddMember(&SCineLensParams::breathingIntensity,			'brth', "BreathingIntensity", "Focus Breathing",			"How much the field of view tightens when focusing close (unit-focus lens physics: "			"magnification f/(d-f) scales the effective focal length). 0 = perfectly corrected "			"cine lens, 1 = typical stills lens, 2 = vintage glass.", 1.0f);		desc.AddMember(&SCineLensParams::bUseSpriteBokeh,			'sbok', "UseSpriteBokeh", "Use Sprite Bokeh",			"Add scattered bokeh sprites for out-of-focus HIGHLIGHTS on top of the normal depth "			"of field blur, the way film builds them up: bright points grow into readable discs "			"with a bright rim instead of being averaged flat. The base blur is unchanged, so "			"scenes without bright highlights look the same either way. Required for Bokeh "			"Shape Texture to have any effect.", false);		desc.AddMember(&SCineLensParams::bokehShapeTex,			'bktx', "BokehShapeTexture", "Bokeh Shape Texture",			"Silhouette (red channel) of a physical mask mounted in front of the lens. Iris "			"opened wider than Mask Aperture: every out-of-focus highlight is this shape "			"alone, edge glow included. Stopped down past it: the blade polygon takes over "			"completely. The switch is a clean crossover at Mask Aperture. Empty = procedural "			"disc. Only used when Use Sprite Bokeh is on.", Schematyc::TextureFileName());		desc.AddMember(&SCineLensParams::maskAperture,			'mskf', "MaskAperture", "Mask Aperture",			"Equivalent f-number of the shape mask's opening - the crossover point of the two "			"apertures. F-numbers below this value (iris wider): the mask is the smaller hole "			"and its shape rules every highlight. F-numbers above it (iris narrower): the "			"blade polygon rules and the mask is invisible. Only used when a Bokeh Shape "			"Texture is set.", 2.0f);		desc.AddMember(&SCineLensParams::maskOpenArea,			'mska', "MaskOpenArea", "Mask Open Area",			"Transmittance of the mask's OPEN parts (a tinted gel or scrim in the cut-out; 1 = "			"clear). The blocked part is measured from the texture itself: the light reaching the "			"sensor is the area of iris-intersect-mask over the iris area, at every f-number, "			"times this value - exposure and film grain react like a real neutral density loss, "			"and depth of field / bokeh SIZE follow whichever hole is the smaller one.", 1.0f);		desc.AddMember(&SCineLensParams::maskRotation,			'mskr', "MaskRotation", "Mask Rotation (deg)",			"Rotation of the shape mask around the optical axis. The mask is its own element, so "			"this is independent of Blade Rotation (the iris phase): a note cut-out can be turned "			"while the blade polygon and the sunstars stay put. Applies to the splatted "			"silhouette and to the soft gather halo alike.", 0.0f);		desc.AddMember(&SCineLensParams::maxAperture,			'mxap', "MaxAperture", "Maximum Aperture",			"The lens's wide-open f-number. Sets how big the pupil is relative to the barrel, i.e. "			"where the blades disappear into it: at this f-number they are fully retracted, the "			"pupil is the round barrel, and the glare is rings rather than rays - the sunstars "			"only appear as the polygon emerges from the barrel on the way down. Cat-eye and coma "			"are likewise strongest here and fade as the iris is stopped down from it. Does not "			"clamp Aperture.", 1.4f);		desc.AddMember(&SCineLensParams::catEye,			'ctey', "CatEye", "Cat-eye",			"Mechanical clipping of the pupil by the lens barrel toward the frame edges: corner "			"bokeh becomes lens-shaped (long axis tangential), edges darken and their bokeh dims, "			"all fading as the iris closes. 0.2-0.4 natural, 1 caricature. Replaces the guessed "			"mechanical part of Vignette Aperture Link.", 0.0f);		desc.AddMember(&SCineLensParams::coma,			'coma', "Coma", "Coma",			"Off-axis highlights grow radial comet tails, strongest wide open, gone when stopped "			"down.", 0.0f);		desc.AddMember(&SCineLensParams::astigmatism,			'astg', "Astigmatism", "Astigmatism",			"Radial/tangential focus split: off-axis blur stretches radially on one side of focus "			"and tangentially on the other. Sign swaps which side.", 0.0f);		desc.AddMember(&SCineLensParams::fieldCurvature,			'fcrv', "FieldCurvature", "Field Curvature",			"Curved focal surface: with the centre in focus the edges focus at a different "			"distance. Positive = edges focus closer (a flat wall goes soft toward the edges, "			"far-blurred), negative = edges focus further. Hidden by stopping down, like any "			"focus error.", 0.0f);		desc.AddMember(&SCineLensParams::edgeSoftness,			'esft', "EdgeSoftness", "Edge Softness",			"Residual off-axis aberration blur: edges stay soft even when in focus, strongest at "			"Maximum Aperture and gone stopped down. The \"sharp centre, mushy corners\" of cheap "			"and vintage glass.", 0.0f);		desc.AddMember(&SCineLensParams::chromaticAberration,			'chra', "ChromaticAberration", "Chromatic Aberration",			"Lateral colour fringing: red and blue are magnified differently from green, so "			"high-contrast edges pick up red/cyan fringes that grow toward the frame edges. "			"Zero in the centre.", 0.0f);		desc.AddMember(&SCineLensParams::axialChromatic,			'axch', "AxialChromatic", "Axial Chromatic Aberration",			"Focus differs by wavelength: out-of-focus highlights behind the focus plane get a "			"green rim, in front of it a magenta one, in the centre of the frame as much as at "			"the edges. The bokeh fringing of fast and vintage lenses. Hidden by stopping down "			"like any focus error.", 0.0f);		desc.AddMember(&SCineLensParams::sphericalAberration,			'sabr', "SphericalAberration", "Spherical Aberration",			"Intensity profile ACROSS the bokeh disc, not its outline: rays through the rim of "			"the pupil focus at a different distance from the ones through its centre, so on one "			"side of focus the light piles up at the edge of the disc and on the other it smears "			"into the middle. Positive = bright rim behind focus, soft in front (over-corrected, "			"soap-bubble bokeh). Negative = soft behind, bright rim in front (the STF / DC "			"apodized look). 0 = a flat disc. Needs Lens Model DOF.", 0.0f);	}	Schematyc::Range<1, 2400> focalLength = 50.f;	EFocalStepMode focalStepMode = EFocalStepMode::PrimeSet;	Schematyc::Range<1, 24, 2, 12, int> focalStepsPerDoubling = 6;	Schematyc::Range<0, 2, 0, 1> focalStepTime = 0.25f;	Schematyc::Range<1, 64> aperture = 5.6f;	Schematyc::Range<3, 16, 5, 12, int> apertureBlades = 9;	Schematyc::Range<0, 1, 0, 1> bladeCurvature = 0.4f;	Schematyc::Range<0, 1, 0, 1> transmission = 1.0f;	Schematyc::Range<0, 10, 0, 6, int> ndFilterStops = 0;	Schematyc::Range<0, 10000> focusDistance = 5.f;	Schematyc::Range<0, 2, 0, 1> vignetteAmount = 0.0f;	Schematyc::Range<0, 1, 0, 1> vignetteApertureLink = 0.0f;	Schematyc::Range<-1, 1, -1, 1> distortion = 0.0f;	Schematyc::Range<-1, 1, -1, 1> distortionFocalLink = 0.0f;	Schematyc::Range<0, 2, 0, 1> breathingIntensity = 1.0f;	bool bUseSpriteBokeh = false;	Schematyc::Range<1, 22, 1, 8> maskAperture = 2.0f;	Schematyc::Range<0, 1, 0, 1> maskOpenArea = 1.0f;	Schematyc::Range<0, 360, 0, 360> maskRotation = 0.f;	// Field-dependent pupil. Kept in front of bokehShapeTex, which has to stay last.	Schematyc::Range<1, 22, 1, 8> maxAperture = 1.4f;	Schematyc::Range<0, 1, 0, 1> catEye = 0.0f;	Schematyc::Range<0, 1, 0, 1> coma = 0.0f;	Schematyc::Range<-1, 1, -1, 1> astigmatism = 0.0f;	// Lens flaws riding the same field vector (LensFlawsSpec.md). Chromatic aberration is a	// cvar rather than a DOF post param: it is distortion done per wavelength and lives in the	// PostAA composite's coordinate chain, next to Distortion and the letterbox.	Schematyc::Range<-1, 1, -1, 1> fieldCurvature = 0.0f;	Schematyc::Range<0, 1, 0, 1> edgeSoftness = 0.0f;	Schematyc::Range<0, 1, 0, 1> chromaticAberration = 0.0f;	// Axial (longitudinal) chromatic aberration, AxialChromaticSpec.md. The other half of the	// colour error: chromaticAberration above is a magnification difference and lives in the	// PostAA composite, this one is a FOCUS difference and lives in the depth of field.	Schematyc::Range<0, 1, 0, 1> axialChromatic = 0.0f;	// Spherical aberration (DofLensModelSpec.md 3.2). The one lens property that only a SHARED	// pupil function could express: it is an intensity profile over the pupil, so the gather,	// the sprite splats and the ghosts all have to evaluate the same weighting or the halo and	// the sprite on top of it disagree about where the light in a disc sits.	Schematyc::Range<-1, 1, -1, 1> sphericalAberration = 0.0f;	// Keep last: operator== memcmp's the members in front of this one.	Schematyc::TextureFileName bokehShapeTex;};struct SCineExposureParams{	inline bool operator==(const SCineExposureParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineExposureParams>& desc)	{		desc.SetGUID("{3D9082C6-5F74-4E3A-BC81-042DA6E37F93}"_cry_guid);		desc.SetLabel("Exposure");		desc.AddMember(&SCineExposureParams::bEnableExposure,			'expo', "EnableExposure", "Enable Exposure Control",			"Override HDR eye-adaptation EV compensation.", false);		desc.AddMember(&SCineExposureParams::exposureMode,			'expm', "ExposureMode", "Exposure Mode",			"Manual locks exposure to the aperture / shutter / ISO triangle.",			EExposureMode::AutoBiased);		desc.AddMember(&SCineExposureParams::exposureResponse,			'xrsp', "ExposureResponse", "Exposure Response",			"Engine stops per photographic stop, compressed around Neutral EV. 1 = strict physics "			"(each aperture stop doubles/halves brightness, which reads as extreme on screen); "			"0.5-0.7 matches the tone response of a real camera's processed image.", 0.6f);		desc.AddMember(&SCineExposureParams::neutralEV,			'nuev', "NeutralEV", "Neutral EV",			"EV100 that renders as a neutral image; the response compression pivots here.", 9.0f);		desc.AddMember(&SCineExposureParams::evCompensation,			'evcp', "EVCompensation", "EV Compensation",			"EV bias (-3 darker ??0 neutral ??+3 brighter). In Manual mode this is a true stop.", 0.0f);		// Scene-referred AUTO controls. New members with new CRCs, because they are genuinely new		// controls - the shipped parameters keep their names, CRCs and defaults exactly		// (research/s1-ccam-exposure-today.md section 5). They do nothing off the switch.		desc.AddMember(&SCineExposureParams::bAutoLock,			'alck', "AutoExposureLock", "Auto Exposure Lock",			"Scene-referred AUTO only: freeze the metered EV where it is, like a camera's AE lock. "			"Exposure compensation and the aperture still apply on top.", false);		desc.AddMember(&SCineExposureParams::autoTau,			'atau', "AutoExposureTau", "Auto Exposure Response Time",			"Scene-referred AUTO only: seconds for the metered EV to cover 63 % of a change. "			"A cut always snaps instantly. 0 = no smoothing at all.", 1.0f);		desc.AddMember(&SCineExposureParams::autoDarkenTime,			'adkt', "AutoExposureDarkenTime", "Auto Darken Time",			"Scene-referred AUTO only: seconds for the metered EV to cover 63 % of a change when "			"the scene gets BRIGHTER and the picture therefore has to darken - walking out of a "			"hangar into the sun. Half of Response Time by default, because the problem is "			"asymmetric and a single time constant cannot serve both halves of it: a real camera "			"stops down at once and the eye adapts to light in about a fifth of a second, while "			"adapting to dark takes minutes. Response Time still governs the other direction. "			"0 = use Response Time for both, which is the old symmetric behaviour.", 0.5f);		desc.AddMember(&SCineExposureParams::meteringMode,			'mtmd', "MeteringMode", "Metering Mode",			"Scene-referred AUTO only: WHERE in the frame the camera meters. Average reads the "			"whole frame uniformly; Centre-weighted keeps full sensitivity in the middle and a "			"quarter of it in the corners, which is what stops a bright sky from stopping the "			"subject down; Spot reads a soft-edged disc a fifth of the frame height across, so "			"you can put it on a face or a grey card and let everything else fall where it falls.",			EMeteringMode::CenterWeighted);		desc.AddMember(&SCineExposureParams::meterMinPercent,			'mtlo', "MeterMinPercent", "Metering Band Low",			"Scene-referred AUTO only: discard everything below this percentile of metering "			"weight before averaging. 50 throws away the darker half of the frame, which is "			"shadow detail nobody meters for and which is exactly what drags an outdoor scene "			"below mid grey. 0 keeps the whole frame.", 50.0f);		desc.AddMember(&SCineExposureParams::meterMaxPercent,			'mthi', "MeterMaxPercent", "Metering Band High",			"Scene-referred AUTO only: discard everything above this percentile. 90 throws away "			"the top decile - sky, glints, the sun itself - so panning past the sun is a "			"non-event instead of a stop-down. 100 keeps the whole frame. The camera exposes so "			"that the log-mean of what is LEFT between the two lands on mid grey.", 90.0f);		desc.AddMember(&SCineExposureParams::autoEVMin,			'aevn', "AutoExposureEVMin", "Auto Exposure EV Min",			"Scene-referred AUTO only: the brightest the camera will open up to (low EV = open).", -2.0f);		desc.AddMember(&SCineExposureParams::autoEVMax,			'aevx', "AutoExposureEVMax", "Auto Exposure EV Max",			"Scene-referred AUTO only: the darkest the camera will stop down to (high EV = closed).", 20.0f);		desc.AddMember(&SCineExposureParams::sensorClipStops,			'sclp', "SensorClipStops", "Sensor Clip (stops over grey)",			"Scene-referred only: the stops over mid grey at which the sensor saturates. "			"Highlights are clipped per channel at this level, AFTER the white balance, so a "			"clipped highlight stays white at every colour temperature instead of taking the "			"balance's tint. Real sensors hold 6-8 stops over grey (the advertised '14+ stops' "			"is the TOTAL range, most of it BELOW grey). Above about 10 the display transform "			"has already turned everything white on its own, so higher values are visible only "			"in the EXR negative. Lower = a smaller, earlier-clipping sensor (more flat white "			"sky). The lens effects upstream - bloom, glare, depth of field - are deliberately "			"NOT clipped: the lens sees the real light.", 8.0f);	}	bool bEnableExposure = false;	bool bAutoLock = false;	EExposureMode exposureMode = EExposureMode::AutoBiased;	Schematyc::Range<0, 1, 0, 1> exposureResponse = 0.6f;	Schematyc::Range<-6, 24, 4, 14> neutralEV = 9.0f;	Schematyc::Range<-3, 3, -3, 3> evCompensation = 0.f;	Schematyc::Range<0, 10, 0, 5> autoTau = 1.0f;	Schematyc::Range<0, 10, 0, 5> autoDarkenTime = 0.5f;	EMeteringMode meteringMode = EMeteringMode::CenterWeighted;	Schematyc::Range<0, 100, 0, 100> meterMinPercent = 50.0f;	Schematyc::Range<0, 100, 0, 100> meterMaxPercent = 90.0f;	Schematyc::Range<-6, 24, -6, 24> autoEVMin = -2.0f;	Schematyc::Range<-6, 24, -6, 24> autoEVMax = 20.0f;	Schematyc::Range<4, 12, 4, 12> sensorClipStops = 8.0f;};struct SCineDofParams{	inline bool operator==(const SCineDofParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineDofParams>& desc)	{		desc.SetGUID("{4EA193D7-6085-4F4B-CD92-153EB7F480A4}"_cry_guid);		desc.SetLabel("Depth Of Field");		desc.AddMember(&SCineDofParams::bEnableDOF,			'dof_', "EnableDOF", "Enable Depth of Field",			"Compute and apply DOF blur from aperture + focus distance.", false);		desc.AddMember(&SCineDofParams::bLensModelDof,			'lmdf', "LensModelDof", "Lens Model DOF",			"Drive the depth of field from the thin lens itself - focal length, f-number, focus "			"distance and sensor size - instead of the engine's focus-range ramp. The blur then "			"follows the optics exactly (stopping down widens the sharp band, a long lens blurs "			"more than a short one at the same f-number, focusing far makes the background go "			"nearly sharp), the gather is weighted by the energy each point actually scatters so "			"bokeh discs dim as they grow, foreground edges are translucent in proportion to "			"their blur, and the depth of field itself stays NATIVELY sharp. Off = the stock "			"engine depth of field, fed with Focus Falloff and the derived blur amount.", true);		desc.AddMember(&SCineDofParams::focusFalloff,			'ffal', "FocusFalloff", "Focus Falloff",			"How far past the depth of field limits the blur keeps ramping. 1 = fully blurred "			"right at the limits (the engine default, which reads as a very thin focus zone); "			"higher values widen the sharp zone. STOCK DOF PATH ONLY - ignored by Lens Model "			"DOF, which has a real focus plane and needs no fudge factor to keep it sharp.", 3.0f);		desc.AddMember(&SCineDofParams::highlightThreshold,			'hlth', "HighlightThreshold", "Highlight Threshold",			"Scene HDR luminance a defocused pixel must exceed before it throws a bokeh sprite "			"on top of the ordinary blur. Lower = more pixels splat. A soft knee of +/-50% "			"around this value fades them in, so they do not pop as the aperture moves. "			"Only used when Use Sprite Bokeh is on. "			"WITH SCENE REFERRED ON this number is in STOPS ABOVE MID GREY, not an absolute value: "			"0 = a correctly exposed grey card, +3 = eight times that. The setting then keeps its "			"meaning when the aperture or the ND changes, which an absolute number cannot. "			"The shipped 2.0 then means four times mid grey.", 2.0f);		desc.AddMember(&SCineDofParams::highlightSplatGain,			'hlgn', "HighlightSplatGain", "Highlight Splat Gain",			"Brightness of each bokeh sprite. A splat spreads a fixed amount of energy across "			"its disc, so this scales how much of it is added back on top of the blur. Raise it "			"if shaped bokeh reads too faint against the base blur; the renderer already "			"compensates for how much of the disc a shape texture actually covers.", 3.0f);	}	bool bEnableDOF = false;	// Kept next to bEnableDOF rather than appended: two bools side by side add no new padding	// hole to memcmp over. Schematyc keys saved values by member id, so the order is free.	bool bLensModelDof = true;	Schematyc::Range<1, 10, 1, 6> focusFalloff = 3.f;	Schematyc::Range<0, 20, 0, 8> highlightThreshold = 2.0f;	Schematyc::Range<0, 8, 0, 4> highlightSplatGain = 3.0f;};struct SCineGrainParams{	inline bool operator==(const SCineGrainParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineGrainParams>& desc)	{		desc.SetGUID("{5FB2A4E8-7196-405C-DEA3-264FC80591B5}"_cry_guid);		desc.SetLabel("Film Grain");		desc.AddMember(&SCineGrainParams::bEnableGrain,			'gren', "EnableFilmGrain", "Enable Film Grain",			"Drive the film grain post effect from ISO, aperture and shutter, following a "			"real sensor noise curve: clean at base ISO, climbing steeply past ISO 6400, "			"boosted when the lens is starved of light.", true);		desc.AddMember(&SCineGrainParams::grainStrength,			'grst', "GrainStrength", "Grain Strength",			"Scales the computed grain before it reaches the renderer.", 1.0f);	}	bool bEnableGrain = true;	Schematyc::Range<0, 4, 0, 1> grainStrength = 1.0f;};struct SCineMotionBlurParams{	inline bool operator==(const SCineMotionBlurParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineMotionBlurParams>& desc)	{		desc.SetGUID("{60C3B5F9-82A7-416D-EFB4-3750D91602C6}"_cry_guid);		desc.SetLabel("Motion Blur");		desc.AddMember(&SCineMotionBlurParams::bEnableMotionBlur,			'mblr', "EnableMotionBlur", "Enable Motion Blur",			"Drive the renderer's motion blur shutter from the camera shutter, so 1/50 gives the "			"classic 180-degree film blur and fast shutters give crisp, staccato motion.", true);	}	bool bEnableMotionBlur = true;};struct SCineWhiteBalanceParams{	inline bool operator==(const SCineWhiteBalanceParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineWhiteBalanceParams>& desc)	{		desc.SetGUID("{71D4C60A-93B8-427E-F0C5-4861EA2713D7}"_cry_guid);		desc.SetLabel("White Balance");		desc.AddMember(&SCineWhiteBalanceParams::bEnableWhiteBalance,			'wbon', "EnableWhiteBalance", "Enable White Balance",			"Apply the camera's colour temperature on top of the scene grading. 6500K is neutral; "			"lower reads blue (tungsten setting in daylight), higher reads warm.", true);		desc.AddMember(&SCineWhiteBalanceParams::colorTemperature,			'kelv', "ColorTemperature", "Color Temperature (K)",			"White balance in Kelvin. 3200 tungsten, 5600 daylight, 6500 neutral.", 6500);		desc.AddMember(&SCineWhiteBalanceParams::tint,			'wbtn', "Tint", "Tint",			"The other axis of white balance: the green/magenta correction a temperature alone "			"cannot make, for light that is off the blackbody locus (fluorescent, LED, foliage "			"bounce). Positive is magenta, negative is green; 0 is on the locus. Only reaches "			"the picture on the scene-referred path, where white balance is a Bradford "			"adaptation before the display transform.", 0.0f);	}	bool bEnableWhiteBalance = true;	Schematyc::Range<1500, 15000, 2500, 10000, int> colorTemperature = 6500;	Schematyc::Range<-1, 1, -1, 1> tint = 0.0f;};// The camera's grade (SceneReferredSpec.md S5). ASC CDL in ACEScct, the interchange every// grading tool speaks, applied to scene data before the output transform - not to a picture// after it. Neutral by default: with these values the chain is exactly ACES 2.0 (D10).struct SCineGradeParams{	inline bool operator==(const SCineGradeParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineGradeParams>& desc)	{		desc.SetGUID("{4C81D3E6-7A29-4F05-B6D4-92E37C0158BA}"_cry_guid);		desc.SetLabel("Grade");		desc.AddMember(&SCineGradeParams::bBypassGrade,			'gbyp', "BypassGrade", "Bypass Grade",			"Switch the whole camera grade out of the chain - white balance, CDL and look - and "			"leave only the output transform. The A/B: what the shot looks like ungraded, one "			"click away, with the exposure and the optics unchanged.", false);		desc.AddMember(&SCineGradeParams::slope,			'cdls', "Slope", "Slope (RGB)",			"ASC CDL slope, per channel. A multiply in log - gain, the highlight end. 1 = "			"neutral. Applied first: out = (in * slope + offset) ^ power.", Vec3(1.0f, 1.0f, 1.0f));		desc.AddMember(&SCineGradeParams::offset,			'cdlo', "Offset", "Offset (RGB)",			"ASC CDL offset, per channel. An add in log - lift, the shadow end, and the usual "			"place a colour cast is corrected. 0 = neutral.", Vec3(0.0f, 0.0f, 0.0f));		desc.AddMember(&SCineGradeParams::power,			'cdlp', "Power", "Power (RGB)",			"ASC CDL power, per channel. Gamma, the midtones, applied last. 1 = neutral; below 1 "			"opens the mids, above 1 closes them.", Vec3(1.0f, 1.0f, 1.0f));		desc.AddMember(&SCineGradeParams::saturation,			'cdlt', "Saturation", "Saturation",			"The ASC's saturation extension, applied after the slope/offset/power triple about "			"the luminance of the graded value. 1 = neutral, 0 = monochrome. Because this "			"happens in the working space and before the output transform, pushing it does not "			"drift the hues the way a saturation slider on a finished picture does.", 1.0f);	}	bool bBypassGrade = false;	Vec3 slope = Vec3(1.0f, 1.0f, 1.0f);	Vec3 offset = Vec3(0.0f, 0.0f, 0.0f);	Vec3 power = Vec3(1.0f, 1.0f, 1.0f);	Schematyc::Range<0, 4, 0, 2> saturation = 1.0f;};struct SCineStreaksParams{	inline bool operator==(const SCineStreaksParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineStreaksParams>& desc)	{		desc.SetGUID("{82E5D71B-A4C9-438F-01D6-5972FB3824E8}"_cry_guid);		desc.SetLabel("Diffraction Streaks");		desc.AddMember(&SCineStreaksParams::bEnableStreaks,			'strk', "EnableStreaks", "Enable Diffraction Streaks",			"Grow sunstars out of very bright sources, the way a real iris diffracts light off "			"its blade edges: invisible wide open, appearing around f/8 and sharp by f/16. Ray "			"count follows Aperture Blades - even blade counts give one ray per blade, odd "			"counts give two. More blades therefore means THINNER and FAINTER rays, not a "			"brighter star: the same diffracted light is divided among more of them.", true);		desc.AddMember(&SCineStreaksParams::bladeRotation,			'blrt', "BladeRotation", "Blade Rotation (deg)",			"Phase of the iris, rotating the whole set of rays. Rays are spread evenly, so the "			"pattern repeats every 360 / ray count degrees.", 0.0f);		desc.AddMember(&SCineStreaksParams::mode,			'stMd', "StreakMode", "Streak Model",			"How the sunstar is computed. Wave optics transforms the actual pupil - blade count, "			"blade curvature, shape mask, spherical aberration - into its diffraction pattern, so "			"9 blades give 18 rays, a star mask gives the mask's own pattern, and stopping down "			"lengthens the rays by itself. Analytic paints the older line pairs with a hand-tuned "			"f-number ramp; keep it for an A/B or when the wave kernel is switched off.",			EStreakMode::Wave);		desc.AddMember(&SCineStreaksParams::streakAmount,			'stAm', "StreakAmount", "Streak Amount",			"Multiplier on the diffracted light in the Wave model. 1 takes the source's HDR "			"value as its true radiance, which gives the physically exact but nearly invisible "			"pattern - the rays carry only about half a percent of the light, spread over "			"hundreds of pixels. Game lights are clamped far below what a real lamp or the sun "			"actually is, so 10-100 reproduces the sunstar a real source of that kind shows. The "			"pattern's SHAPE and falloff stay physical at every value. Analytic ignores this: "			"there the f-number ramp still owns the amount. "			"WITH SCENE REFERRED ON the sources carry their real brightness and this becomes GAIN "			"IN STOPS: ten on the slider is one stop, and the shipped 50 sits exactly on the "			"physical 1.0. So 0 = off, 50 = physical, 100 = +5 stops, 150 = +10, 200 = +15. The "			"range is that wide because the physics needs it - a 200 kcd/m2 practical at a "			"daylight exposure puts its rays some fifteen stops under mid grey, which is right "			"(nobody sees a sunstar off a desk lamp at noon), while the same lamp at a night "			"exposure needs nothing added at all. For rays off an ordinary daylight practical, "			"try 130-150.",			50.0f);		desc.AddMember(&SCineStreaksParams::reach,			'stRc', "StreakReach", "Streak Reach",			"How far the rays are drawn, as a fraction of frame height. This is where the faint "			"far-field tail is CUT OFF, not how bright it is: a cost and coverage choice, not a "			"property of the lens. Wave model only.", 0.35f);		desc.AddMember(&SCineStreaksParams::dispersion,			'stDs', "StreakDispersion", "Streak Dispersion",			"Spread of the rays' rainbow. Every wavelength diffracts to a different size, which is "			"why a real ray runs blue near the source and red at the tip. 0 = one white pattern at "			"550 nm. Wave model only.", 1.0f);	}	bool bEnableStreaks = true;	Schematyc::Range<0, 360, 0, 360> bladeRotation = 0.f;	EStreakMode mode = EStreakMode::Wave;	Schematyc::Range<0, 200, 0, 100> streakAmount = 50.0f;	Schematyc::Range<0, 1, 0, 1> reach = 0.35f;	Schematyc::Range<0, 1, 0, 1> dispersion = 1.0f;};struct SCineAnamorphicParams{	inline bool operator==(const SCineAnamorphicParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineAnamorphicParams>& desc)	{		desc.SetGUID("{93F6E82C-B5DA-4490-12E7-6A83AC4935F9}"_cry_guid);		desc.SetLabel("Anamorphic");		desc.AddMember(&SCineAnamorphicParams::squeeze,			'sqz_', "Squeeze", "Squeeze Factor",			"1 = spherical. 1.33 / 1.5 / 1.8 / 2 = anamorphic: the horizontal capture widens by "			"this factor for the same focal length, out-of-focus discs become vertical ovals of "			"this ratio, and the frame is masked to the desqueezed format (sensor width x squeeze "			"/ sensor height) with black bars whenever that format is wider than the viewport, so "			"the full horizontal field of view is kept.", 1.0f);		desc.AddMember(&SCineAnamorphicParams::flareAmount,			'afla', "FlareAmount", "Horizontal Flare",			"Strength of the anamorphic streak: the long horizontal line thrown across the frame "			"by bright sources off the cylindrical front element. 0 = off. Strongest wide open, "			"fading out by f/11. Independent of the diffraction streaks.", 0.0f);		desc.AddMember(&SCineAnamorphicParams::flareLength,			'afln', "FlareLength", "Flare Length",			"How far the line reaches across the frame.", 0.7f);		desc.AddMember(&SCineAnamorphicParams::flareThreshold,			'afth', "FlareThreshold", "Flare Threshold",			"Scene HDR value a source must exceed to throw a flare. "			"WITH SCENE REFERRED ON this number is in STOPS ABOVE MID GREY, not an absolute value: "			"0 = a correctly exposed grey card, +3 = eight times that. The setting then keeps its "			"meaning when the aperture or the ND changes, which an absolute number cannot. ", 3.0f);		desc.AddMember(&SCineAnamorphicParams::flareTint,			'aftn', "FlareTint", "Flare Tint",			"Colour of the flare. Classic anamorphic coatings give the cyan-blue line; a neutral "			"tint gives white.", ColorF(0.35f, 0.62f, 1.0f, 1.0f));		desc.AddMember(&SCineAnamorphicParams::bSqueezedFrame,			'asqf', "SqueezedFrame", "Show Squeezed Frame",			"Present the raw anamorphic negative instead of the projected frame: the band the "			"gate mask would keep is stretched vertically to fill the viewport, so the whole "			"picture carries the squeeze - circles become tall ovals, faces go thin - not just "			"the bokeh. The camera frustum is identical to the masked frame; only the final "			"presentation differs. Off = the physically projected (desqueezed) frame with bars.", false);	}	Schematyc::Range<1, 2, 1, 2> squeeze = 1.0f;	bool bSqueezedFrame = false;	Schematyc::Range<0, 3, 0, 2> flareAmount = 0.0f;	Schematyc::Range<0, 1, 0, 1> flareLength = 0.7f;	Schematyc::Range<0, 50, 0, 20> flareThreshold = 3.0f;	ColorF flareTint = ColorF(0.35f, 0.62f, 1.0f, 1.0f);};struct SCineHalationParams{	inline bool operator==(const SCineHalationParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineHalationParams>& desc)	{		desc.SetGUID("{A407F93D-C6EB-45A1-23F8-7B94BD5A46FA}"_cry_guid);		desc.SetLabel("Halation");		desc.AddMember(&SCineHalationParams::amount,			'hamt', "Halation", "Halation",			"Red-orange halo around sources that overload the emulsion: light passes through the "			"film, reflects off the base and re-exposes the red-sensitive layer around the source. "			"A film-stock property, so it does not follow the f-number. 0 = off.", 0.0f);		desc.AddMember(&SCineHalationParams::threshold,			'hthr', "HalationThreshold", "Halation Threshold",			"Scene HDR value a source must exceed to halate. High on purpose: a bright sky must "			"not halate, only sources that overload the emulsion. "			"WITH SCENE REFERRED ON this number is in STOPS ABOVE MID GREY, not an absolute value: "			"0 = a correctly exposed grey card, +3 = eight times that. The setting then keeps its "			"meaning when the aperture or the ND changes, which an absolute number cannot. ", 2.0f);		desc.AddMember(&SCineHalationParams::radius,			'hrad', "HalationRadius", "Halation Radius",			"How far the halo spreads from the source.", 0.5f);		desc.AddMember(&SCineHalationParams::tint,			'htnt', "HalationTint", "Halation Tint",			"Colour of the halo. Red-orange for colour negative stock (the red layer sits "			"deepest); a neutral tint mimics a stock whose anti-halation backing is partly "			"working.", ColorF(1.0f, 0.35f, 0.15f, 1.0f));	}	Schematyc::Range<0, 3, 0, 2> amount = 0.0f;	Schematyc::Range<0, 50, 0, 20> threshold = 2.0f;	Schematyc::Range<0, 1, 0, 1> radius = 0.5f;	ColorF tint = ColorF(1.0f, 0.35f, 0.15f, 1.0f);};// Front filter (FilterPSFSpec.md). A plate screwed onto the front of the lens - etched grooves// for a star filter, or any other authored point spread function. Every bright point in the// frame is convolved with it, at every f-number: this is glass in front of the lens, not the// iris, so nothing here follows the aperture.struct SCineFilterParams{	// psfTexture holds a string, so only the POD prefix in front of it can be memcmp'd -	// exactly as SCineLensParams does for its bokeh shape texture.	inline bool operator==(const SCineFilterParams& rhs) const	{		return 0 == memcmp(this, &rhs, offsetof(SCineFilterParams, psfTexture))		       && psfTexture.value == rhs.psfTexture.value;	}	static void ReflectType(Schematyc::CTypeDesc<SCineFilterParams>& desc)	{		desc.SetGUID("{7D4B0E96-31CA-4F58-6BD2-84E17C0A9F63}"_cry_guid);		desc.SetLabel("Front Filter");		desc.AddMember(&SCineFilterParams::starAmount,			'flAm', "StarAmount", "Star Amount",			"Strength of the star / PSF filter: a plate of etched grooves in front of the lens "			"diffracts every bright point into lines, at every f-number (unlike the iris "			"sunstars, which need stopping down). 0 = off (no pass runs).", 0.0f);		desc.AddMember(&SCineFilterParams::starPoints,			'flPt', "StarPoints", "Star Points",			"Points of the procedural star when no PSF texture is set: 4, 6 or 8 (odd values "			"round down). Each pair of points is one groove direction.", 4);		desc.AddMember(&SCineFilterParams::starSize,			'flSz', "StarSize", "Star Size",			"Reach of the pattern, as a fraction of the frame height. Brighter sources show "			"more of it - the pattern is fixed, only its faint tail rises above the scene.", 0.25f);		desc.AddMember(&SCineFilterParams::starRotation,			'flRt', "StarRotation", "Star Rotation (deg)",			"Turning the filter turns the star.", 0.0f);		desc.AddMember(&SCineFilterParams::starThreshold,			'flTh', "StarThreshold", "Star Threshold",			"Scene HDR luminance a source needs to spawn the pattern. "			"WITH SCENE REFERRED ON this number is in STOPS ABOVE MID GREY, not an absolute value: "			"0 = a correctly exposed grey card, +3 = eight times that. The setting then keeps its "			"meaning when the aperture or the ND changes, which an absolute number cannot. ", 6.0f);		desc.AddMember(&SCineFilterParams::psfTexture,			'flTx', "PSFTexture", "PSF Texture",			"RGB point-spread image of the filter: what one bright point looks like through it, "			"centred, black outside the pattern. Covers any front-element PSF a preset wants - "			"a dirty or smeared filter, a custom flare plate. Same asset type as the bokeh shape "			"masks: an uncompressed 32-bit DDS, e.g. textures/lights/lens_flares/filter_star6.dds. "			"Empty = procedural star.",			Schematyc::TextureFileName());	}	Schematyc::Range<0, 2, 0, 1> starAmount = 0.0f;	Schematyc::Range<4, 8, 4, 8, int> starPoints = 4;	Schematyc::Range<0, 1, 0, 1> starSize = 0.25f;	Schematyc::Range<0, 360, 0, 360> starRotation = 0.f;	Schematyc::Range<1, 64, 2, 16> starThreshold = 6.0f;	// Keep last: operator== memcmp's the members in front of this one.	Schematyc::TextureFileName psfTexture;};struct SCineGhostParams{	inline bool operator==(const SCineGhostParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineGhostParams>& desc)	{		desc.SetGUID("{C61AD84F-E82B-47C3-56BA-9CB6DF6C58BC}"_cry_guid);		desc.SetLabel("Lens Ghosts");		desc.AddMember(&SCineGhostParams::amount,			'ghAm', "GhostAmount", "Amount",			"Strength of the internal-reflection ghosts: a bright source bounces between the "			"lens surfaces and lands on the sensor as a chain of images of the aperture. "			"0 = off (no pass runs). Modern multi-coated glass around 0.2, vintage around 0.6, "			"above 1 for show.", 0.0f);		desc.AddMember(&SCineGhostParams::count,			'ghCn', "GhostCount", "Count",			"Number of ghosts per source - roughly the number of reflecting element pairs.", 3);		desc.AddMember(&SCineGhostParams::spread,			'ghSp', "GhostSpread", "Spread",			"How far the chain reaches from the centre toward the opposite side of the frame.", 1.0f);		desc.AddMember(&SCineGhostParams::size,			'ghSz', "GhostSize", "Size",			"Ghost diameter, 1 is about 5% of the frame height wide open. Shrinks with the iris "			"like the aperture it is an image of.", 1.0f);		desc.AddMember(&SCineGhostParams::threshold,			'ghTh', "GhostThreshold", "Threshold",			"Scene HDR luminance a source needs to spawn ghosts. Far above the bokeh threshold "			"on purpose: only lamps, the sun and hot reflections throw ghosts. "			"WITH SCENE REFERRED ON this number is in STOPS ABOVE MID GREY, not an absolute value: "			"0 = a correctly exposed grey card, +3 = eight times that. The setting then keeps its "			"meaning when the aperture or the ND changes, which an absolute number cannot. ", 8.0f);		desc.AddMember(&SCineGhostParams::tint,			'ghTn', "GhostTint", "Tint",			"Coating colour of the first ghost; the others fan out around it by Hue Spread.",			ColorF(0.6f, 0.8f, 1.0f, 1.0f));		desc.AddMember(&SCineGhostParams::hueSpread,			'ghHu', "GhostHueSpread", "Hue Spread",			"How far successive ghosts' colours rotate away from Tint (1 = a third of the "			"colour wheel per ghost).", 0.3f);	}	Schematyc::Range<0, 2, 0, 1> amount = 0.0f;	Schematyc::Range<1, 6, 1, 6, int> count = 3;	// Hard range 0.2 - 2 on the renderer side; the slider bounds are integers, so the widget	// starts at 0 and the stage clamps whatever arrives.	Schematyc::Range<0, 2, 0, 2> spread = 1.0f;	Schematyc::Range<0, 3, 0, 2> size = 1.0f;	Schematyc::Range<1, 64, 2, 16> threshold = 8.0f;	ColorF tint = ColorF(0.6f, 0.8f, 1.0f, 1.0f);	Schematyc::Range<0, 1, 0, 1> hueSpread = 0.3f;};struct SCineViewfinderParams{	inline bool operator==(const SCineViewfinderParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineViewfinderParams>& desc)	{		desc.SetGUID("{B518EA4E-D7FC-46B2-34A9-8CA5CE6B57AB}"_cry_guid);		desc.SetLabel("Viewfinder");		desc.AddMember(&SCineViewfinderParams::bShowGuides,			'vgon', "ShowGuides", "Frame Guides",			"Draw the selected framing guides over the frame. Viewfinder overlay, never part of "			"the picture.", false);		desc.AddMember(&SCineViewfinderParams::bThirds,			'vthd', "RuleOfThirds", "Rule Of Thirds", "", true);		desc.AddMember(&SCineViewfinderParams::bCentre,			'vctr', "CentreCross", "Centre Cross", "", true);		desc.AddMember(&SCineViewfinderParams::bSafeAreas,			'vsaf', "SafeAreas", "Safe Areas",			"Action safe (90%) and title safe (80%).", false);		desc.AddMember(&SCineViewfinderParams::bAspectGuide,			'vasp', "AspectGuide", "Aspect Guide",			"Outline the target aspect band and shade the outside.", false);		desc.AddMember(&SCineViewfinderParams::guideAspect,			'vgas', "GuideAspect", "Guide Aspect",			"Target aspect of the framing guide (2.39, 1.85, 1.33 ...).", 2.39f);		desc.AddMember(&SCineViewfinderParams::guideOpacity,			'vgop', "GuideOpacity", "Guide Opacity", "", 0.6f);		desc.AddMember(&SCineViewfinderParams::peaking,			'vpk_', "FocusPeaking", "Focus Peaking",			"Tint in-focus edges. 0 = off. Uses this camera's thin-lens depth of field as the "			"in-focus band.", 0.0f);		desc.AddMember(&SCineViewfinderParams::peakingColour,			'vpkc', "PeakingColour", "Peaking Colour",			"1 red, 2 green, 3 blue, 4 yellow, 5 white.", 1);		desc.AddMember(&SCineViewfinderParams::bOverlaysInGame,			'vgam', "OverlaysInGame", "Overlays In Game Mode",			"Keep the overlays on in game / simulation mode. Off = editor only, so a recording "			"never contains them.", false);	}	bool bShowGuides   = false;	bool bThirds       = true;	bool bCentre       = true;	bool bSafeAreas    = false;	bool bAspectGuide  = false;	Schematyc::Range<1, 4, 1, 3> guideAspect = 2.39f;	Schematyc::Range<0, 1, 0, 1> guideOpacity = 0.6f;	Schematyc::Range<0, 2, 0, 1> peaking = 0.0f;	Schematyc::Range<1, 5, 1, 5, int> peakingColour = 1;	bool bOverlaysInGame = false;};// Scene-referred pipeline (SceneReferredSpec.md). Its own group rather than a member of an// existing one: the later stages hang exposure, white balance, grade and output parameters off// this switch, and none of them belong to the lens or to the stock exposure block.struct SCineSceneReferredParams{	inline bool operator==(const SCineSceneReferredParams& rhs) const { return 0 == memcmp(this, &rhs, sizeof(rhs)); }	static void ReflectType(Schematyc::CTypeDesc<SCineSceneReferredParams>& desc)	{		desc.SetGUID("{0B4F1E27-9C3A-4D56-8E71-5A2C6F0D8B93}"_cry_guid);		desc.SetLabel("Scene Referred");		desc.AddMember(&SCineSceneReferredParams::bSceneReferred,			'scnr', "SceneReferred", "Scene Referred",			"Ask the renderer for the scene-referred pipeline: everything between the lights and "			"the display transform stays in scene units - real luminance, no [0,1], no display "			"encoding - so exposure, white balance, grading and the optical effects work on how "			"much light is in the scene instead of on a picture already squeezed onto a monitor. "			"Gated again by the r_SceneReferred cvar; off = the stock engine pipeline, byte for "			"byte. Default off while the pipeline is being built stage by stage.", false);	}	bool bSceneReferred = false;};// The look slot (LMT) and the output transform (ODT), the two .cube files of the display chain// (SceneReferredSpec.md S4/S5, D8). Both are plain asset paths rather than a resource picker:// a .cube is not a texture asset the RC knows about, so there is no selector to reuse, and the// files ship with the plugin. Paths are resolved through ICryPak - first as given (relative to// the project's asset directory), then under %ENGINE% - so the same string works in the editor// and in the launcher whichever tree the file was installed into.struct SCineLookParams{	inline bool operator==(const SCineLookParams& rhs) const { return lmtFile == rhs.lmtFile; }	static void ReflectType(Schematyc::CTypeDesc<SCineLookParams>& desc)	{		desc.SetGUID("{3A0C6D18-5E47-4B92-9C31-7F6E2D840A15}"_cry_guid);		desc.SetLabel("Look");		desc.AddMember(&SCineLookParams::lmtFile,			'lmtf', "LMTFile", "LMT File",			"Creative look, as a Resolve .cube in ACEScct - the Look Modification Transform. It "			"is applied AFTER the CDL and BEFORE the output transform, so it grades scene data "			"and not a picture. Empty = identity (pure ACES, no house look: SceneReferredSpec.md "			"D10). Author it in Resolve on an ACEScct timeline and export a 33-cube; "			"Assets/ODT/lmt_identity_33.cube is the pass-through reference.",			Schematyc::CSharedString());	}	// Keep last: operator== compares the string by value.	Schematyc::CSharedString lmtFile;};//! The shipped ACES 2.0 SDR 100 nit sRGB output transform. One definition, used as the member's//! initialiser, as the reflected default, and as the fallback when the field is empty.constexpr const char* kDefaultODTFile = "Assets/ODT/odt_srgb_100nit_aces2_65.cube";struct SCineOutputParams{	inline bool operator==(const SCineOutputParams& rhs) const { return odtFile == rhs.odtFile; }	static void ReflectType(Schematyc::CTypeDesc<SCineOutputParams>& desc)	{		desc.SetGUID("{6B2E9F40-18D7-4C63-A5B8-0E914C7D25AF}"_cry_guid);		desc.SetLabel("Output");		desc.AddMember(&SCineOutputParams::odtFile,			'odtf', "ODTFile", "ODT File",			"Output transform, as a Resolve .cube in ACEScct baked by tools/ocio-bake from the "			"ACES 2.0 studio config. THE one place in the chain that knows a display exists. "			"The default is the ACES 2.0 SDR 100 nit sRGB view; swapping this file changes only "			"what the monitor shows, never the scene data. Empty or unreadable = the picture goes "			"deliberately, visibly wrong (a clamped encode) rather than silently falling back. "			"Left empty, the built-in default below is loaded instead - an unset field is not a "			"statement about the display, and a component that has never been touched has to "			"come up looking right.",			Schematyc::CSharedString(kDefaultODTFile));	}	// Keep last: operator== compares the string by value.	// The in-class initialiser is what the RUNTIME reads. AddMember's default value feeds	// Schematyc's reflected default (graph nodes, "reset to default", the serializer's	// skip-if-equal test) and never touches the member itself, so a member without one is an	// empty string at activation no matter what the reflection says.	Schematyc::CSharedString odtFile = kDefaultODTFile;};class CCinematicCameraComponent final	: public IEntityComponent	, public ICinematicCameraOptics{public:	static void ReflectType(Schematyc::CTypeDesc<CCinematicCameraComponent>& desc)	{		desc.SetGUID("{F2A3B4C5-D6E7-4F8A-B9C0-1D2E3F4A5B6C}"_cry_guid);		desc.SetEditorCategory("Cameras");		desc.SetLabel("Cinematic Camera");		desc.SetDescription(			"Physically-based camera: FOV from focal length, DOF from aperture, "			"exposure compensation from EV bias. Automatically creates a companion "			"Camera component so the entity appears in the Camera Entity menu.");		desc.SetIcon("icons:General/Camera.ico");		desc.SetComponentFlags({			IEntityComponent::EFlags::Transform,			IEntityComponent::EFlags::Socket,			IEntityComponent::EFlags::Attach,			IEntityComponent::EFlags::Singleton,			IEntityComponent::EFlags::ClientOnly });		desc.AddMember(&CCinematicCameraComponent::m_body, 'gbdy', "CameraBody", "Camera Body", "", SCineBodyParams());		desc.AddMember(&CCinematicCameraComponent::m_sceneReferred, 'gscn', "SceneReferred", "Scene Referred", "", SCineSceneReferredParams());		desc.AddMember(&CCinematicCameraComponent::m_lens, 'glns', "Lens", "Lens", "", SCineLensParams());		desc.AddMember(&CCinematicCameraComponent::m_anamorphic, 'gana', "Anamorphic", "Anamorphic", "", SCineAnamorphicParams());		desc.AddMember(&CCinematicCameraComponent::m_exposure, 'gexp', "Exposure", "Exposure", "", SCineExposureParams());		desc.AddMember(&CCinematicCameraComponent::m_dof, 'gdof', "DepthOfField", "Depth Of Field", "", SCineDofParams());		desc.AddMember(&CCinematicCameraComponent::m_grain, 'ggrn', "FilmGrain", "Film Grain", "", SCineGrainParams());		desc.AddMember(&CCinematicCameraComponent::m_halation, 'ghal', "Halation", "Halation", "", SCineHalationParams());		desc.AddMember(&CCinematicCameraComponent::m_ghosts, 'ggho', "LensGhosts", "Lens Ghosts", "", SCineGhostParams());		desc.AddMember(&CCinematicCameraComponent::m_motionBlur, 'gmbl', "MotionBlur", "Motion Blur", "", SCineMotionBlurParams());		desc.AddMember(&CCinematicCameraComponent::m_whiteBalance, 'gwbl', "WhiteBalance", "White Balance", "", SCineWhiteBalanceParams());		desc.AddMember(&CCinematicCameraComponent::m_streaks, 'gstk', "DiffractionStreaks", "Diffraction Streaks", "", SCineStreaksParams());		desc.AddMember(&CCinematicCameraComponent::m_filter, 'gflt', "FrontFilter", "Front Filter", "", SCineFilterParams());		desc.AddMember(&CCinematicCameraComponent::m_viewfinder, 'gvfd', "Viewfinder", "Viewfinder", "", SCineViewfinderParams());		desc.AddMember(&CCinematicCameraComponent::m_grade, 'ggrd', "Grade", "Grade", "", SCineGradeParams());		desc.AddMember(&CCinematicCameraComponent::m_look, 'glok', "Look", "Look", "", SCineLookParams());		desc.AddMember(&CCinematicCameraComponent::m_output, 'gout', "Output", "Output", "", SCineOutputParams());	}	static void Register(Schematyc::CEnvRegistrationScope& componentScope);	// IEntityComponent	virtual void Initialize() override;	virtual Cry::Entity::EventFlags GetEventMask() const override;	virtual void ProcessEvent(const SEntityEvent& event) override;	virtual void OnShutDown() override;	// ~IEntityComponent	// Schematyc / scripting API	void Activate();	//! Strips this camera's cinematic effects and marks it suspended: the engine always needs	//! SOME active view, so deactivating cannot null the camera manager's active camera. The	//! entity remains a plain view until another camera activates; ResolveActive() treats a	//! suspended component as not-active, and Activate() clears the suspension.	void Deactivate();	bool IsActive() const;	// ICinematicCameraOptics - live lens control, used by CryPhoneTracker	virtual void SetFocalLength(float millimetres) override;	virtual void SetAperture(float fNumber) override;	virtual void SetFocusDistance(float metres) override;	virtual void SetEVCompensation(float ev) override;	virtual void SetISO(float iso) override;	virtual void SetDepthOfFieldEnabled(bool enable) override;	virtual void SetExposureEnabled(bool enable) override;	virtual float GetFocalLength() const override { return m_lens.focalLength; }	virtual float GetAperture() const override { return m_lens.aperture; }	virtual float GetFocusDistance() const override { return m_lens.focusDistance; }	virtual float GetEVCompensation() const override { return m_exposure.evCompensation; }	virtual float GetISO() const override { return (float)(int)m_body.iso; }	// ~ICinematicCameraOptics	// Called by CCinematicCameraPlugin::UpdateBeforeFinalizeCamera() - the engine's	// sanctioned override point: the view camera "is only considered final after	// UpdateBeforeFinalizeCamera has been executed" (ISystem.h). Writing it any earlier	// (as CCameraComponent does from ENTITY_EVENT_UPDATE) loses to the GameSDK ViewSystem,	// which runs in the game framework post update.	void FinalizeGameCamera();	void ApplyExposureGlobalParam();	static CCinematicCameraComponent* GetActiveComponent() { return s_pActive; }	//! The live component whose Camera Slot equals slot (1-9), or nullptr.	static CCinematicCameraComponent* FindBySlot(int slot);	//! Single ownership authority: the camera manager decides which camera is active and	//! s_pActive merely caches its answer. Called once per frame from the plugin hook, before	//! anything else. Hands the render state back when a component is dethroned.	static CCinematicCameraComponent* ResolveActive();	bool IsExposureEnabled() const { return m_exposure.bEnableExposure; }	//! A shape mask is in use and the iris is opened wider than Mask Aperture (the mask is the	//! smaller hole). Diagnostic only - every quantity below is continuous across this point.	bool IsMaskDomain() const;	//! The f-number that sets circle-of-confusion size: the pupil's bounding circle, i.e. the	//! larger of the iris f-number and Mask Aperture while a mask is in use.	float GetGeometricAperture() const;	//! Light reaching the sensor relative to the bare iris: area(iris INTERSECT mask) / area(iris),	//! integrated from the mask texture for the current f-number, times Mask Open Area (tint).	float ComputeMaskTransmission() const;private:	void UpdateCameraComponentParams();	//! Puts every effect we touched back the way we found it (the full Restore sweep,	//! exposure included). Safe to call repeatedly: every Restore early-outs when unsaved.	void RestoreAllEffects();	//! Drops every saved baseline WITHOUT writing anything, for the case where the renderer	//! state was reset behind our back and our captured values are stale.	void ClearSavedBaselines();	//! Ownership gate for every Apply* path: only the resolver-confirmed active component may	//! write the shared render state, and never from a preview window.	bool CanDriveRenderState() const;	//! Removes stray sibling CCameraComponents (accumulated by saves made before the	//! preview camera carried EEntityComponentFlags::NoSave). User-added cameras are kept.	void PurgeDuplicateCameraComponents();	float GetEffectiveFocalLength() const;	float ComputeTargetFocalLength() const;	void  UpdateFocalMotor(float deltaSeconds);	//! log2(T^2 / t): the stops of light the aperture and shutter let through, transmission and	//! bokeh-mask blocking included. One definition, because the exposure and the film-grain	//! light term were computing it separately and were free to drift	//! (research/s1-ccam-exposure-today.md section 5, item 12).	float ComputeApertureShutterStops() const;	float ComputeManualEV100() const;	float ComputeFilmGrain01() const;	float ComputeFocusRange() const;	float GetSensorWidth()  const;	float GetSensorHeight() const;	float ComputeVerticalFOV() const;	//! Aspect the desqueezed anamorphic format has to be masked to, or 0 when no bars are	//! needed (spherical lens, or a format that is not wider than the viewport).	float ComputeLetterboxAspect() const;	float ComputeCoCLimit() const;	void  ComputeDOFBounds(float& outNear, float& outFar) const;	float ComputeMaxBlurAmount() const;	void ApplyDOF();	void RestoreDOF();	void RestoreExposure();	void ApplyFilmGrain();	void RestoreFilmGrain();	void ApplyMotionBlur();	void RestoreMotionBlur();	void ApplySpriteBokeh();	void RestoreSpriteBokeh();	void ApplyLensCharacter();	void RestoreLensCharacter();	//! Scene-referred request on the post-effect parameter bus. Editor-live: it changes the	//! whole picture, so it has to be visible while the properties are being tuned.	void ApplySceneReferred();	void RestoreSceneReferred();	//! Mirror the switch into the 3D engine (E3DPARAM_SCENE_REFERRED) and, on a flip, force a	//! full time-of-day re-evaluation and sky-dome recompute so nothing stays cached or	//! mid-crossfade. See SceneReferredSpec.md S2 / decisions/s2-engine-side-switch.md.	void PublishSceneReferredConvention(bool bSceneReferred);	//! Load (or reload) the two display-chain .cube files into fp16 3D textures and publish their	//! renderer texture IDs on the post-effect bus, so the tone map's output-transform branch can	//! resolve them with CTexture::GetByID. Editor-live: a path change reloads on the next frame,	//! everything else is a member read. See SceneReferredSpec.md D8.	void ApplyDisplayLuts();	void RestoreDisplayLuts();	//! The camera's grade - white balance, ASC CDL, saturation, bypass - published on the	//! post-effect bus every frame. Editor-live, like the switch and the exposure beside it.	void ApplyGrade();	void RestoreGrade();	//! The camera's pre-exposure for this frame, published as a linear scale next to the request	//! above (Global_User_SceneExposure). Editor-live for the same reason.	void ApplySceneExposure();	void RestoreSceneExposure();	//! Publish what the camera IS onto the post-effect bus, for the EXR header (S6 item 3). None of	//! it changes a pixel: it exists so that a frame on disk can say what took it, and above all so	//! that the pre-exposure that was applied travels with the file - without it the EXR's absolute	//! scale is unrecoverable.	void ApplyExportMetadata();	void RestoreExportMetadata();	//! The linear scale the renderer must multiply scene light by so that this camera's EV100	//! renders correctly. 1.0 when the scene-referred path is not requested.	float ComputeSceneReferredExposureScale();	//! EV100 the camera is exposing at under the scene-referred path. Manual is the pure	//! photographic triangle; the two auto modes fall back to it until the AUTO metering loop	//! lands (see decisions/s1-auto-mode.md part 2).	float ComputeSceneReferredEV100();	//! Reduce the renderer's 64-bin metering histogram to the EV that lands the log-mean of the	//! [Metering Band Low, Metering Band High] percentile band on mid grey. False = the band could	//! not be trusted (it resolved inside a saturating bucket, or left the plausibility window) and	//! the camera must hold its last EV.	bool ReduceMeterHistogram(const SSceneReferredMeterHistogram& hist, float& targetEV);	void ApplyStreaks();	void RestoreStreaks();	void ApplyHalation();	void RestoreHalation();	void ApplyWhiteBalance();	void RestoreWhiteBalance();	//! Viewfinder overlays (frame guides + focus peaking). Editor-live like the lens character.	void ApplyViewfinder();	void RestoreViewfinder();	//! Small monitor legend drawn next to the frame guides: aspect of the framing guide and	//! a one line framing readout. Overlay text, exactly like the guides themselves.	void DrawViewfinderText() const;private:	// Reflected properties	// Reflected property groups	SCineBodyParams m_body;	SCineSceneReferredParams m_sceneReferred;	SCineLensParams m_lens;	SCineAnamorphicParams m_anamorphic;	SCineExposureParams m_exposure;	SCineDofParams m_dof;	SCineGrainParams m_grain;	SCineHalationParams m_halation;	SCineGhostParams m_ghosts;	SCineMotionBlurParams m_motionBlur;	SCineWhiteBalanceParams m_whiteBalance;	SCineStreaksParams m_streaks;	SCineFilterParams m_filter;	SCineViewfinderParams m_viewfinder;	SCineGradeParams m_grade;	SCineLookParams m_look;	SCineOutputParams m_output;	// Zoom motor state: eased focal length chasing the quantised target.	float  m_focalMotorMm      = 0.0f;	uint32 m_focalMotorFrameId = 0;	// Sibling camera component (created in Initialize)	Cry::DefaultComponents::CCameraComponent* m_pCameraComponent = nullptr;	//! Set by Deactivate(): "no cinematic effects from this component until Activate()".	bool m_bCinematicSuspended = false;	// DOF saved params	float m_savedDofActive     = 0.f;	float m_savedDofUserActive = 0.f;	float m_savedDofFocusDist  = 3.5f;	float m_savedDofFocusRange = 5.f;	float m_savedDofBlurAmount = 1.f;	float m_savedDofBladeRotation = 0.f;	float m_savedDofBladeCurvature = 0.4f;	float m_savedDofMaskFNumber = 2.f;	float m_savedDofMaskRotation = 0.f;	float m_savedDofHighlightThreshold = 2.f;	float m_savedDofHighlightGain = 2.f;	float m_savedDofApertureBlades = 6.f;	float m_savedDofFNumber    = 2.f;	float m_savedDofAnamorphicSqueeze = 1.f;	float m_savedDofCatEye = 0.f;	float m_savedDofPupilScale = 1.f;	float m_savedDofComa = 0.f;	float m_savedDofAstigmatism = 0.f;	float m_savedDofFieldCurvature = 0.f;	float m_savedDofEdgeSoftness = 0.f;	float m_savedDofAxialChromatic = 0.f;	// Lens model (DofLensModelSpec.md): the switch plus the three quantities the stock focus	// range path never carried.	float m_savedDofLensModel = 0.f;	float m_savedDofLensFocal = 50.f;	float m_savedDofSensorWidth = 36.f;	float m_savedDofSphericalAberration = 0.f;	// Lens ghosts. They ride ApplyDOF because the renderer draws them inside the depth of field	// stage, sharing the splat layers - nothing about them depends on focus.	float m_savedDofGhostAmount = 0.f;	float m_savedDofGhostCount = 3.f;	float m_savedDofGhostSpread = 1.f;	float m_savedDofGhostSize = 1.f;	float m_savedDofGhostThreshold = 8.f;	float m_savedDofGhostHueSpread = 0.3f;	Vec4  m_savedDofGhostTint = Vec4(0.6f, 0.8f, 1.0f, 0.0f);	int   m_savedDofCVar       = 0;	bool  m_bDofParamsSaved    = false;	// Exposure saved param. Manual mode rewrites EV min and max as well as the auto	// compensation, so the whole vector has to be put back. AutoBiased tracks the TOD	// window through the base/last-written pair so its shift never compounds.	Vec3 m_savedEyeAdaptation = Vec3(4.5f, 17.0f, 1.5f);	Vec3 m_baseEyeAdaptationWindow = Vec3(4.5f, 17.0f, 1.5f);	Vec3 m_lastWrittenEyeAdaptation = Vec3(4.5f, 17.0f, 1.5f);	bool m_bExpParamsSaved    = false;	// Film grain saved param (FilterGrain_Amount post effect)	float m_savedGrainAmount  = 0.f;	bool  m_bGrainParamSaved  = false;	// White balance saved state (E3DPARAM_HDR_COLORGRADING_COLOR_BALANCE). The base tracks	// external (TOD) writes so our per-frame multiply never compounds on itself.	Vec3 m_savedColorBalance  = Vec3(1.0f);	Vec3 m_baseColorBalance   = Vec3(1.0f);	Vec3 m_lastWrittenBalance = Vec3(1.0f);	bool m_bWBParamsSaved     = false;	// Viewfinder saved cvars (r_LensGuides / r_LensGuide* / r_LensPeaking* / r_LensOverlays)	int   m_savedGuides         = 0;	float m_savedGuideAspect    = 2.39f;	float m_savedGuideOpacity   = 0.6f;	float m_savedPeaking        = 0.f;	float m_savedPeakingNear    = 0.f;	float m_savedPeakingFar     = 0.f;	int   m_savedPeakingColor   = 1;	int   m_savedOverlays       = 1;	bool  m_bViewfinderSaved    = false;	// Lens character saved cvars (r_HDRVignetteAmount / r_LensDistortion /	// r_LensChromaticAberration / r_LensLetterbox)	float m_savedVignetteAmount = 0.f;	float m_savedLensDistortion = 0.f;	float m_savedLensChromatic  = 0.f;	float m_savedLetterbox      = 0.f;	bool  m_bLensCharSaved      = false;	// Scene-referred saved state (Global_User_SceneReferred post param). Unknown to an	// unmodified renderer, where the read returns nothing and the write is dropped.	float m_savedSceneReferred  = 0.f;	bool  m_bSceneReferredSaved = false;	// Pre-exposure saved state (Global_User_SceneExposure post param). Same story: unknown to an	// unmodified renderer. The neutral value is 1.0, not 0.	float m_savedSceneExposure  = 1.f;	// The sensor clip travels with the pre-exposure (same Apply/Restore, same saved flag): it is	// the other half of "what the sensor did with the light", and it is meaningless without it.	float m_savedSensorClipStops = 0.f;	bool  m_bSceneExposureSaved = false;	// Log-once flags for the parameters whose meaning changes under the scene-referred switch.	// Per component, not function statics: the statics in ApplyExposureGlobalParam() are shared	// by every camera in the level (research/s1-ccam-exposure-today.md section 5, item 13).	bool m_bLoggedSceneRefResponseIgnored = false;	bool m_bLoggedSceneRefAutoAsManual    = false;	bool m_bLoggedMeterBandInverted       = false;	// Scene-referred AUTO state: the smoothed metered EV100 and whether it holds a real sample	// yet. Invalid means "snap on the next measurement" - the camera-cut behaviour the engine's	// own adaptation has at AutoExposure.cpp:165, which a camera must reproduce or every cut	// ramps. Cleared on activation and whenever the component stops driving the render state.	float  m_autoMeteredEV       = 0.f;	bool   m_bAutoMeteredEVValid = false;	// The EV is asked for more than once a frame (the bus publication and the eye-adaptation	// write); the smoothing step must advance exactly once, or the response time depends on how	// many callers there happen to be.	uint32 m_autoMeteredFrameId  = 0;	// What the last histogram reduction resolved to: the absolute luminance at each edge of the	// percentile band and the EV it asked for. Nothing is drawn with these - they are published on	// the post-effect bus for the r_HDRDebug 1 overlay, so that "the exposure is wrong" stays one	// screenshot to diagnose rather than a session of guessing which half of the loop moved.	float  m_meterBandLowLum  = 0.f;	float  m_meterBandHighLum = 0.f;	float  m_meterTargetEV    = 0.f;	// Edge-trigger state for the effective-T-stop log line, per component for the same reason.	float m_lastLoggedApertureN     = -1.f;	float m_lastLoggedTransmission  = -1.f;	// Sprite bokeh saved state (r_DepthOfFieldMode cvar + Dof_User_BokehShapeTex post param).	// Unknown to an unmodified renderer, where the read returns nothing and the writes are dropped.	int    m_savedDofModeCVar  = 1;	// Whether we have pushed a shape texture name that still needs unbinding on restore.	bool   m_bBokehShapeTexPushed = false;	bool   m_bSpriteBokehSaved = false;	// Motion blur saved cvars (r_MotionBlurShutterSpeed / r_MotionBlur)	float m_savedMBShutter    = 250.f;	int   m_savedMBMode       = 0;	bool  m_bMBParamsSaved    = false;	// Diffraction streak saved params (HDR_Streaks_*). Unknown to an unmodified renderer,	// where the reads return 0 and the writes are silently dropped.	float m_savedStreaksActive    = 0.f;	float m_savedStreaksAmount    = 0.f;	float m_savedStreaksCount     = 8.f;	float m_savedStreaksAngle     = 0.f;	float m_savedStreaksLength    = 0.5f;	// Wave-optics diffraction glare (DiffractionKernelSpec.md), pushed alongside the analytic	// params: the mode switch decides which of the two the renderer actually draws, and the	// analytic ones stay live so r_HDRDiffractionKernel 0 can fall back without a frame of black.	float m_savedStreaksMode      = 1.f;	float m_savedStreaksWaveAmount = 1.f;	float m_savedStreaksWaveReach = 0.35f;	float m_savedStreaksDispersion = 1.f;	// Anamorphic horizontal flare, pushed alongside the blade streaks but gated separately.	float m_savedAnamorphicAmount    = 0.f;	float m_savedAnamorphicLength    = 0.7f;	float m_savedAnamorphicThreshold = 3.f;	Vec4  m_savedAnamorphicTint      = Vec4(0.35f, 0.62f, 1.0f, 0.0f);	// Front filter PSF (HDR_Filter_*), pushed alongside the streaks because a plate and an	// iris are both "what the front of the lens does to a point" and share one apply path.	float m_savedFilterAmount    = 0.f;	float m_savedFilterSize      = 0.25f;	float m_savedFilterRotation  = 0.f;	float m_savedFilterThreshold = 6.f;	float m_savedFilterPoints    = 4.f;	//! Whether we have pushed a PSF texture name that still needs unbinding on restore.	bool  m_bFilterPSFTexPushed  = false;	bool  m_bStreakParamsSaved    = false;	// Film halation saved params (HDR_Halation_*). Unknown to an unmodified renderer, where	// the reads return 0 and the writes are silently dropped.	float m_savedHalationAmount    = 0.f;	float m_savedHalationThreshold = 2.f;	float m_savedHalationRadius    = 0.5f;	Vec4  m_savedHalationTint      = Vec4(1.0f, 0.35f, 0.15f, 0.0f);	bool  m_bHalationSaved         = false;	// Derived cache of the camera manager's answer; written ONLY by ResolveActive().	static CCinematicCameraComponent* s_pActive;	//! Every live cinematic camera component. Main-thread only (entity lifecycle), no locking.	static std::vector<CCinematicCameraComponent*> s_all;};