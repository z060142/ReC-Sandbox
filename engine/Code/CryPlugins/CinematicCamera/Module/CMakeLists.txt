start_sources()sources_platform(ALL)add_sources("CinematicCamera_uber_0.cpp"	SOURCE_GROUP "Root"		"resource.h"		"StdAfx.cpp"		"StdAfx.h"		"CinematicCameraPlugin.h"		"CinematicCameraPlugin.cpp"	SOURCE_GROUP "Components"		"CinematicCameraComponent.h"		"CinematicCameraComponent.cpp"	SOURCE_GROUP "Tools"		"HDRIImport.h"		"HDRIImport.cpp")end_sources()CryEngineModule(CinematicCamera PCH "StdAfx.cpp" SOLUTION_FOLDER "CryPlugins")target_include_directories(${THIS_PROJECT}	PRIVATE		"${CRYENGINE_DIR}/Code/CryEngine/CryCommon"		"${CRYENGINE_DIR}/Code/CryPlugins/CryDefaultEntities/Module")# rec_ImportHDRI reads OpenEXR through the same static tinyexr the renderer's EXR writer uses, so# there is one copy of the library and one set of codec settings in the build. zlib comes with it# as a PUBLIC dependency of that target.target_link_libraries(${THIS_PROJECT} PRIVATE tinyexr)