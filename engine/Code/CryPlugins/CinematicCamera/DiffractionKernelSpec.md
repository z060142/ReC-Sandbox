# Wave-Optics Diffraction Kernel (FFT glare) ??SpecificationStatus: implemented, 2026-08-27 (pending user verification); revised the same day - half resolution,own layer, source pre-blur, no f-number gate in Wave mode. Owner of semantics: the camera manager.Branch: `glare` (off `dev` @ bc6e297c). Merged back to `dev` only after user verification.Predecessors: `DiffractionStreaksSpec.md` (the analytic streaks, which stay as a mode),`FilterPSFSpec.md` (the sparse PSF convolution this reuses), `DofLensModelSpec.md` 禮3.2(`LensPupil`, the pupil this diffracts).## 0. What this adds, and why nowThe analytic streaks paint N line pairs with an exponential falloff, a hue ramp and an f-numberfade-in that was tuned by eye. They cannot show what a *real* aperture does: a 9-blade iristhrows 18 rays with a specific intensity pattern, a star-shaped mask throws a pattern of itsown, a rounded polygon (curved blades) softens and broadens the rays, and a slightly aberratedpupil puts halos around them. All of that is one statement: **the glare of a point source isthe Fraunhofer diffraction pattern of the pupil, |F{P}|簡.** With `LensPupil` / the iris polygon/ the shape mask now defined in one place, the pupil is available; this round computes itsdiffraction pattern and convolves the frame's highlights with it, using the sparse spriteconvolution the front filter already has.Physics reminders that shape the design:* The pattern's scale on the sensor is set by `弇 繚 N`: stopping down spreads the same energy  fraction over a proportionally larger pattern. Rays get longer *and dimmer per pixel* as N  grows; wide open they are short and hide under the source's own bloom. This IS the "sunstars  appear when you stop down" behaviour ??no hand-authored f-number ramp is needed or wanted.* Straight edges diffract into rays whose intensity falls as `1/u簡` along the edge normal  (edge diffraction); corners and smooth outlines fall faster (`1/u?循). The rays of a real  sunstar are visible for hundreds of pixels only because the source is 10糧??0?腕?brighter than  the scene; the pattern itself is fixed.* Each wavelength is a scaled copy: `PSF_弇(u) = (弇?/弇)簡 繚 PSF_弇?(u 繚 弇?/弇)`. Blue is compact,  red is stretched ??the rainbow along a ray is dispersion, not a colour ramp.* A DFT of the pupil sampled on an `L?L` grid with the pupil `d` texels across gives a PSF  whose texel pitch on the sensor is `弇 N 繚 d / L` ??for `L = 512, d = 256, 弇 = 550 nm, N = 11`  that is 3 繕m ??0.16 px at 1080p full frame, so the whole texture spans only ~40 px of  screen. **The FFT resolves the structure; the reach has to come from the analytic far-field  law.** That is the central design decision below (禮2.3).## 1. Modes and gating`Diffraction Streaks` group gains `Mode` ('stMd', enum): `Analytic` (today's line pairs,byte-identical behaviour) / `Wave` (this spec). **Default `Wave`.** Post param`HDR_Streaks_Mode` (0/1). Everything under the `Wave` branch is new code; the analytic passesare not touched. `r_HDRStreaks` stays the master switch (0 = neither mode; 2 = the analyticdiagnostic force, unchanged, does not force Wave). New cvar `r_HDRDiffractionKernel` (int,default 1; 0 = kernel never built, Wave mode falls back to Analytic with one log line ??killswitch) and `r_HDRDiffractionDebug` (int, default 0; 1 = draw the kernel texture in the top-leftcorner at 4? with a log-scale tone curve, and log the rebuild parameters).## 2. Model### 2.1 The pupil rasterOn the CPU, a job builds the pupil amplitude `A(x)` on an `L ? L` complex grid, `L = 512`,pupil bounding diameter `d = L/2 = 256` texels (zero-padded ?2 so `|F|簡` ??the transform of thepupil's autocorrelation, which is twice as wide ??does not wrap). The pupil is the ON-AXIS pupil(no cat-eye: field-dependent clipping varies across the frame and a convolution kernel must beshift-invariant; the corners' clipped stars are a later refinement):```r, 庛 from the grid centre, unit radius = d/2polygon:  inside if r ??NGon_Rad(庛 ??phase, blades)^exponent          (same closed form and the SAME exponent the gather/splat use: 1 ??curvature 繚 p 繚 strength)mask:     ? mask(rotate(x, ?askRotation) 繚 maskScale)                 (R channel of the shape DDS, read through the DOF stage's existing CryPak DDS reader; N_mask / N scaling exactly as the splat's vDofBokehMask.y; 1 when no mask)barrel:   ? (r ??cos(?/n)^exponent / p)                                (the mechanical opening; no field offset on axis. p = N_open/N_geo = fPupilScale, 1 wide open)A(x)      = sqrt(polygon 繚 mask)  ?  exp(i 繚 2? 繚 W(x))                 (amplitude = sqrt of the intensity transmittance; W = wavefront error in waves)W(x)      = k_sa 繚 W_sa 繚 r??                                           (spherical aberration as a PHASE error; W_sa = 0.25 waves at |k_sa| = 1, cvar r_HDRDiffractionSAWaves, default 0.25. This is the physically right place for SA in the glare: an aberrated pupil puts rings and halos around the rays. Optional, 0 disables.)```**The barrel is what makes the rays appear when you stop down.** It is the fixed round openingthe blades retract into, so it does not move with the iris: `p = N_open / N_geo` is the polygon'ssize divided by the barrel's, and the barrel's radius in the polygon's own units is therefore`(polygon size) / p`. `NGon_Rad` is normalised to the CIRCUMRADIUS, so "polygon size" ??theinscribed radius, where the blade edges actually are ??is `cos(?/n)^exponent`. Consequences:* `p = 1` (wide open): the barrel disc sits exactly on the blade edges and lies inside the  polygon everywhere else, so the intersection **is** the circle, tangent to the edges at their  midpoints. A circular pupil diffracts into rings. **No rays wide open** ??which is what a real  lens does, and the second half of "sunstars appear when you stop down" (the first half being  the `弇繚N` scale on the sensor). Curved blades (exponent < 1) are clipped by exactly the same  relative amount; at exponent 0 the polygon is already a circle and nothing is clipped.* `p ??cos(?/n)^exponent` (??0.92 for eight straight blades, 0.5 for three): the barrel is past  the corners and clips nothing at all. This is where a normal working aperture lives, and the  pupil there is the bare polygon ??unchanged from before this rule existed.* In between, the corners are cut back and the rays fade in continuously.The on-screen scale (`弇繚N繚d/L`, 禮2.3) is untouched by any of this: the polygon's size in theraster stays the unit and only the barrel moves.Edges are anti-aliased: evaluate 2?2 supersamples per texel (the polygon and barrel tests) andaverage; the mask is bilinearly sampled. The barrel applies to the mask as well ??the pupil is`mask ??polygon ??barrel`, and the mask's scale relative to the polygon is unchanged. A hard-edged pupil gives the sharp `1/u簡` rays; thesplat's soft edge (`fEdgeSoft`) is a rendering convenience and is **not** applied here.### 2.2 The transform2-D FFT of `A`, `I(u) = |F{A}|簡`, DC at the centre (fftshift). Normalise so that `峉 I = 1`over the grid (Parseval: this is the total transmitted light). Then **remove the core**: theframe already contains the source itself (and its bloom), so the kernel must carry only thediffracted light. Zero every texel within `r_core = 2.5` Airy radii of the centre (`1.22 繚 L/d`texels per Airy radius ??~6 texels) and record the removed fraction `E_core` (log it; for acircular pupil it is ??0.9). What remains ??`I_glare`, integral `1 ??E_core` ??is the kernel.Store `I_glare` as a **prefiltered mip chain of the LINEAR intensity** in an `R16F` `L?L`texture with `log??L + 1` levels (`$DiffractionKernel`). The chain exists because the kernel'stexel pitch on screen is a small *fraction* of a pixel (禮2.3): one deposit pixel covers~17 kernel texels at f/16 and ~100 wide open, while the rays inside the kernel are 1?? texelswide, so a single tap per pixel lands on a ray about one time in seventeen and the patterndisappears from the picture even though its *average* energy is right. Level `m` is built bybox-filtering 2?2 of level `m??` **in linear intensity**, so `I` at every level is the meanintensity over that level's texel footprint.Encode `sqrt(I)` per level **after** the filtering, never before: `sqrt` is not linear, so abox filter of `sqrt(I)` is not `sqrt(mean I)` and the shader's `k簡` would come outsystematically dark. The square root keeps the 10?領 tails above half-float denormals; thepixel shader squares it back, and because of the ordering `k簡` *is* the mean intensity over thesampled level's texel. Upload every level (the texture is created with the mip count and**without** `FT_NOMIPS`; both `GetOrCreate2DTexture`'s creation path and `UpdateTextureRegion`walk a source buffer whose levels are consecutive, which is how the job lays them out). Track theextrapolation boundary ring: `I_ring(庛)` = the mean of `I_glare` on the ring `r ??[R_tex ??4, R_tex]`(`R_tex = L/2 ??2` texels), 256 angular bins, stored in a second small texture`$DiffractionRing` (`256?1`, R16F, sqrt-encoded as well, **not** mipped ??256 bins over a~1600-texel circumference is already 6+ texels per bin, the same order as the angular footprintof a pixel covering 17 texels). The ring is what the far-field extrapolation samples.The removed core is left as it is: `r_core ??6` texels is 1.4 native pixels at f/16, so acoarse level averages the hole with its surroundings ??which is correct, because the hole sitsat the source pixel and the frame carries the source there anyway.Implementation of the FFT: the engine already carries a radix-2 complex 1-D/2-D FFT in`Code/CryEngine/RenderDll/Common/WaterUtils.cpp` (`CWaterSim::ComputeFFT1D/2D`, "2 DimensionalFFT" by Paul Bourke). Lift that routine into a small standalone helper (`DiffractionKernel.cpp/.h`in the DOF stage's folder ??do not touch the water code) so the job has no dependency on thewater simulation. `512簡` on one core is on the order of 10??0 ms: **never on the renderthread.** The build runs as a `DECLARE_JOB` on the job system, double-buffered: the render threadkeeps using the last finished kernel; when a job completes its result is uploaded to thetexture on the render thread (`CTexture::UpdateTextureRegion` or recreate). At most one job inflight; if parameters change again while one runs, remember the newest request and start itwhen the current one finishes.Rebuild triggers (quantised so a smooth pull does not rebuild every frame; log each rebuildonce): blades; exponent (steps of 0.02); phase (1簞); mask texture name; mask rotation (1簞);`N_mask/N` scale (steps of 0.05); `k_sa` (0.05); mode/cvar switches; **the pupil scale `p`**(steps of 0.02, but ONLY above `cos(?/n)^exponent ??0.02` ??below that the barrel cannot reachthe polygon and every value gives the same pupil, so all of it collapses onto that one boundarybucket and an ordinary aperture sweep does not rebuild the kernel at all). Note that N itself doesNOT trigger a rebuild ??the kernel is computed in the pupil's own units, and N enters onlyas the on-screen scale (禮2.3). The kernel therefore changes only when the pupil's *shape*changes.### 2.3 On-screen scale and the far-field extrapolationKernel texel pitch on the sensor: `s = 弇? 繚 N_geo 繚 d / L` (metres; `弇? = 550 nm`, `N_geo` =the same geometric f-number the DOF uses, `max(N, N_mask)` when a mask is bound). In nativepixels: `s_px = s 繚 W_px / W_sensor`. The texture covers `R_tex` texels, i.e. `R_tex 繚 s_px`pixels ??tens of pixels. The sprite is much larger (禮3); outside the texture the kernel isextrapolated by the edge-diffraction law:```u   = distance from the sprite centre, in kernel texels (after the 弇 scaling below)I(u, 庛) = I_tex(u, 庛)                              u ??R_tex        = I_ring(庛) 繚 (R_tex / u)簡                 u >  R_tex```The `1/u簡` law is exact for a straight edge and pessimistic for everything else, so theextrapolated tail is never brighter than the physics; a smooth (round) pupil, whose ring isalready tiny, extrapolates to nothing. The energy in the extrapolated tail up to the spriteradius is `峉_庛 I_ring(庛) 繚 R_tex簡 繚 ln(R_sprite / R_tex)` ??finite. It is **not** normalisedaway: the kernel already carries physical units (禮2.4), so the tail is simply extra glare thesprite is or is not wide enough to hold. The CPU reports the coefficient so the split can belogged.Wavelength: the pixel shader samples at `n_弇 = 6` wavelengths (420, 470, 520, 570, 620, 670 nm),each a scaled copy `u_弇 = u 繚 弇? / 弇`, weight `(弇?/弇)簡`, colour from a small RGB table(approximate CIE ??sRGB, normalised so the six sum to white):| 弇 nm | R | G | B ||---|---|---|---|| 420 | 0.05 | 0.00 | 0.35 || 470 | 0.00 | 0.10 | 0.50 || 520 | 0.00 | 0.55 | 0.15 || 570 | 0.35 | 0.35 | 0.00 || 620 | 0.45 | 0.00 | 0.00 || 670 | 0.15 | 0.00 | 0.00 |(Columns are then rescaled on the CPU so `峉_弇 colour_弇 繚 (弇?/弇)簡 = (1,1,1)` ??white light stayswhite in the sum.) `Dispersion` ('stDs', 0..1, default 1) lerps every `弇` toward `弇?`: 0 = amonochrome white pattern at 550 nm.### 2.4 Energy and the source excessThe front filter's sparse convolution (`FilterPSFSpec.md` 禮2??) run one resolution finer: the**half-res** HDR downsample (`m_pTexHDRTargetScaled[0][0]`, `$HDRTarget 1/2a`) thresholded withthe streaks' own threshold (`HDR_Streaks_Threshold`, soft knee `T/4`) into a half-res scratch RT,**pre-blurred** (禮3), one sprite per surviving texel, deposit `E 繚 amount 繚 I(u)` additively intothe glare's **own half-res layer** `$DiffractionLayer`, which is composited into the HDR targetafter the bloom stage. **No `1 / fBloomCompositeWeight`**: that factor existed only to undo thetone mapper's scaling of `$HDRFinalBloom`, and the layer does not travel that way. The on-screenenergy is unchanged - `deposit 繚 (1/w) 繚 w` became `deposit`.`I` carries **physical units and is never renormalised**. The full pattern is scaled so`峉 I = 1` (Parseval ??the pupil's whole transmitted light) *before* the core is removed, sowhat the texture holds integrates to `1 ??E_core`, and the extrapolated tail adds`T_tail = 峉_庛 I_ring(庛) 繚 R_tex簡 繚 ln(R_sprite / R_tex)`. **`amount = 1` therefore deposits`E 繚 (1 ??E_core + T_tail)`, the physical fraction** ??about 0.07 of the source for an 8-bladeiris, typically 5??5 % across pupils. Renormalising the core-removed kernel back to unitintegral would be wrong twice over: it would hand the pattern the source's *entire* excess(~14? too much), and it would make the deposit depend on `R_sprite`, which is a cost choice.The only unit conversion the renderer applies is `1 / s_h簡`, turning "per kernel texel" into"per half-resolution pixel"; moving from quarter to half resolution is energy-neutral byconstruction (the pitch doubles, so the per-pixel deposit is quartered, and the sprite coversfour times as many pixels). No f-number ramp on the component side in Wave mode - and, since thefix of 2026-08-27, no f-number **gate** either: `HDR_Streaks_Active` in Wave mode is`Enable Streaks && Streak Amount > 0.001`, with the analytic`pow(clamp((N??.3)/(11??.3)), 1.2)` ramp confined to Analytic mode where it belongs. It used todrive the flag in both modes, which switched the whole streak block off below f/6.3 and on hardat f/6.4 - a pop with no physics behind it. `HDR_Streaks_Threshold` keeps its meaning anddefault.**Visibility, and what `Streak Amount` really is.** That physical fraction is right and almostinvisible, and the two facts are not in conflict. Of the `1 ??E_core` that leaves the source,nearly all sits in the first few Airy rings, under the source's own bloom; only about **0.5 %**of the source's light lands in the RAYS proper ??past ~20 Airy radii, where the eye reads asunstar. Spread over 8 rays several hundred pixels long, that is on the order of **1e-5 of thesource per pixel** twenty pixels out. A real sunstar is visible only because the sun, or a nightstreetlight against a dark street, is **1e4??e6? the rest of the scene**; a game light's HDRvalue is clamped to a few tens, so the same physics deposits nothing the display can show. Thekernel build logs `E_far` (energy past 20 Airy radii) so this can be read per pupil.> **Corrected 2026-08-29 (scene-referred round 3).** The sentence above ??"a game light's HDR> value is clamped to a few tens" ??is the reason `Streak Amount`'s default was 20 and then 50,> and on the scene-referred path it is **no longer true**. A 200 kcd/m簡 practical at a night> exposure arrives in the HDR target at 41 667, seventeen and a half stops over mid grey, and its> rays at 0.5 % of the source land about a stop OVER mid grey. The physics deposits plenty.>> Two things had been eating it, both fixed in round 3 and both documented in> `scene-notes/decisions/s8-glare-source.md`:>> 1. The half-resolution source `$HDRTarget 1/2a` is produced with firefly suppression on>    (`r_HDRBloomQuality` 2), and firefly suppression exists to pull bright OUTLIERS down towards>    their block's mean ??which is exactly what a glare source is. On a lamp a few pixels across>    that cost a factor of 30 000, and took the source below the threshold entirely, so no>    `Streak Amount` could recover it. Under the switch the wave glare's threshold pass now reads>    the FULL-RESOLUTION `$HDRTarget` (the bilinear read at the half-res destination is an exact>    2?2 box); `$HDRTarget 1/2a` still sets the sprite grid. The rest of the glare family keeps>    the half-res copy ??see the decision note for why that asymmetry is deliberate.> 2. That filter's compression knee sat at a literal 1.0, display white. It moves to the camera's>    white, 14 stops over mid grey (`decisions/s8-fp16-writers.md`).>> `Streak Amount` keeps its round-2 meaning ??gain in stops, ten units per stop, 50 = physical> (`decisions/s7-glare-gain.md`) ??and 50 is now visible on a night practical, which is what it> was always supposed to be. A DAYLIGHT-exposed 200 kcd/m簡 lamp is still faint at 50 and still> wants 130??50; that is physics, not a defect.`Streak Amount` is therefore defined as a **source radiance boost**: how much brighter the realsource is than the clamped HDR value standing in for it. `1` means "the HDR value IS the trueradiance" ??physically exact, nearly invisible. `10??00` reproduces the star an actual lamp orsun of that kind would draw. Nothing about the pattern's **shape or falloff** changes with it ??those stay physical at every value; only the source's assumed brightness does. Range `0..200`(slider `0..100`), **default 20**. Analytic mode ignores it.**Superseded on the scene-referred path (2026-08-28/29).** There is no clamped HDR value left tocompensate for, so a linear "boost" over a five-decade quantity is the wrong control.`Streak Amount` is read as **gain in stops, ten units per stop, 50 = physical 1.0**(`decisions/s7-glare-gain.md`): 0 = off, 100 = +5 stops, 150 = +10. The shipped default stays 50,which is now both physical and visible on a night practical ??see the corrected note above. Offthe switch the stored value keeps its original linear meaning exactly.Anamorphic squeeze: the iris sits *behind* the cylindrical front group, so its diffractionpattern is formed unsqueezed on the sensor and the desqueeze stretches it horizontally by `S`:sprite x half-extent = `R 繚 S` (the opposite of the front filter, which sits in front of thecylinder). Check what the analytic streaks do with the squeeze today and note the differencein the code comment; do not change the analytic path.## 3. RendererDOF stage (`DepthOfField.cpp/.h`) owns the kernel (it already owns the pupil description andthe CPU mask reader): `CDiffractionKernel m_diffraction` with `Request(desc)` (quantise,compare, spawn job), `Update()` (called once per frame from `Execute`; uploads a finished job),`GetKernelTexture()`, `GetRingTexture()`, `GetNormalisation()`, `GetTexelPitchPupilUnits()`.The pupil description struct: blades, exponent, phase, mask texture name (or empty), maskrotation, mask scale, `k_sa`, `L`, `d`.Bloom stage (`Bloom.cpp`): `ExecuteDiffraction()` in the Wave branch of `ExecuteStreaks()`(same place in the frame: after bloom's downsamples, before the filter PSF and halation).**Half resolution, own layer, composited after bloom.** The pass owns three lazily allocatedhalf-res `RGBA16F` targets (acquired / released / resized exactly like the halation targets, soAnalytic mode and glare off cost nothing): `$DiffractionSource`, `$DiffractionSourceTemp` and`$DiffractionLayer`. The reason is that neither artefact of the quarter-res version was awindowing problem, and the windows of the previous round did not cure them:* **Source grid.** A lamp clears the threshold over a ~3?3 block of quarter-res texels, each of  which spawns its own sprite; the sum of nine offset copies of a radially symmetric pattern has  the *block's square* as its centre, whatever the pupil was.* **Deposit grid.** A quarter-res deposit is bilinearly magnified 4? onto a `1/u簡` slope that  crosses a whole texel in almost nothing - 4?4 native blocks.Half resolution halves both in each axis. On top of it the thresholded source gets a **separable5-tap Gaussian pre-blur**, ? ??1 half-res texel, weights normalised so the total excess ispreserved (technique `HDRDiffractionSourceBlur`, two tiny passes). That is not smoothing: it isthe convolution of the pattern with the source's own extent, which is what an extended sourcereally does - and it is what turns the surviving block into a round blob.The chain is: threshold (`HDRStreakThreshold`, half-res HDR in, half-res scratch out, 1:1 nowrather than the old 2:1 minification ??**on the scene-referred path the input is the FULL-res`$HDRTarget` instead, so the pass is a 2:1 box again**, see the corrected note in 禮2.4) ??blur H ??blur V ??clear the layer ??sprites into thelayer ??`ExecuteDiffractionComposite()`. The composite is a sibling of the DOF's ghost composite,called from `CStandardGraphicsPipeline::ExecuteHDRPostProcessing` immediately before`CDepthOfFieldStage::ExecuteGhostComposite()`: an additive fullscreen blit(`GS_BLSRC_ONE | GS_BLDST_ONE`, `GS_NOCOLMASK_A`), bilinear from half res, technique`HDRDiffractionComposite`, consume-once through `m_bDiffractionLayerValid`. As with the ghosts,the layer lands **after** every glare threshold in the frame, so halation, the streaks and thefront filter never see it - a diffraction pattern cannot itself halate. That is a deliberateasymmetry with the rest of the glare family and is correct.The sprite primitive is built like `m_passFilterPSF` (own per-view CB, now for the **half-res**grid, `SetCustomVertexStream(~0u,??`, `width_h 繚 height_h 繚 6` vertices, additive, alpha masked;the early quad collapse on a zero source stays, and matters more than before - four times as manycandidate cells). New technique `HDRDiffractionPSF` in `HDRPostProcess.cfx`: VS = the filterPSF's VS with the x half-extent ? `S`, PS:```d        = quad-local in [-1, 1] 繚 R_sprite_px                       (half-res px from the sprite centre; x already carries the squeeze)for each 弇: fScale = lerp(1, 弇?/弇, dispersion)            u_弇 = |d| / s_h 繚 fScale;  庛 = atan2(d.y, d.x)            f_弇 = fScale / s_h                                      (kernel texels covered by ONE pixel at this 弇)            lod  = clamp(log??max(f_弇, 1)), 0, mips ??1)            I_弇 = (u_弇 ??R_tex) ? kernelLod(u_弇, 庛, lod)簡 : ring(庛)簡 繚 (R_tex / u_弇)簡            c  += colour_弇 繚 (弇?/弇)簡 繚 I_弇out = Color.rgb 繚 c 繚 norm 繚 w_in 繚 w_out                             (norm = 1 / s_h簡 only ??texel to pixel; the kernel is NOT renormalised; the windows below)```**The LOD is what makes the deposit an integral rather than a lottery.** `u = r 繚 fScale / s_h`,so one pixel of `r` is `f = fScale / s_h` texels of `u`; reading level `log??f` returns the meanintensity over the pixel's own footprint in one tap, and the surviving `1 / s_h簡` (the texelcount in that footprint) turns that mean into the footprint's integral. The energy bookkeepingof 禮2.4 is unchanged ??what changes is that the *sampling* now integrates instead of point-testing 1 texel in 17. Never go below level 0 (`max(f, 1)`): the pattern can be magnified, inwhich case level 0 is already finer than a pixel.**The two screen-space windows.** The deposit is multiplied by `w_in 繚 w_out`, both functionsof `r` (half-res pixels from the sprite centre) alone. Neither is decoration: each removes anartefact that is a property of the RASTERIZATION, not of the physics.* `w_out = 1 ??smoothstep(0.8, 1.0, r / R_sprite)`. The sprite is a square quad and the `1/u簡`  extrapolation is still finite at its boundary, so without a window the quad's edge is a hard  cut and every small emitter wears a **white square frame**. Feathering the last 20 % of the  radius and zeroing everything beyond it (the quad's corners, `r` up to `?? R`) makes the  cut-off round and invisible. Energy: it takes rather more than half of the last fifth of the  logarithmic tail, so the CPU's diagnostic tail estimate integrates to `0.9 R` ??  `T_tail = c_tail 繚 ln(0.9 R_sprite / R_tex)`.* `w_in = smoothstep(0, 4, r)`, i.e. a fade-in over the first 4 **half-res** pixels ??the same  **8 native** the quarter-res version faded over 2 of its own, so the figure on screen is  unchanged. Just outside the removed core the pattern falls as `1/u簡` from about six kernel  texels ??1.4 native pixels ??a slope that crosses a whole deposit texel in almost nothing,  which showed as **4?4 native blocks** around the source at quarter resolution, 2?2 here.  (The analytic streaks never exposed this: their exponential falloff is orders of magnitude  gentler.) That radius already belongs to the source itself and to the bloom around it, so  fading the glare in over it costs nothing visible. It is **not** free in energy: it is the  steepest part of the pattern. The kernel's own core removal (2.5 Airy radii) is unchanged, so  the accounting of 禮2.4 stands and this is an extra deficit at the very centre; the pass logs  the window's radius in kernel texels.  > **Corrected 2026-08-30 (scene-referred round 4).** "A small extra deficit" was wrong, and the  > constant is now named `kDiffractionInnerWindowHalfPx` in `Bloom.cpp` so that the CPU can cost  > it. The kernel texel pitch is `弇繚N繚d/L`, about **0.047 half-res pixels** at f/8 on a 1080p  > frame, so the whole kernel **texture** spans only some **twelve half-res pixels** ??and the  > fade covers the first four of them. The pattern's energy grows as `ln(u)` outside the core, so  > the fade sits exactly where most of the texture's share lives: it keeps roughly **a third** of  > it, one and a half stops. The `r_HDRDiffractionDebug 3` report now prints that fraction, and  > the "deposited glare fraction" logged next to it should be read as the figure **before** the  > fade ??it has always over-reported the deposit by about 3?.  >  > It does **not** dim the rays. Everything past `r = 4` half-res pixels is untouched, and that is  > where the 1/u簡 tail ??the visible spikes ??lives. Narrowing the fade is a real option and is  > written up as approach B in `scene-notes/decisions/s8-glare-source.md` round 4; it was not  > taken, because it does not answer the evidence and re-opens the blocks artefact.That prediction was tested and came true: the blocks survived the inner window, so the step namedhere ??"run the sprite pass into a half-resolution target of its own and composite that down" ??has been taken, together with the source pre-blur the prediction missed (it read the deposit gridas the whole fault; the source grid was the other half).> **Round 4 note on reading the far field.** `c_tail` is built from the boundary ring's angular> **MEAN**. That is the correct number for an energy budget and the wrong one for "how bright is> the spike": a bladed iris runs 30??00? between a ray and the gap beside it, so an estimate made> off `c_tail` is an estimate of the DARK part of the pattern. `CDiffractionKernel::GetRingPeak()`> carries the peak bin, the kernel's log line prints the ray/gap contrast, and the> `r_HDRDiffractionDebug 3` report prints the deposit both ways. Past `R_tex` the deposit is> `excess 繚 gain 繚 I_ring(庛) 繚 R_tex簡 / r簡` with `r` in half-res pixels ??the texel pitch cancels> exactly, so aperture, resolution and sensor size are all already inside `r`.`kernelLod(u, 庛, lod)` samples `$DiffractionKernel` with `SampleLevel` at`0.5 + u/L 繚 (cos 庛, sin 庛)`; `ring(庛)` samples `$DiffractionRing` at `庛 / 2?` (bilinear, wrap).The kernel's sampler must be **`TrilinearClamp`** ??CRYENGINE builds `FILTER_LINEAR` samplerswith `MaxLOD = 0`, which would silently pin every `SampleLevel` back to level 0 and undo thewhole chain. Constants: `DiffractionParams0 = (R_sprite_px, s_h, R_tex, norm)`,`DiffractionParams1 = (dispersion, L, maxLod = mips ??1, r_HDRDiffractionDebug)`, and the six`colour_弇 繚 (弇?/弇)簡` as a `float4[6]` array (rgb + `弇?/弇`). The debug view(`r_HDRDiffractionDebug`) deliberately stays on level 0: it is there to show the kernel's truestructure, not what a pixel averages out of it.`r_HDRDiffractionDebug 2` (shape only) replaces the deposit with a **decade map** of thespectral sum, `v = saturate((log10 I + 13) / 10)`. It cannot reuse the corner view's`log(1 + I繚10??`: that gain is calibrated for the kernel read at level 0, where a ray texel isaround `1e-4`, whereas what reaches the sprite is the pixel's footprint MEAN (17 texels at f/16,100 wide open) plus the `1/u簡` tail ??`1e-4` down to `1e-12`, which the old curve mapped to0.0007, i.e. black. Mode 2 still applies both windows: they are geometry, and the mode exists toshow the figure the pass actually rasterizes. `r_HDRDiffractionDebug 3` (the thresholded sourcesin the corner) reads the **half-res thresholdedand pre-blurred** source ??what the sprite grid actually walks ??and takes a **5?5 max over thesource's own texel grid**: the panel is a half-size copy of it, so it still minifies 2:1 and a one- or two-texel source is point-sampled away three times out of four ??the panel came up black with a lamp blazing in the frame.The max also dilates, so one hot texel paints a 3?3 block of panel pixels, and the panel carriesa dim floor and a border so that "nothing cleared the threshold" and "the pass never ran" are notthe same black rectangle.Sprite radius `R_sprite = Reach 繚 H_h` (half-res pixels; `Reach` ('stRc', 0..1, default 0.35,fraction of frame height, so the on-screen reach is unchanged) ??the cut-off of the extrapolatedtail, purely a cost/coverage choice. At half resolution it is also **the** cost dial: the spriteis 2? wider in pixels and there are 4? as many candidate cells, so if a scene gets slow, `StreakReach` is what to pull, and the log prints the sprite radius in half-res **and** native px. Costtarget: < 0.3 ms GPU at 1080p with a few sources (each sprite pixel = 6 texture reads + 6 ringreads).Log once per change: `[BLOOM] diffraction kernel blades=%d exp=%.2f phase=%.0f mask=%smaskRot=%.0f maskScale=%.2f sa=%.2f -> core=%.2f tail=%.3f physical glare fraction = %.3fL=%d (job %.1f ms)` ??the fraction is `1 ??E_core`, so how much of the light really becomesglare is readable at a glance, plus one line for the mip chain (levels and size). The glarepass logs the fraction it actually deposits (tail included, `amount` applied) together withthe texels a pixel covers and the LOD that follows from it at `弇?`, so a "no rays" report canbe read straight off the log.### 3.1 The source budget ??what this pass COSTS, and the ceiling on itThe sprite pass emits one quad per half-resolution texel and the vertex shader collapses the oneswhose source is empty, so its cost is **exactly the number of texels above `HDR_Streaks_Threshold`times the sprite's area**. Under the scene-referred pre-exposure that threshold is an absolutescene value, so a bright sky, a large emissive or simply a brighter exposure can move tens ofthousands of texels over it at once ??each drawing a sprite some 160 px across, six spectral tapsdeep. At 1080p, `Reach` 0.15:| on screen | texels over threshold | half-res px blended ||---|---|---|| one practical at night | ~9 | 0.24 M || a street of twenty lamps | ~200 | 5 M || a pre-exposed bright sky | 50 000+ | 1 300 M |That is why the frame rate followed the brightness. It is a population problem, not a shader one:the physics of a broad bright region is a *veil*, and drawing it as fifty thousand overlappingcopies of the same star buys a picture a single blur would have produced.**`r_HDRDiffractionMaxSources`** (default 256, **hard cap 4096**, 0 = unlimited is no longerallowed) caps the population. Three smallpasses reduce the thresholded source to one texel holding `(cutoff, raw count, count above thecutoff, share of the excess below it)`; the last one corrects the cutoff from the previousframe's count with a multiplicative controller, and the sprite vertex shader reads that one texel:```w = smoothstep(cut, 2*cut, E)          one STOP of crossfade, not a hard cutw >= 1   full sprite, as beforew <= 0   NO sprite - one texel carrying E * gain * (deposited glare fraction) instead```Three properties are load bearing:* **Energy conserving.** A source that loses its star keeps its light: the whole quantity the  sprite would have spread is deposited at the source's own position, in the same layer, which is  bilinearly upscaled into the HDR target. Turning the budget on cannot darken a scene. (It does  not *spread* the residual ??the bloom chain has already run by the time this pass is called, so  there is no bloom input left to add to. A veiling pass for the residual is the next step.)* **No popping.** The crossfade is a stop wide, and the controller is asymmetric (fast up, slow  down) with a dead band at `[0.8N, N]`, so a source drifting past the budget while the camera  pans fades its rays over several frames.* **Bit-identical when it is not needed.** `raw <= N` drives the cutoff to zero, and a zero cutoff  is the un-budgeted vertex shader exactly. The budget is gated on `IsSceneReferredStage(5)`, so  off the switch the branch is never entered at all.Worst case is then `N` sprites plus one pixel per remaining source, i.e. bounded by the budget and`Reach` rather than by how bright the scene is.**The hard cap (2026-09-05).** The budget's own upper end could still hang the editor, so it isclamped at the consumer at **4096** and `0` no longer means unlimited. Measured on the airfieldwith the sun in frame at `Streak Amount 200`: 256 is the first value at which the sun's star is*stable* (below it, it flickers), 512 fans it up, 1024 costs a large slice of the frame rate,**2048 is the last value at which the picture changes at all**, 16384 puts a star on everyreflection and 32768 ran at 2 fps and held the main thread long enough for Windows to killSandbox as `AppHangB1`. 4096 is one stop of headroom over the last setting that alters thepicture. A clamp is reported in the log rather than applied silently.**Measuring it.** `r_HDRDiffractionDebug 3`'s once-a-second report leads with the population,raw first, and with the **GPU cost** under it, so the count and what it buys are on adjacentlines:```[BLOOM]   SOURCES: raw above threshold = 51234 of the 960 x 540 grid -- one sprite each,          162 x 162 half-res px per sprite = 1344.6 Mpx of blending unbudgeted[BLOOM]            budget 256 (r_HDRDiffractionMaxSources): 261 drew a star, cutoff 2.9 = +4.0          stops -- the other 50973 kept their LIGHT (12.4% of the source excess, deposited at          the source) and lost their shape[BLOOM]            GPU 1.83 ms in the sprite pass + 0.11 ms in the composite = 1.94 ms of this          frame, for 261 sprites (7.4 us each)```The counts are the GPU's own arithmetic over that frame's source, not an estimate, and the rawcount uses exactly the vertex shader's collapse test. The milliseconds are the render pipelineprofiler's own GPU timestamps for the `HDR_DIFFRACTION` and `HDR_DIFFRACTION_COMPOSITE` sections(smoothed), which means they need the profiler to be **recording**: with `r_profiler 0` and`r_DisplayInfo` under 3 the line says so instead of printing a zero that would read as free.**The analytic streaks, the anamorphic flare and halation need no budget and do not have one.**They are directional stretch blurs: a fixed number of full-screen passes whose cost is independentof how many texels cleared the threshold. Only the threshold *constant* is shared. The **frontfilter PSF** is the one other sparse sprite scatter in the file and has the same failure mode at aquarter of the candidate cells; when it is budgeted it should reuse this chain rather than growits own. Full reasoning: `scene-notes/decisions/s8-glare-budget.md`.## 4. ParametersComponent `SCineStreakParams` (Diffraction Streaks): `Mode` (enum Analytic/Wave, default Wave),`Streak Amount` ('stAm', 0..200 with a 0..100 slider, default 50 ??a source radiance boost,禮2.4; Wave only ??in Analytic the f-number ramp still owns the amount), `Reach` ('stRc'), `Dispersion` ('stDs'). Existing `Blade Rotation`, `EnableStreaks`, threshold unchanged. Post params: `HDR_Streaks_Mode`, `HDR_Streaks_WaveAmount`,`HDR_Streaks_WaveReach`, `HDR_Streaks_Dispersion`; `SHDRStreaksParams` gains the four fields.Pupil description reaches the bloom stage through the DOF stage (`GetStage<CDepthOfFieldStage>()`),not through more post params: the DOF stage already resolves blades / exponent / phase / mask /mask rotation / mask scale / `k_sa` / `N_geo` every frame ??expose them from there.Cvars: `r_HDRDiffractionKernel` (1), `r_HDRDiffractionDebug` (0), `r_HDRDiffractionSAWaves`(0.25), `r_HDRDiffractionKernelSize` (512; 256 allowed for a 4? cheaper job at half thestructural resolution ??same `d = L/2`)., `r_HDRDiffractionMaxSources` (256; hard cap 4096,禮3.1 ??the source budget, scene-referred path only).Schematyc `szName`s `[A-Za-z0-9_]+`; enum reflection as the other enums in the component.## 5. Limitations, stated up front* On-axis pupil only: the corners do not get their cat-eye-clipped stars. Later.* Shift-invariant: the pattern of a source is the same anywhere in the frame (true for the  iris; not for a vignetting barrel).* The `1/u簡` extrapolation is exact for straight edges only; rounded pupils fade faster than  the tail says ??but their ring is small, so the error is invisible.* The kernel is built on the CPU with a frame or two of latency after a shape change; during  a fast blade-rotation animation the pattern lags by that much. Acceptable; a compute-shader  FFT is the upgrade path.* Spectral sampling at six wavelengths shows slight banding in very long rays; raise `n_弇`  if the user sees it.* The source budget's residual is deposited AT the source rather than spread as a veil, and  sources inside the one-stop crossfade drop their residual entirely (a fraction of a fraction of  a percent of the frame). The budget's controller is also one frame behind, so a hard cut into a  very bright scene overshoots `N` for two or three frames before it settles.* No source MERGE: a lamp that clears the threshold over a 5?5 block spends 25 of the budget's  slots. The report's `SOURCES` line is the instrument that will say when that starts to matter;  `decisions/s8-glare-budget.md` approach B is the answer when it does.## 6. Acceptance (user, by eye)1. Analytic mode: byte-identical to before (A/B).2. Wave mode, 8 blades, f/16, a lamp: 8 sharp rays with rainbow tails (blue near the core,   red at the tips), turning with Blade Rotation; f/8 ??shorter rays; f/2.8 ??nothing visible   beyond the bloom. No fade-in ramp needed.3. 3 blades ??6 rays; 9 blades ??18 rays (the analytic mode clamps at 24 line pairs; here   there is no clamp because it is not drawing lines).4. A shape mask (star / heart): the glare is the mask's own diffraction pattern, turning with   Mask Rotation; stopping the iris down through the mask (N > N_mask) changes it as the   polygon takes over.5. Blade Curvature 1 at f/2.8 (rounded polygon): rays soften and shorten compared with   curvature 0.6. Spherical Aberration 簣1 (with `r_HDRDiffractionSAWaves` 0.25): faint rings / halo around   the core of the pattern; 0 ??none.7. Dispersion 0: white rays.8. `Streak Amount` 1 reads as physically plausible against the lamp; 0 ??no pass;   `r_HDRDiffractionKernel 0` ??analytic fallback, one log line.9. RenderDoc: sprite pass < 0.3 ms at 1080p with a few sources; kernel job time in the log;   no hitch on the render thread when the pupil changes.## 7. DocsREADME: "Diffraction Streaks" group table (Mode, Streak Amount, Reach, Dispersion) and the"Diffraction streak physics" section rewritten around the wave model (keep a short Analyticparagraph). ConsoleReference: the four cvars. This spec listed. English.## 8. Build, deploy, commitBundled CMake, `CryRenderD3D11` ??`CinematicCamera`, 0 error / 0 warning outside `Code/SDKs`.Deploy the DLLs and `HDRPostProcess.cfx` (and `DepthOfField.cfx` if touched) per HANDOVER; alocked file means the engine is running ??report, do not kill. Commit on `glare` per reviewedstep; do not touch `dev`; no `sync.ps1`.