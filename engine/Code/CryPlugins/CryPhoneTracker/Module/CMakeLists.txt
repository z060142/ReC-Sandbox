start_sources()sources_platform(ALL)add_sources("CryPhoneTracker_uber_0.cpp"	SOURCE_GROUP "Root"		"resource.h"		"StdAfx.cpp"		"StdAfx.h"		"PhoneTrackerPlugin.h"		"PhoneTrackerPlugin.cpp"	SOURCE_GROUP "Components"		"PhoneTrackerProtocol.h"		"PoseFilter.h"		"PhoneTrackerComponent.h"		"PhoneTrackerComponent.cpp")end_sources()CryEngineModule(CryPhoneTracker PCH "StdAfx.cpp" SOLUTION_FOLDER "CryPlugins")target_include_directories(${THIS_PROJECT}	PRIVATE		"${CRYENGINE_DIR}/Code/CryEngine/CryCommon"		"${CRYENGINE_DIR}/Code/CryPlugins/CinematicCamera/Interface")# CinematicCamera is reached through CryGetCinematicCameraOptics at runtime, so there is# deliberately no link dependency here; only ws2_32 for the UDP socket.target_link_libraries(${THIS_PROJECT} PRIVATE ws2_32)