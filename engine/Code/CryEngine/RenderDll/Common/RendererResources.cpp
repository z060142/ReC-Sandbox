// Copyright 2017-2021 Crytek GmbH / Crytek Group. All rights reserved.#include "StdAfx.h"#include <CryRenderer/IFlares.h>#include "RendererResources.h"#include "RendererCVars.h"#include "Renderer.h"#include "Textures/Texture.h"#include "../XRenderD3D9/D3DDeferredShading.h"#include "../XRenderD3D9/D3DPostProcess.h"#include "RendElements/FlareSoftOcclusionQuery.h"#include <map>////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////void SRenderTargetPool::AddRenderTarget(uint32 nWidth, uint32 nHeight, const ColorF& cClear, ETEX_Format Format, float fPriority, const char* szName, CTexture** pStorage, uint32 nFlags, int32 nCustomID, bool bDynamicTex){	SRenderTargetInfo drt;	drt.nWidth = nWidth;	drt.nHeight = nHeight;	drt.cClearColor = cClear;	drt.nFlags = FT_USAGE_RENDERTARGET | FT_DONT_STREAM | nFlags;	drt.Format = Format;	drt.fPriority = fPriority;	drt.lplpStorage = pStorage;	drt.nCustomID = nCustomID;	drt.nPitch = nWidth * CTexture::BytesPerPixel(Format);	cry_strcpy(drt.szName, szName);	m_pRenderTargets.push_back(drt);}bool SRenderTargetPool::CreateRenderTargetList(){	std::sort(m_pRenderTargets.begin(), m_pRenderTargets.end(), RenderTargetSizeSort());	for (uint32 t = 0; t < m_pRenderTargets.size(); ++t)	{		SRenderTargetInfo& drt = m_pRenderTargets[t];		CTexture*& pTex = (*drt.lplpStorage);		if (!pTex && !(pTex = CTexture::GetOrCreateRenderTarget(drt.szName, drt.nWidth, drt.nHeight, drt.cClearColor, eTT_2D, drt.nFlags, drt.Format, drt.nCustomID)))			continue;		pTex->SetFlags(drt.nFlags);		pTex->SetWidth(drt.nWidth);		pTex->SetHeight(drt.nHeight);		pTex->CreateRenderTarget(drt.Format, drt.cClearColor);		if (!(pTex->GetFlags() & FT_FAILED))		{			// Clear render target surface before using it			pTex->Clear();		}	}	m_pRenderTargets.clear();	return true;}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////SPixFormatSupport CRendererResources::s_hwTexFormatSupport;CTexture* CRendererResources::s_ptexNoTexture;CTexture* CRendererResources::s_ptexNoTextureCM;CTexture* CRendererResources::s_ptexWhite;CTexture* CRendererResources::s_ptexWhite3D;CTexture* CRendererResources::s_ptexGray;CTexture* CRendererResources::s_ptexMedian;CTexture* CRendererResources::s_ptexBlack;CTexture* CRendererResources::s_ptexBlackAlpha;CTexture* CRendererResources::s_ptexBlackCM;CTexture* CRendererResources::s_ptexDefaultProbeCM;CTexture* CRendererResources::s_ptexDefaultMergedDetail;CTexture* CRendererResources::s_ptexFlatBump;#if !defined(_RELEASE)CTexture* CRendererResources::s_ptexMipMapDebug;CTexture* CRendererResources::s_ptexColorBlue;CTexture* CRendererResources::s_ptexColorCyan;CTexture* CRendererResources::s_ptexColorGreen;CTexture* CRendererResources::s_ptexColorPurple;CTexture* CRendererResources::s_ptexColorRed;CTexture* CRendererResources::s_ptexColorWhite;CTexture* CRendererResources::s_ptexColorYellow;CTexture* CRendererResources::s_ptexColorOrange;CTexture* CRendererResources::s_ptexColorMagenta;#endifCTexture* CRendererResources::s_ptexPaletteDebug;CTexture* CRendererResources::s_ptexPaletteTexelsPerMeter;CTexture* CRendererResources::s_ptexIconShaderCompiling;CTexture* CRendererResources::s_ptexIconStreaming;CTexture* CRendererResources::s_ptexIconStreamingTerrainTexture;CTexture* CRendererResources::s_ptexIconNavigationProcessing;CTexture* CRendererResources::s_ptexMipColors_Diffuse;CTexture* CRendererResources::s_ptexMipColors_Bump;CTexture* CRendererResources::s_ptexShadowJitterMap;CTexture* CRendererResources::s_ptexEnvironmentBRDF;CTexture* CRendererResources::s_ptexScreenNoiseMap;CTexture* CRendererResources::s_ptexDissolveNoiseMap;CTexture* CRendererResources::s_ptexNoise3D;CTexture* CRendererResources::s_ptexGrainFilterMap;CTexture* CRendererResources::s_ptexFilmGrainMap;CTexture* CRendererResources::s_ptexVignettingMap;CTexture* CRendererResources::s_ptexAOJitter;CTexture* CRendererResources::s_ptexAOVOJitter;CTexture* CRendererResources::s_ptexFromRE[8];CTexture* CRendererResources::s_ptexShadowID[8];CTexture* CRendererResources::s_ptexFromRE_FromContainer[2];CTexture* CRendererResources::s_ptexFromObj;CTexture* CRendererResources::s_ptexRT_2D;CTexture* CRendererResources::s_ptexNormalsFitting;CTexture* CRendererResources::s_ptexPerlinNoiseMap;CTexture* CRendererResources::s_ptexLTC1;CTexture* CRendererResources::s_ptexLTC2;CTexture* CRendererResources::s_ptexWindGrid;// Post-process related texturesCTexture* CRendererResources::s_ptexDisplayTargetScaledPrev;CTexture* CRendererResources::s_ptexWaterOcean;CTexture* CRendererResources::s_ptexWaterVolumeTemp[2];CTexture* CRendererResources::s_ptexWaterVolumeDDN;CTexture* CRendererResources::s_ptexFarPlane;CTexture* CRendererResources::s_ptexCloudsLM;CTexture* CRendererResources::s_ptexHDRAdaptedLuminanceCur[8];int CRendererResources::s_nCurLumTextureIndex;CTexture* CRendererResources::s_ptexCurLumTexture;CTexture* CRendererResources::s_ptexHDRToneMaps[NUM_HDR_TONEMAP_TEXTURES];CTexture* CRendererResources::s_ptexHDRMeasuredLuminanceDummy;CTexture* CRendererResources::s_ptexColorChart;CTexture* CRendererResources::s_ptexFlaresOcclusionRing[MAX_OCCLUSION_READBACK_TEXTURES] = { NULL };CTexture* CRendererResources::s_ptexFlaresGather = NULL;CTexture* CRendererResources::s_pTexNULL = 0;CTexture* CRendererResources::s_ptexVolumetricFog = NULL;SEnvTexture CRendererResources::s_EnvTexts[MAX_ENVTEXTURES];TArray<SEnvTexture> CRendererResources::s_CustomRT_2D;TArray<CTexture> CRendererResources::s_ShaderTemplates(EFTT_MAX);bool CRendererResources::s_ShaderTemplatesInitialized = false;ETEX_Format CRendererResources::s_eTFZ = eTF_R32F;////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////bool CRendererResources::m_bLoadedSystem = false;void CRendererResources::UnloadDefaultSystemTextures(bool bFinalRelease){	// Keep these valid - if any textures failed to load and also leaked across the shutdown,	// they'll be left with a dangling dev texture pointer otherwise.	//SAFE_RELEASE_FORCE(s_ptexNoTexture);	//SAFE_RELEASE_FORCE(s_ptexNoTextureCM);	//SAFE_RELEASE_FORCE(s_ptexWhite);	//SAFE_RELEASE_FORCE(s_ptexGray);	//SAFE_RELEASE_FORCE(s_ptexBlack);	//SAFE_RELEASE_FORCE(s_ptexBlackAlpha);	//SAFE_RELEASE_FORCE(s_ptexBlackCM);	//SAFE_RELEASE_FORCE(s_ptexFlatBump);#if !defined(_RELEASE)	//SAFE_RELEASE_FORCE(s_ptexDefaultMergedDetail);	//SAFE_RELEASE_FORCE(s_ptexMipMapDebug);	//SAFE_RELEASE_FORCE(s_ptexColorBlue);	//SAFE_RELEASE_FORCE(s_ptexColorCyan);	//SAFE_RELEASE_FORCE(s_ptexColorGreen);	//SAFE_RELEASE_FORCE(s_ptexColorPurple);	//SAFE_RELEASE_FORCE(s_ptexColorRed);	//SAFE_RELEASE_FORCE(s_ptexColorWhite);	//SAFE_RELEASE_FORCE(s_ptexColorYellow);	//SAFE_RELEASE_FORCE(s_ptexColorMagenta);	//SAFE_RELEASE_FORCE(s_ptexColorOrange);#endif	//SAFE_RELEASE_FORCE(s_ptexPaletteDebug);	//SAFE_RELEASE_FORCE(s_ptexShadowJitterMap);	//SAFE_RELEASE_FORCE(s_ptexEnvironmentBRDF);	//SAFE_RELEASE_FORCE(s_ptexScreenNoiseMap);	//SAFE_RELEASE_FORCE(s_ptexDissolveNoiseMap);	//SAFE_RELEASE_FORCE(s_ptexNoise3D);	//SAFE_RELEASE_FORCE(s_ptexGrainFilterMap);	//SAFE_RELEASE_FORCE(s_ptexFilmGrainMap);	//SAFE_RELEASE_FORCE(s_ptexVignettingMap);	//SAFE_RELEASE_FORCE(s_ptexAOJitter);	//SAFE_RELEASE_FORCE(s_ptexAOVOJitter);	SAFE_RELEASE_FORCE(s_ptexRT_2D);	SAFE_RELEASE_FORCE(s_ptexCloudsLM);	//SAFE_RELEASE_FORCE(s_ptexHitAreaRT[0]);	//SAFE_RELEASE_FORCE(s_ptexHitAreaRT[1]);	SAFE_RELEASE_FORCE(s_ptexVolumetricFog);	uint32 i;	for (i = 0; i < 8; i++)	{		//SAFE_RELEASE_FORCE(s_ptexFromRE[i]);	}	for (i = 0; i < 8; i++)	{		SAFE_RELEASE_FORCE(s_ptexShadowID[i]);	}	for (i = 0; i < 2; i++)	{		//SAFE_RELEASE_FORCE(s_ptexFromRE_FromContainer[i]);	}	SAFE_RELEASE_FORCE(s_ptexColorChart);	SAFE_RELEASE_FORCE(s_ptexWindGrid);	for (i = 0; i < MAX_ENVTEXTURES; i++)	{		s_EnvTexts[i].Release();	}	//m_EnvTexTemp.Release();	SAFE_RELEASE_FORCE(s_ptexMipColors_Diffuse);	SAFE_RELEASE_FORCE(s_ptexMipColors_Bump);	SAFE_RELEASE_FORCE(s_ptexFarPlane);	s_CustomRT_2D.Free();	//s_ShaderTemplates.Free();	// release targets pools	SDynTexture::ShutDown();	//ReleaseSystemTargets();	m_bLoadedSystem = false;}void CRendererResources::LoadDefaultSystemTextures(){	CRY_PROFILE_FUNCTION(PROFILE_LOADING_ONLY);	char str[256];	int i;	MEMSTAT_CONTEXT(EMemStatContextType::Other, "Engine textures");	if (!m_bLoadedSystem)	{		m_bLoadedSystem = true;		// Textures loaded directly from file		struct		{			CTexture*&  pTexture;			const char* szFileName;			uint32      flags;		}		texturesFromFile[] =		{			{ s_ptexWhite,                       "%ENGINE%/EngineAssets/Textures/White.dds",                         FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexBlack,                       "%ENGINE%/EngineAssets/Textures/Black.dds",                         FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexBlackAlpha,                  "%ENGINE%/EngineAssets/Textures/BlackAlpha.dds",                    FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexBlackCM,                     "%ENGINE%/EngineAssets/Textures/BlackCM.dds",                       FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexGray,                        "%ENGINE%/EngineAssets/Textures/Grey.dds",                          FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexDefaultMergedDetail,         "%ENGINE%/EngineAssets/Textures/GreyAlpha.dds",                     FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexDefaultProbeCM,              "%ENGINE%/EngineAssets/Shading/defaultProbe_cm.tif",                FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexFlatBump,                    "%ENGINE%/EngineAssets/Textures/White_ddn.dds",                     FT_DONT_RELEASE | FT_DONT_STREAM | FT_TEX_NORMAL_MAP },			{ s_ptexPaletteDebug,                "%ENGINE%/EngineAssets/Textures/palletteInst.dds",                  FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexPaletteTexelsPerMeter,       "%ENGINE%/EngineAssets/Textures/TexelsPerMeterGrad.dds",            FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexIconShaderCompiling,         "%ENGINE%/EngineAssets/Icons/ShaderCompiling.tif",                  FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexIconStreaming,               "%ENGINE%/EngineAssets/Icons/Streaming.tif",                        FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexIconStreamingTerrainTexture, "%ENGINE%/EngineAssets/Icons/StreamingTerrain.tif",                 FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexIconNavigationProcessing,    "%ENGINE%/EngineAssets/Icons/NavigationProcessing.tif",             FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexShadowJitterMap,             "%ENGINE%/EngineAssets/Textures/rotrandom.dds",                     FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexEnvironmentBRDF,             "%ENGINE%/EngineAssets/Shading/environmentBRDF.tif",                FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexScreenNoiseMap,              "%ENGINE%/EngineAssets/Textures/JumpNoiseHighFrequency_x27y19.dds", FT_DONT_RELEASE | FT_DONT_STREAM | FT_NOMIPS },			{ s_ptexDissolveNoiseMap,            "%ENGINE%/EngineAssets/Textures/noise.dds",                         FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexNoise3D,                     "%ENGINE%/EngineAssets/Textures/noise3d.dds",                       FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexGrainFilterMap,              "%ENGINE%/EngineAssets/ScreenSpace/grain_bayer_mul.tif",            FT_DONT_RELEASE | FT_DONT_STREAM | FT_NOMIPS },			{ s_ptexFilmGrainMap,                "%ENGINE%/EngineAssets/ScreenSpace/film_grain.dds",                 FT_DONT_RELEASE | FT_DONT_STREAM | FT_NOMIPS },			{ s_ptexVignettingMap,               "%ENGINE%/EngineAssets/Shading/vignetting.tif",                     FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexAOJitter,                    "%ENGINE%/EngineAssets/ScreenSpace/PointsOnSphere4x4.tif",          FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexAOVOJitter,                  "%ENGINE%/EngineAssets/ScreenSpace/PointsOnSphereVO4x4.tif",        FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexNormalsFitting,              "%ENGINE%/EngineAssets/ScreenSpace/NormalsFitting.dds",             FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexPerlinNoiseMap,              "%ENGINE%/EngineAssets/Textures/perlinNoise2D.dds",                 FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexLTC1,                        "%ENGINE%/EngineAssets/Shading/ltc_1.dds",                          FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexLTC2,                        "%ENGINE%/EngineAssets/Shading/ltc_2.dds",                          FT_DONT_RELEASE | FT_DONT_STREAM },#if !defined(_RELEASE)			{ s_ptexNoTexture,                   "%ENGINE%/EngineAssets/TextureMsg/ReplaceMe.tif",                   FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexNoTextureCM,                 "%ENGINE%/EngineAssets/TextureMsg/ReplaceMeCM.tif",                 FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexMipMapDebug,                 "%ENGINE%/EngineAssets/TextureMsg/MipMapDebug.dds",                 FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorBlue,                   "%ENGINE%/EngineAssets/TextureMsg/color_Blue.dds",                  FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorCyan,                   "%ENGINE%/EngineAssets/TextureMsg/color_Cyan.dds",                  FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorGreen,                  "%ENGINE%/EngineAssets/TextureMsg/color_Green.dds",                 FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorPurple,                 "%ENGINE%/EngineAssets/TextureMsg/color_Purple.dds",                FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorRed,                    "%ENGINE%/EngineAssets/TextureMsg/color_Red.dds",                   FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorWhite,                  "%ENGINE%/EngineAssets/TextureMsg/color_White.dds",                 FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorYellow,                 "%ENGINE%/EngineAssets/TextureMsg/color_Yellow.dds",                FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorOrange,                 "%ENGINE%/EngineAssets/TextureMsg/color_Orange.dds",                FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexColorMagenta,                "%ENGINE%/EngineAssets/TextureMsg/color_Magenta.dds",               FT_DONT_RELEASE | FT_DONT_STREAM },#else			{ s_ptexNoTexture,                   "%ENGINE%/EngineAssets/Textures/Grey.dds",                          FT_DONT_RELEASE | FT_DONT_STREAM },			{ s_ptexNoTextureCM,                 "%ENGINE%/EngineAssets/Shading/defaultProbe_cm.tif",                FT_DONT_RELEASE | FT_DONT_STREAM },#endif		};		for (int t = 0; t < CRY_ARRAY_COUNT(texturesFromFile); ++t)		{			if (!texturesFromFile[t].pTexture)			{				texturesFromFile[t].pTexture = CTexture::ForName(texturesFromFile[t].szFileName, texturesFromFile[t].flags, eTF_Unknown);				if (texturesFromFile[t].pTexture->GetFlags() & FT_FAILED)				{					CryFatalError("Default system texture \"%s\" failed to load, please make sure the engine.pak or the engine folder is available.", texturesFromFile[t].szFileName);				}				if (!texturesFromFile[t].pTexture || !texturesFromFile[t].pTexture->IsLoaded() || !texturesFromFile[t].pTexture->GetDevTexture())				{					CryFatalError("Can't open %s texture file.", texturesFromFile[t].szFileName);				}			}		}		// Associate dummy NULL-resource with s_pTexNULL		s_pTexNULL = new CTexture(FT_DONT_RELEASE | FT_DONT_STREAM, Clr_Transparent, CDeviceTexture::Associate(s_ptexBlackAlpha->GetLayout(), nullptr));		// Default Template textures		int nRTFlags = FT_DONT_RELEASE | FT_DONT_STREAM | FT_STATE_CLAMP | FT_USAGE_RENDERTARGET;		s_ptexMipColors_Diffuse = CTexture::GetOrCreateTextureObject("$MipColors_Diffuse", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_MIPCOLORS_DIFFUSE);		s_ptexMipColors_Bump = CTexture::GetOrCreateTextureObject("$MipColors_Bump", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_MIPCOLORS_BUMP);		s_ptexRT_2D = CTexture::GetOrCreateTextureObject("$RT_2D", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_RT_2D);		//s_ptexHitAreaRT[0] = CTexture::CreateTextureObject("$HitEffectAccumBuffRT_0", 128, 128, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown);		//s_ptexHitAreaRT[1] = CTexture::CreateTextureObject("$HitEffectAccumBuffRT_1", 128, 128, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown);		s_ptexWhite3D = CTexture::GetOrCreateTextureObject("White3D", 0, 0, 1, eTT_3D, FT_DONT_RELEASE | FT_DONT_STREAM, eTF_Unknown, TO_WINDGRID);		if (!CTexture::IsTextureExist(s_ptexWhite3D))		{			static float white[4] = { 1, 1, 1, 1 };			s_ptexWhite3D->Create3DTexture(1, 1, 1, 1, FT_DONT_RELEASE | FT_DONT_STREAM, (const byte*)white, eTF_R32G32B32A32F);		}		s_ptexWindGrid = CTexture::GetOrCreateTextureObject("WindGrid", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM, eTF_Unknown, TO_WINDGRID);		if (!CTexture::IsTextureExist(s_ptexWindGrid))		{			s_ptexWindGrid->Create2DTexture(256, 256, 1, FT_DONT_RELEASE | FT_DONT_STREAM, nullptr, eTF_R16G16F);		}		s_ptexMedian = CTexture::GetOrCreateTextureObject("$MedianColor", 8, 8, 1, eTT_2D, FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown);		if (!CTexture::IsTextureExist(s_ptexMedian))		{			s_ptexMedian->CreateRenderTarget(eTF_R8G8B8A8, Clr_Median);			CClearSurfacePass::Execute(s_ptexMedian, Clr_Median);		}		s_ptexFarPlane = CTexture::GetOrCreateTextureObject("$FarPlane", 8, 8, 1, eTT_2D, FT_DONT_STREAM | FT_USAGE_DEPTHSTENCIL, eTF_Unknown);		//	if (!s_ptexBackBuffer)		{			s_ptexDisplayTargetScaledPrev = CTexture::GetOrCreateTextureObject("$DisplayTarget 1 / 2p", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown);			s_ptexFlaresGather = CTexture::GetOrCreateTextureObject("$FlaresGather", 0, 0, 1, eTT_2D, nRTFlags, eTF_R8 /* 1-bit used only */);			for (i = 0; i < MAX_OCCLUSION_READBACK_TEXTURES; i++)			{				cry_sprintf(str, "$FlaresOcclusion_%d", i);				s_ptexFlaresOcclusionRing[i] = CTexture::GetOrCreateTextureObject(str, 0, 0, 1, eTT_2D, nRTFlags, eTF_R8G8);			}			// fixme: get texture resolution from CREWaterOcean			// TODO: make s_ptexWaterVolumeTemp an array texture			s_ptexWaterOcean = CTexture::GetOrCreateTextureObject("$WaterOceanMap", 64, 64, 1, eTT_2D, FT_DONT_RELEASE | FT_NOMIPS | FT_DONT_STREAM, eTF_Unknown, TO_WATEROCEANMAP);			s_ptexWaterVolumeTemp[0] = CTexture::GetOrCreateTextureObject("$WaterVolumeTemp_0", 64, 64, 1, eTT_2D, /*FT_DONT_RELEASE |*/ FT_NOMIPS | FT_DONT_STREAM, eTF_Unknown);			s_ptexWaterVolumeTemp[1] = CTexture::GetOrCreateTextureObject("$WaterVolumeTemp_1", 64, 64, 1, eTT_2D, /*FT_DONT_RELEASE |*/ FT_NOMIPS | FT_DONT_STREAM, eTF_Unknown);			s_ptexWaterVolumeDDN = CTexture::GetOrCreateTextureObject("$WaterVolumeDDN", 64, 64, 1, eTT_2D, /*FT_DONT_RELEASE |*/ FT_DONT_STREAM | FT_USAGE_RENDERTARGET | FT_FORCE_MIPS, eTF_Unknown, TO_WATERVOLUMEMAP);		}		// Create dummy texture object for terrain and clouds lightmap		s_ptexCloudsLM = CTexture::GetOrCreateTextureObject("$CloudsLM", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_CLOUDS_LM);		for (i = 0; i < 8; i++)		{			cry_sprintf(str, "$FromRE_%d", i);			if (!s_ptexFromRE[i])				s_ptexFromRE[i] = CTexture::GetOrCreateTextureObject(str, 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_FROMRE0 + i);		}		for (i = 0; i < 8; i++)		{			cry_sprintf(str, "$ShadowID_%d", i);			s_ptexShadowID[i] = CTexture::GetOrCreateTextureObject(str, 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_SHADOWID0 + i);		}		for (i = 0; i < 2; i++)		{			cry_sprintf(str, "$FromRE%d_FromContainer", i);			if (!s_ptexFromRE_FromContainer[i])				s_ptexFromRE_FromContainer[i] = CTexture::GetOrCreateTextureObject(str, 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_FROMRE0_FROM_CONTAINER + i);		}		s_ptexColorChart = CTexture::GetOrCreateTextureObject("$ColorChart", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_RENDERTARGET, eTF_Unknown, TO_COLORCHART);		for (i = 0; i < EFTT_MAX; i++)		{			new(&s_ShaderTemplates[i])CTexture(FT_DONT_RELEASE);			s_ShaderTemplates[i].SetCustomID(EFTT_DIFFUSE + i);			s_ShaderTemplates[i].SetFlags(FT_DONT_RELEASE);		}		s_ShaderTemplatesInitialized = true;		s_ptexVolumetricFog = CTexture::GetOrCreateTextureObject("$VolFogInscattering", 0, 0, 0, eTT_3D, FT_NOMIPS | FT_DONT_RELEASE | FT_DONT_STREAM | FT_USAGE_UNORDERED_ACCESS, eTF_Unknown);	}}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////ETEX_Format CRendererResources::GetHDRFormat(bool withAlpha, bool lowQuality){	ETEX_Format candidate = eTF_R16G16B16A16F;	if (!withAlpha && s_hwTexFormatSupport.IsFormatSupported(eTF_R11G11B10F))	{		if (CRenderer::CV_r_HDRTexFormat <= 1 && lowQuality)			candidate = eTF_R11G11B10F;		if (CRenderer::CV_r_HDRTexFormat <= 0)			candidate = eTF_R11G11B10F;	}	return candidate;}ETEX_Format CRendererResources::GetLDRFormat(bool precisionBits){	ETEX_Format candidate = eTF_R8G8B8A8;	if (precisionBits)		candidate = eTF_R10G10B10A2;	return candidate;}ETEX_Format CRendererResources::GetDisplayFormat(){	ETEX_Format candidate = eTF_R8G8B8A8;	return candidate;}// The component asks; the cvar may refuse. Same shape as the lens model's gate in// CDepthOfFieldStage::Execute(): the request travels on the post-effect parameter bus (it is// per-frame, per-camera data and the bus already has the reset/handover semantics for it), and// r_SceneReferred ANDs it away. When this returns false not one line of the scene-referred work// runs and every target, pass and constant keeps its stock value.// The pipeline gate (research/s8-editor-pipelines.md, decisions/s8-preview-gate.md). Set once per// render view on the render thread by CD3D9Renderer::LatchSceneReferredExposure(), read by every// scene-referred site through IsSceneReferred(). Default OPEN so that any render that somehow// reaches a gated site without passing the latch behaves exactly as it did before this gate// existed - the gate can only ever take work AWAY from a render, never add it.static bool        s_bSceneReferredPipelineGate = true;static const char* s_szSceneReferredPipelineName = "";// The remembered verdict is PER PIPELINE, and it has to be: a routine editor session cycles six to// ten pipelines within a single frame, and this function is called once for each of them. A single// remembered (verdict, name) pair - which is what the first version of this gate kept - is// therefore invalidated by the very next call, so "log only on a change" degenerated into one line// per pipeline per frame, and the two lines for 'Minimum_1' (no) and 'Standard_0' (yes) buried// every other line in the console. Keyed by pipeline name, the alternation is no longer a change.//// The reason string is part of the key of the decision, not decoration: 'Standard_0' going from// "owns a display transform" to "map screenshot - an asset, not a picture" is the same verdict// (no longer scene-referred) for a different cause, and that IS worth a line.struct SSceneReferredGateVerdict{	bool   bOpen;	string sReason;};static std::map<string, SSceneReferredGateVerdict> s_sceneReferredGateVerdicts;static bool                                        s_bSceneReferredGateRequestLogged = false;void CRendererResources::SetSceneReferredPipelineGate(bool bOpen, const char* szPipelineName, const char* szReason){	s_bSceneReferredPipelineGate = bOpen;	s_szSceneReferredPipelineName = szPipelineName ? szPipelineName : "";	// One line per decision, not per frame: the pair (pipeline, verdict) is what a user needs to	// see once when a preview stops being pre-exposed, and it is what a bug report needs to carry.	// It is only interesting while the camera is actually asking for the path.	if (!IsSceneReferredRequested())	{		// ...but the moment it stops asking is worth exactly one line, and it is also the moment to		// forget every verdict: the next time the switch comes on, the user wants the full roll call		// of pipelines again rather than silence left over from the previous session.		if (s_bSceneReferredGateRequestLogged)		{			s_bSceneReferredGateRequestLogged = false;			s_sceneReferredGateVerdicts.clear();			CryLogAlways("[SceneReferred] scene-referred no longer requested - the pipeline gate is idle");		}		return;	}	s_bSceneReferredGateRequestLogged = true;	const string sName(s_szSceneReferredPipelineName);	const string sReason(szReason ? szReason : "");	auto it = s_sceneReferredGateVerdicts.find(sName);	if (it != s_sceneReferredGateVerdicts.end() && it->second.bOpen == bOpen && it->second.sReason == sReason)		return;	SSceneReferredGateVerdict& verdict = s_sceneReferredGateVerdicts[sName];	verdict.bOpen = bOpen;	verdict.sReason = sReason;	CryLogAlways("[SceneReferred] pipeline '%s' scene-referred: %s (%s)",	             sName.c_str(), bOpen ? "yes" : "no", sReason.c_str());}bool CRendererResources::IsSceneReferredPipelineGateOpen(){	return s_bSceneReferredPipelineGate;}const char* CRendererResources::GetSceneReferredPipelineName(){	return s_szSceneReferredPipelineName;}bool CRendererResources::IsSceneReferred(){	// The gate first: it is a plain bool and it is false for every preview, thumbnail and minimap	// render, so the common editor case exits here without touching the parameter bus at all.	if (!s_bSceneReferredPipelineGate)		return false;	return IsSceneReferredRequested();}bool CRendererResources::IsSceneReferredRequested(){	if (CRenderer::CV_r_SceneReferred == 0)		return false;	CPostEffectsMgr* pPostMgr = PostEffectMgr();	if (!pPostMgr)		return false;	// Resolved once at CPostEffectsMgr::Init(), not looked up by name here: this runs from the	// tone map and from the pipeline resource update on every frame, and GetByName() is a CRC	// plus a map lookup behind a one-entry per-thread cache that the other params keep evicting.	//	// The value read is the one CParamFloat::SyncMainWithRender() copied into this thread's slot	// at the frame boundary, so it is the request ccam made for THIS frame and it cannot change	// underneath a frame that has already started.	CEffectParam* pRequest = pPostMgr->GetSceneReferredParam();	return pRequest != nullptr && pRequest->GetParam() > 0.5f;}// The bisection gate. IsSceneReferred() stays the master - nothing runs without it - and this// narrows the request to the stage items at or below r_SceneReferredStage, so that a bad frame// (or a crash) can be walked back to one item from the console instead of from a rebuild.//// Deliberately a plain AND of the master and the stage number, evaluated at the same sites the// master is already read, so at the default (99) every call is exactly IsSceneReferred() and the// stock path is untouched either way.bool CRendererResources::IsSceneReferredStage(int nStage){	return IsSceneReferred() && CRenderer::CV_r_SceneReferredStage >= nStage;}// The un-gated twin, for the world-shaped decisions. See the header.bool CRendererResources::IsSceneReferredRequestedStage(int nStage){	return IsSceneReferredRequested() && CRenderer::CV_r_SceneReferredStage >= nStage;}const float CRendererResources::kSceneReferredMidGrey = 0.18f;// The luminance weights of the space the HDR target is actually in this frame. One place, because// two of them would eventually disagree: the per-view constant buffer publishes this to every// pixel shader in the chain (as a DELTA from Rec.709, see GeneratePerViewConstantBuffer), and the// metering histogram's compute pass needs the same row without a per-view binding of its own.Vec3 CRendererResources::GetWorkingLuminanceWeights(){	static const Vec3 kRec709Y(0.2126f, 0.7152f, 0.0722f);	static const Vec3 kAP1Y(0.27222872f, 0.67408177f, 0.05368952f);	// IsSceneReferredWorkingSpace() and not IsSceneReferred(): this follows what is IN the buffer,	// so r_SceneReferredDebug 1 (skip the conversion) keeps the weights on Rec.709 as well.	return CRendererResources::IsSceneReferredWorkingSpace() ? kAP1Y : kRec709Y;}const float CRendererResources::kSceneReferredMeterCeilingStops = 14.0f;float CRendererResources::GetSceneReferredMeterCeiling(){	// Gated on stage 2, the exposure stage: the ceiling only means anything once the pre-exposure	// is what put absolute scene radiance in the HDR target. Zero off it, which is what the	// shader reads as "stock NaN filter, unchanged".	if (!IsSceneReferredStage(2))		return 0.0f;	return kSceneReferredMidGrey * powf(2.0f, kSceneReferredMeterCeilingStops);}float CRendererResources::GetSceneReferredWriterClamp(){	// Stage 2, the exposure stage: before the pre-exposure exists there is nothing that can	// overflow that did not overflow in stock CE, and a clamp there would be a change to the	// stock picture for no reason.	if (!IsSceneReferredStage(2))		return 0.0f;	return max(CRenderer::CV_r_SceneReferredWriterClamp, 0.0f);}float CRendererResources::GetSceneReferredSkyPeakClamp(float sceneExposure){	const float writerClamp = GetSceneReferredWriterClamp();	// Same stage-2 gate as the writer clamp, by construction: off the switch that returns 0 and	// so does this, which is what the sky shader reads as "stock NaN filter, unchanged".	if (!IsSceneReferredStage(2))		return 0.0f;	const float peakNits = max(CRenderer::CV_r_SceneReferredSkyPeakLuminance, 0.0f);	if (peakNits <= 0.0f)		return writerClamp;	// cd/m2 -> engine units -> exposed. Doing the pre-exposure HERE is the whole point: the	// resulting number moves with the aperture in exposed units precisely so that the physical	// luminance it stands for does NOT (research/s8-nishita-sky.md 5.5).	const float peakExposed = (peakNits / RENDERER_LIGHT_UNIT_SCALE) * max(sceneExposure, 0.0f);	return (writerClamp > 0.0f) ? min(peakExposed, writerClamp) : peakExposed;}// D11's one line of arithmetic, in one place, so that seven effects cannot each grow their own// slightly different idea of what a threshold means. The apply sites call this and store the// result; nothing downstream - no shader, no constant layout, no serialised default - knows the// difference.float CRendererResources::ResolveThreshold(float stored){	// Stage 5 on the bisection gate: reading the same stored number as stops above mid grey is	// part of the camera-grade half of the pipeline (S7), and below that stage every threshold	// keeps its absolute engine-unit meaning exactly as on the stock path.	if (!IsSceneReferredStage(5))		return max(stored, 0.0f);	return kSceneReferredMidGrey * powf(2.0f, clamp_tpl(stored, -6.0f, 16.0f));}// The working space of the HDR TARGET, which is a third question again: the scene-referred path// can be running while r_SceneReferredDebug 1 deliberately leaves the buffer in Rec.709 for an// A/B. Everything that reads the target's CONTENTS - the per-view luminance weights, and any// future consumer that has to know which primaries it is looking at - keys on this, so the A/B// stays coherent instead of measuring 709 data with AP1 weights.bool CRendererResources::IsSceneReferredWorkingSpace(){	// Stage 4 on the bisection gate: the working-space conversion is S3, and below that stage the	// HDR target still holds Rec.709 whatever else is running.	return IsSceneReferredStage(4) && CRenderer::CV_r_SceneReferredDebug != 1;}// The LIGHT-UNIT convention, which is a different question from "is the scene-referred pipeline// running" and has to be answered by a different authority (SceneReferredSpec.md D4,// decisions/s2-engine-side-switch.md).//// The sun colour these callers are correcting was computed inside CTimeOfDay::UpdateEnvLighting// under E3DPARAM_SCENE_REFERRED, and it is cached there until the next time-of-day update. Asking// the post-effect bus instead would be asking a different clock: the bus value changes the frame// ccam publishes it, the sun's value changes only when the time of day is re-evaluated. Reading// the same global that produced the number keeps the correction and the number in lockstep, which// is the entire point of the manual undos this gates.// Rec.709 linear -> ACEScg (AP1, D60 via Bradford), on the CPU, and ONLY while the HDR target is// actually in the working space.//// Every colour our own effects author - the anamorphic coating, the halation dye, the ghost// coatings, the diffraction spectrum - was picked by eye against a Rec.709 picture. Those numbers// stay Rec.709: they are what the artist chose, they are what the tooltips describe, and they are// what a preset file carries. What has to change is the moment they become a MULTIPLIER of AP1// data, and that is here, at the constant upload.//// The picture must not visibly move: this is the same colour expressed in different primaries,// not a different colour. What it fixes is a real error - a 709-encoded orange multiplying AP1// values is a slightly different orange, and the wider the gamut the further off it drifts.//// The matrix is CommonMath.cfi's REC709_TO_ACESCG to the digit, verified against PyOpenColorIO// 2.5 by tools/ocio-bake/verify_matrices.py. Every entry is non-negative and every row sums to// one, so (1,1,1) maps to (1,1,1) exactly: a white tint stays white and an untinted effect is// bit-identical either way.Vec3 CRendererResources::ToWorkingSpace(const Vec3& rec709){	if (!IsSceneReferredWorkingSpace())		return rec709;	return Vec3(		0.61309740f * rec709.x + 0.33952315f * rec709.y + 0.04737945f * rec709.z,		0.07019372f * rec709.x + 0.91635388f * rec709.y + 0.01345240f * rec709.z,		0.02061559f * rec709.x + 0.10956977f * rec709.y + 0.86981463f * rec709.z);}bool CRendererResources::IsSceneReferredLightUnits(){	if (!gEnv->p3DEngine)		return false;	// Stage 3 on the bisection gate. The 3D engine's own four decision sites read	// E3DPARAM_SCENE_REFERRED directly and cannot see a renderer cvar, so the stage is enforced	// where the parameter is PUBLISHED instead - CCinematicCameraComponent::ApplySceneReferred()	// withholds it below stage 3 - and the test here is the renderer-side half of the same gate.	if (CRenderer::CV_r_SceneReferredStage < 3)		return false;	Vec3 v(0.0f, 0.0f, 0.0f);	gEnv->p3DEngine->GetGlobalParameter(E3DPARAM_SCENE_REFERRED, v);	return v.x > 0.5f;}ETEX_Format CRendererResources::GetDepthFormat(){	return		gRenDev->GetDepthBpp() == 32 ? eTF_D32FS8 :		gRenDev->GetDepthBpp() == 24 ? eTF_D24S8 :		gRenDev->GetDepthBpp() ==  8 ? eTF_D16S8 : eTF_D16;}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////void CRendererResources::CreateSystemTargets(int resourceWidth, int resourceHeight){	if (!resourceWidth ) resourceWidth =  s_resourceWidth;	if (!resourceHeight) resourceHeight = s_resourceHeight;		if (!resourceWidth ) resourceWidth  = s_renderWidth;	if (!resourceHeight) resourceHeight = s_renderHeight;	CRY_ASSERT(resourceWidth);	CRY_ASSERT(resourceHeight);	if (!gcpRendD3D->m_bSystemTargetsInit)	{		// Create HDR targets		CreateHDRMaps(resourceWidth, resourceHeight);		// Create post effects targets		CreatePostFXMaps(resourceWidth, resourceHeight);		s_resourceWidth  = resourceWidth;		s_resourceHeight = resourceHeight;		gcpRendD3D->m_bSystemTargetsInit = 1;	}}void CRendererResources::ResizeSystemTargets(int renderWidth, int renderHeight){	int resourceWidth  = s_resourceWidth;	int resourceHeight = s_resourceHeight;#if 0	if (resourceWidth  < renderWidth ||		resourceHeight < renderHeight)	{		resourceWidth  = std::max(resourceWidth , renderWidth);		resourceHeight = std::max(resourceHeight, renderHeight);#else	if (resourceWidth  != renderWidth ||		resourceHeight != renderHeight)	{		resourceWidth  = renderWidth;		resourceHeight = renderHeight;#endif		// Resize HDR targets		CreateHDRMaps(resourceWidth, resourceHeight);		// Resize post effects targets		CreatePostFXMaps(resourceWidth, resourceHeight);		s_resourceWidth  = resourceWidth;		s_resourceHeight = resourceHeight;	}}void CRendererResources::DestroySystemTargets(){	if (gcpRendD3D->m_bSystemTargetsInit)	{		DestroyHDRMaps();		DestroyPostFXMaps();		SAFE_RELEASE(s_ptexWaterVolumeTemp[0]);		SAFE_RELEASE(s_ptexWaterVolumeTemp[1]);		SAFE_RELEASE_FORCE(s_ptexWaterOcean);		s_resourceWidth  = 0;		s_resourceHeight = 0;		gcpRendD3D->m_bSystemTargetsInit = 0;	}}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////void CRendererResources::CreateHDRMaps(int resourceWidth, int resourceHeight){	CD3D9Renderer* r = gcpRendD3D;	SRenderTargetPool* pHDRPostProcess = SRenderTargetPool::GetInstance();	pHDRPostProcess->ClearRenderTargetList();	CRY_DISABLE_WARN_UNUSED_VARIABLES();	const int width = resourceWidth, width_r2 = (width + 1) / 2, width_r4 = (width_r2 + 1) / 2, width_r8 = (width_r4 + 1) / 2;	const int height = resourceHeight, height_r2 = (height + 1) / 2, height_r4 = (height_r2 + 1) / 2, height_r8 = (height_r4 + 1) / 2, height_r16 = (height_r8 + 1) / 2;	const ETEX_Format nHDRFormat  = GetHDRFormat(false, false); // No alpha, default is HiQ, can be downgraded	const ETEX_Format nHDRQFormat = GetHDRFormat(false, true ); // No alpha, default is LoQ, can be upgraded	const ETEX_Format nHDRAFormat = GetHDRFormat(true , false); // With alpha	uint32 nHDRTargetFlags = FT_DONT_RELEASE;	uint32 nHDRTargetFlagsUAV = nHDRTargetFlags | (FT_USAGE_UNORDERED_ACCESS);  // UAV required for tiled deferred shading	CRY_RESTORE_WARN_UNUSED_VARIABLES();	for (int i = 0; i < 8; i++)	{		char szName[256];		sprintf(szName, "$HDRAdaptedLuminanceCur_%d", i);		pHDRPostProcess->AddRenderTarget(1, 1, Clr_White, eTF_R16G16F, 0.1f, szName, &s_ptexHDRAdaptedLuminanceCur[i], FT_DONT_RELEASE);	}#if RENDERER_ENABLE_FULL_PIPELINE	// TODO: make it a MIP-mapped resource and/or use compute	// Luminance rt	for (int i = 0; i < NUM_HDR_TONEMAP_TEXTURES; i++)	{		char szName[256];		int iSampleLen = 1 << (2 * i);		cry_sprintf(szName, "$HDRToneMap_%d", i);		pHDRPostProcess->AddRenderTarget(iSampleLen, iSampleLen, Clr_Dark, eTF_R16G16F, 0.7f, szName, &s_ptexHDRToneMaps[i], FT_DONT_RELEASE | (i == 0 ? FT_STAGE_READBACK : 0));	}	s_ptexHDRMeasuredLuminanceDummy = CTexture::GetOrCreateTextureObject("$HDRMeasuredLum_Dummy", 0, 0, 1, eTT_2D, FT_DONT_RELEASE | FT_DONT_STREAM, eTF_R16G16F, TO_HDR_MEASURED_LUMINANCE);#endif	pHDRPostProcess->CreateRenderTargetList();	r->m_vSceneLuminanceInfo = Vec4(1.0f, 1.0f, 1.0f, 1.0f);	r->m_fAdaptedSceneScaleLBuffer = r->m_fAdaptedSceneScale = r->m_fScotopicSceneScale = 1.0f;}void CRendererResources::DestroyHDRMaps(){	int i;	for (i = 0; i < 8; i++)	{		SAFE_RELEASE_FORCE(s_ptexHDRAdaptedLuminanceCur[i]);	}	for (i = 0; i < NUM_HDR_TONEMAP_TEXTURES; i++)	{		SAFE_RELEASE_FORCE(s_ptexHDRToneMaps[i]);	}	SAFE_RELEASE_FORCE(s_ptexHDRMeasuredLuminanceDummy);	s_ptexCurLumTexture = NULL;}//==================================================================================================bool CRendererResources::CreatePostFXMaps(int resourceWidth, int resourceHeight){#if RENDERER_ENABLE_FULL_PIPELINE		CRY_DISABLE_WARN_UNUSED_VARIABLES();	const int width = resourceWidth, width_r2 = (width + 1) / 2, width_r4 = (width_r2 + 1) / 2, width_r8 = (width_r4 + 1) / 2;	const int height = resourceHeight, height_r2 = (height + 1) / 2, height_r4 = (height_r2 + 1) / 2, height_r8 = (height_r4 + 1) / 2;	const ETEX_Format nHDRFormat  = GetHDRFormat(false, false); // No alpha, default is HiQ, can be downgraded	const ETEX_Format nHDRAFormat = GetHDRFormat(true , false); // With alpha	const ETEX_Format nHDRQFormat = GetHDRFormat(false, true ); // No alpha, default is LoQ, can be upgraded	const ETEX_Format nLDRPFormat = GetLDRFormat(true);         // With more than 8 mantissa bits for calculations	CRY_RESTORE_WARN_UNUSED_VARIABLES();	assert(gRenDev);	// Ghosting requires data overframes, need to handle for each GPU in MGPU mode	SPostEffectsUtils::GetOrCreateRenderTarget("$DisplayTarget 1/2p", s_ptexDisplayTargetScaledPrev, width_r2, height_r2, Clr_Unknown, 1, 0, nLDRPFormat, -1, FT_DONT_RELEASE);	//	s_ptexWaterVolumeRefl[0]->DisableMgpuSync();	//	s_ptexWaterVolumeRefl[1]->DisableMgpuSync();	// TODO: Only create necessary RTs for minimal ring?	char str[256];	for (int i = 0, end = gcpRendD3D->GetActiveGPUCount() * MAX_FRAMES_IN_FLIGHT; i < end; i++)	{		sprintf(str, "$FlaresOcclusion_%d", i);		SPostEffectsUtils::GetOrCreateRenderTarget(str, s_ptexFlaresOcclusionRing[i], CFlareSoftOcclusionQuery::s_nIDColMax, CFlareSoftOcclusionQuery::s_nIDRowMax, Clr_Unknown, 1, 0, eTF_R8G8, -1, FT_DONT_RELEASE | FT_STAGE_READBACK);	}	SPostEffectsUtils::GetOrCreateRenderTarget("$FlaresGather", s_ptexFlaresGather, CFlareSoftOcclusionQuery::s_nGatherTextureWidth, CFlareSoftOcclusionQuery::s_nGatherTextureHeight, Clr_Unknown, 1, 0, eTF_R8, -1, FT_DONT_RELEASE);	/*	 *	The following textures do not need to be recreated on resize	 */	if (!CTexture::IsTextureExist(s_ptexWaterVolumeDDN))	{		SPostEffectsUtils::GetOrCreateRenderTarget("$WaterVolumeDDN", s_ptexWaterVolumeDDN, 64, 64, Clr_Unknown, 1, true, eTF_R16G16B16A16F, TO_WATERVOLUMEMAP);		//s_ptexWaterVolumeDDN->DisableMgpuSync();	}#endif	return 1;}void CRendererResources::DestroyPostFXMaps(){	SAFE_RELEASE_FORCE(s_ptexDisplayTargetScaledPrev);	SAFE_RELEASE(s_ptexWaterVolumeDDN);	for (int i = 0; i < MAX_OCCLUSION_READBACK_TEXTURES; i++)		SAFE_RELEASE_FORCE(s_ptexFlaresOcclusionRing[i]);	SAFE_RELEASE_FORCE(s_ptexFlaresGather);}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// The rules://// #renderResolution can be whole multiples of #outputResolution (super-sampling)// #outputResolution can be [1 to 2] factor of #displayResolution (dynamic downscale)//// See @CD3D9Renderer::CalculateResolutionsint CRendererResources::s_resourceWidth = 0, CRendererResources::s_resourceHeight = 0;int CRendererResources::s_renderWidth   = 0, CRendererResources::s_renderHeight   = 0;int CRendererResources::s_renderMinDim  = 0, CRendererResources::s_renderArea     = 0;int CRendererResources::s_outputWidth   = 0, CRendererResources::s_outputHeight   = 0;int CRendererResources::s_displayWidth  = 0, CRendererResources::s_displayHeight  = 0;void CRendererResources::OnRenderResolutionChanged(int renderWidth, int renderHeight){	if (s_renderWidth  != renderWidth ||		s_renderHeight != renderHeight)	{		if (gcpRendD3D->m_bSystemTargetsInit)			ResizeSystemTargets(renderWidth, renderHeight);		// Stereo resources are used as temporary targets for deferred shaded viewports		gcpRendD3D->GetS3DRend().OnResolutionChanged(renderWidth, renderHeight);		s_renderWidth  = renderWidth;		s_renderHeight = renderHeight;		s_renderMinDim = std::min(renderWidth, renderHeight);		s_renderArea   = renderWidth * renderHeight;	}}void CRendererResources::OnOutputResolutionChanged(int outputWidth, int outputHeight){	if (s_outputWidth  != outputWidth ||		s_outputHeight != outputHeight)	{		s_outputWidth  = outputWidth;		s_outputHeight = outputHeight;	}}void CRendererResources::OnDisplayResolutionChanged(int displayWidth, int displayHeight){	if (s_displayWidth  != displayWidth ||		s_displayHeight != displayHeight)	{		s_displayWidth  = displayWidth;		s_displayHeight = displayHeight;	}}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////void CRendererResources::ShutDown(){	UnloadDefaultSystemTextures(true);	if (s_ShaderTemplatesInitialized)	{		for (int i = 0; i < EFTT_MAX; i++)		{			s_ShaderTemplates[i].~CTexture();		}	}	s_ShaderTemplates.Free();	SAFE_DELETE(s_pTexNULL);}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////size_t CRendererResources::m_RTallocs = 0;size_t CRendererResources::m_DTallocs = 0;CTexture* CRendererResources::CreateDepthTarget(int nWidth, int nHeight, const ColorF& cClear, ETEX_Format eTF){	char pName[128]; // Create unique names for every allocation, otherwise name-matches would occur in GetOrCreateDepthStencil()	cry_sprintf(pName, "$DynDepthStencil_%8_x%d", m_DTallocs + 1, m_DTallocs + 2);	++m_DTallocs;	return CTexture::GetOrCreateDepthStencil(pName, nWidth, nHeight, cClear, eTT_2D, FT_USAGE_TEMPORARY | FT_NOMIPS, eTF == eTF_Unknown ? GetDepthFormat() : eTF);}CTexture* CRendererResources::CreateRenderTarget(int nWidth, int nHeight, const ColorF& cClear, ETEX_Format eTF){	char pName[128]; // Create unique names for every allocation, otherwise name-matches would occur in GetOrCreateRenderTarget()	cry_sprintf(pName, "$DynRenderTarget_%8_x%d", m_RTallocs + 1, m_RTallocs + 2);	++m_RTallocs;	return CTexture::GetOrCreateRenderTarget(pName, nWidth, nHeight, cClear, eTT_2D, FT_USAGE_TEMPORARY | FT_NOMIPS, eTF);}////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////Ang3 sDeltAngles(Ang3& Ang0, Ang3& Ang1){	Ang3 out;	for (int i = 0; i < 3; i++)	{		float a0 = Ang0[i];		a0 = (float)((360.0 / 65536) * ((int)(a0 * (65536 / 360.0)) & 65535)); // angmod		float a1 = Ang1[i];		a1 = (float)((360.0 / 65536) * ((int)(a1 * (65536 / 360.0)) & 65535));		out[i] = a0 - a1;	}	return out;}SEnvTexture* CRendererResources::FindSuitableEnvTex(Vec3& Pos, Ang3& Angs, bool bMustExist, int RendFlags, bool bUseExistingREs, CShader* pSH, CShaderResources* pRes, CRenderObject* pObj, bool bReflect, CRenderElement* pRE, bool* bMustUpdate, const SRenderingPassInfo* pPassInfo){	float time0 = iTimer->GetAsyncCurTime();	int i;	float distO = 999999;	float adist = 999999;	int firstForUse = -1;	int firstFree = -1;	Vec3 objPos;	if (bMustUpdate)		*bMustUpdate = false;	if (!pObj)		bReflect = false;	else	{		if (bReflect)		{			Plane pl;			pRE->mfGetPlane(pl);			objPos = pl.MirrorPosition(Vec3(0, 0, 0));		}		else if (pRE)			pRE->mfCenter(objPos, pObj, *pPassInfo);		else		{			CRY_ASSERT(!pPassInfo);			CRY_ASSERT(!gcpRendD3D->m_pRT->IsRenderThread());			objPos = pObj->GetMatrix().GetTranslation();		}	}	float dist = 999999;	for (i = 0; i < MAX_ENVTEXTURES; i++)	{		SEnvTexture* cur = &s_EnvTexts[i];		if (cur->m_bReflected != bReflect)			continue;		float s = (cur->m_CamPos - Pos).GetLengthSquared();		Ang3 angDelta = sDeltAngles(Angs, cur->m_Angle);		float a = angDelta.x * angDelta.x + angDelta.y * angDelta.y + angDelta.z * angDelta.z;		float so = 0;		if (bReflect)			so = (cur->m_ObjPos - objPos).GetLengthSquared();		if (s <= dist && a <= adist && so <= distO)		{			dist = s;			adist = a;			distO = so;			firstForUse = i;			if (!so && !s && !a)				break;		}		if (cur->m_pTex && !cur->m_pTex->m_pTexture && firstFree < 0)			firstFree = i;	}	if (bMustExist && firstForUse >= 0)		return &s_EnvTexts[firstForUse];	if (bReflect)		dist = distO;	float curTime = iTimer->GetCurrTime();	int nUpdate = -2;	float fTimeInterval = dist * CRenderer::CV_r_envtexupdateinterval + CRenderer::CV_r_envtexupdateinterval * 0.5f;	float fDelta = curTime - s_EnvTexts[firstForUse].m_TimeLastUpdated;	if (bMustExist)		nUpdate = -2;	else if (dist > MAX_ENVTEXSCANDIST)	{		if (firstFree >= 0)			nUpdate = firstFree;		else			nUpdate = -1;	}	else if (fDelta > fTimeInterval)		nUpdate = firstForUse;	if (nUpdate == -2 && firstForUse >= 0)	{		// No need to update (Up to date)		return &s_EnvTexts[firstForUse];	}	if (nUpdate >= 0 && !s_EnvTexts[nUpdate].m_pTex)		return NULL;	if (nUpdate >= 0)	{		if (!s_EnvTexts[nUpdate].m_pTex->m_pTexture || SRenderStatistics::Write().m_fEnvTextUpdateTime < 0.1f)		{			int n = nUpdate;			s_EnvTexts[n].m_TimeLastUpdated = curTime;			s_EnvTexts[n].m_CamPos = Pos;			s_EnvTexts[n].m_Angle = Angs;			s_EnvTexts[n].m_ObjPos = objPos;			s_EnvTexts[n].m_bReflected = bReflect;			if (bMustUpdate)				*bMustUpdate = true;		}		SRenderStatistics::Write().m_fEnvTextUpdateTime += iTimer->GetAsyncCurTime() - time0;		return &s_EnvTexts[nUpdate];	}	dist = 0;	firstForUse = -1;	for (i = 0; i < MAX_ENVTEXTURES; i++)	{		SEnvTexture* cur = &s_EnvTexts[i];		if (dist < curTime - cur->m_TimeLastUpdated && !cur->m_bInprogress)		{			dist = curTime - cur->m_TimeLastUpdated;			firstForUse = i;		}	}	if (firstForUse < 0)	{		return NULL;	}	int n = firstForUse;	s_EnvTexts[n].m_TimeLastUpdated = curTime;	s_EnvTexts[n].m_CamPos = Pos;	s_EnvTexts[n].m_ObjPos = objPos;	s_EnvTexts[n].m_Angle = Angs;	s_EnvTexts[n].m_bReflected = bReflect;	if (bMustUpdate)		*bMustUpdate = true;	SRenderStatistics::Write().m_fEnvTextUpdateTime += iTimer->GetAsyncCurTime() - time0;	return &s_EnvTexts[n];}