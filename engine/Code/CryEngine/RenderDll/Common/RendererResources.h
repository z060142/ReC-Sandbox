// Copyright 2017-2021 Crytek GmbH / Crytek Group. All rights reserved.#pragma once#include <CryRenderer/IRenderer.h>#include <CryRenderer/ITexture.h>//===================================================================#define MAX_ENVTEXTURES    16#define MAX_ENVTEXSCANDIST 0.1fclass CTexture;struct SEnvTexture;class CCVarUpdateRecorder;// Custom Textures IDsenum{	TO_RT_2D = 1,	TO_FOG,	TO_FROMOBJ,	TO_WINDGRID,	TO_SVOTREE,	TO_SVOTRIS,	TO_SVOGLCM,	TO_SVORGBS,	TO_SVONORM,	TO_SVOOPAC,	TO_FROMOBJ_CM,	TO_SHADOWID0,	TO_SHADOWID1,	TO_SHADOWID2,	TO_SHADOWID3,	TO_SHADOWID4,	TO_SHADOWID5,	TO_SHADOWID6,	TO_SHADOWID7,	TO_FROMRE0,	TO_FROMRE1,	TO_FROMRE0_FROM_CONTAINER,	TO_FROMRE1_FROM_CONTAINER,	TO_SCREENMAP,	TO_SHADOWMASK,	TO_TERRAIN_LM,	TO_RT_CM,	TO_CLOUDS_LM,	TO_BACKBUFFERMAP,	TO_MIPCOLORS_DIFFUSE,	TO_MIPCOLORS_BUMP,	TO_DOWNSCALED_ZTARGET_FOR_AO,	TO_QUARTER_ZTARGET_FOR_AO,	TO_WATEROCEANMAP,	TO_WATERVOLUMEMAP,	TO_WATERVOLUMEREFLMAP,	TO_WATERVOLUMEREFLMAPPREV,	TO_WATERVOLUMECAUSTICSMAP,	TO_WATERVOLUMECAUSTICSMAPTEMP,	TO_COLORCHART,	TO_ZTARGET_MS,	TO_SCENE_NORMALMAP,	TO_SCENE_NORMALMAP_MS,	TO_SCENE_DIFFUSE_ACC,	TO_SCENE_SPECULAR_ACC,	TO_SCENE_DIFFUSE_ACC_MS,	TO_SCENE_SPECULAR_ACC_MS,	TO_SCENE_TEXTURES,	TO_SCENE_TARGET,	TO_BACKBUFFERSCALED_D2,	TO_BACKBUFFERSCALED_D4,	TO_BACKBUFFERSCALED_D8,	TO_SKYDOME_MIE,	TO_SKYDOME_RAYLEIGH,	TO_SKYDOME_MOON,	TO_VOLFOGSHADOW_BUF,	TO_HDR_MEASURED_LUMINANCE,	TO_MODELHUD,};#define NUM_HDR_TONEMAP_TEXTURES 4#define NUM_HDR_BLOOM_TEXTURES   3#define MIN_DOF_COC_K            6#define MAX_OCCLUSION_READBACK_TEXTURES (MAX_GPU_NUM * MAX_FRAMES_IN_FLIGHT)//  render targets info - first gather list of hdr targets, sort by size and create afterstruct SRenderTargetInfo{	SRenderTargetInfo() : nWidth(0), nHeight(0), cClearColor(Clr_Empty), Format(eTF_Unknown), nFlags(0), lplpStorage(0), nPitch(0), fPriority(0.0f), nCustomID(0)	{}	uint32      nWidth;	uint32      nHeight;	ColorF      cClearColor;	ETEX_Format Format;	uint32      nFlags;	CTexture**  lplpStorage;	char        szName[64];	uint32      nPitch;	float       fPriority;	int32       nCustomID;};struct RenderTargetSizeSort{	bool operator()(const SRenderTargetInfo& drtStart, const SRenderTargetInfo& drtEnd) { return (drtStart.nPitch * drtStart.fPriority) > (drtEnd.nPitch * drtEnd.fPriority); }};class SRenderTargetPool{public:	void AddRenderTarget(uint32 nWidth, uint32 nHeight, const ColorF& cClear, ETEX_Format Format, float fPriority, const char* szName, CTexture** pStorage, uint32 nFlags = 0, int32 nCustomID = -1, bool bDynamicTex = 0);	bool CreateRenderTargetList();	void ClearRenderTargetList()	{		m_pRenderTargets.clear();	}	static SRenderTargetPool* GetInstance()	{		static SRenderTargetPool pInstance;		return &pInstance;	}private:	std::vector<SRenderTargetInfo> m_pRenderTargets;};class CRendererResources{public:	void InitResources() {}public:	static size_t m_RTallocs, m_DTallocs;	static TMP_RENDER_API CTexture* CreateDepthTarget(int nWidth, int nHeight, const ColorF& cClear, ETEX_Format eTF);	static TMP_RENDER_API CTexture* CreateRenderTarget(int nWidth, int nHeight, const ColorF& cClear, ETEX_Format eTF);	static ETEX_Format GetHDRFormat(bool withAlpha, bool lowQuality);	static ETEX_Format GetLDRFormat(bool precisionBits);	static ETEX_Format GetDisplayFormat();	static ETEX_Format GetDepthFormat();	//! The scene-referred path is taken this frame: the Cinematic Camera asked for it on the	//! post-effect parameter bus AND the r_SceneReferred kill switch honours the request AND the	//! render currently in flight is one that owns a display transform (the level viewport / game	//! view, not a thumbnail, a material preview or a terrain minimap - see	//! research/s8-editor-pipelines.md and SetSceneReferredPipelineGate below).	//! Everything gated by SceneReferredSpec.md keys on this and on nothing else, so with the	//! component off or the cvar at 0 the renderer is byte-identical to stock.	static bool        IsSceneReferred();	//! IsSceneReferred() WITHOUT the pipeline gate: "the camera asked and the cvar agrees",	//! regardless of which render is in flight. Reserved for the handful of decisions that are	//! properties of the WORLD rather than of a picture - the light-unit convention and its	//! siblings - which have exactly one value per frame for every viewport at once. Everything	//! else must use IsSceneReferred(); if in doubt, that is the one you want.	static bool        IsSceneReferredRequested();	static bool        IsSceneReferredRequestedStage(int nStage);	//! Open or close the pipeline gate for the render view about to be drawn. Called once per	//! render view from CD3D9Renderer::LatchSceneReferredExposure(), on the render thread, before	//! any stage runs - so every consumer downstream sees one decision for the whole view.	//! Logs the decision once per pipeline whenever it changes, naming the pipeline.	static void        SetSceneReferredPipelineGate(bool bOpen, const char* szPipelineName, const char* szReason);	//! The gate's current state and the pipeline that set it, for r_HDRDebug 1.	static bool        IsSceneReferredPipelineGateOpen();	static const char* GetSceneReferredPipelineName();	//! IsSceneReferred() narrowed to the bisection gate r_SceneReferredStage: true only while the	//! camera asks for the scene-referred path AND the cvar honours stage items up to nStage.	//! 0 = the switch alone, 1 = + S0 formats, 2 = + S1 exposure/AUTO, 3 = + S2 light units and	//! sun shafts, 4 = + S3 working space, 5 = + S4/S5/S7 display transform and grade,	//! 6 = + S6 export. The default (99) makes every one of these identical to IsSceneReferred().	static bool        IsSceneReferredStage(int nStage);	//! True while the 3D engine is emitting radiometric light values - i.e. while the sun is NOT	//! pre-divided by PI and the 1/PI belongs in the BRDF (SceneReferredSpec.md D4). Read from	//! E3DPARAM_SCENE_REFERRED, the engine-side mirror of the switch, and NOT from the post-effect	//! bus: this is the authority that produced the sun colour the callers are about to correct,	//! and the two must never disagree. Use IsSceneReferred() for everything else.	static bool        IsSceneReferredLightUnits();	//! True while the HDR target actually holds ACEScg (AP1) rather than Rec.709 - i.e. the	//! scene-referred path is running AND r_SceneReferredDebug is not skipping the working-space	//! conversion (SceneReferredSpec.md S3). Anything whose correctness depends on the CONTENTS of	//! the HDR target - the luminance weights above all - must ask this and not IsSceneReferred(),	//! or the debug A/B would leave Rec.709 data being measured with AP1 weights.	static bool        IsSceneReferredWorkingSpace();	//! A Rec.709-authored colour, expressed in whatever primaries the HDR target is currently in.	//! Identity off the working space, and identity for any neutral colour on it. Call it at the	//! constant upload, on the value the artist chose - not on the stored parameter, which stays	//! Rec.709 because that is what the tooltip, the preset file and the picker all mean.	static Vec3        ToWorkingSpace(const Vec3& rec709);	//! Mid grey in the working space. After the pre-exposure a correctly exposed 18 % reflector	//! lands here by construction (SceneReferredSpec.md D6), which is what makes "stops above mid	//! grey" a meaning the renderer can act on rather than a figure of speech.	static const float kSceneReferredMidGrey;	//! The luminance weights of the space the HDR target holds this frame: Rec.709 before the	//! working-space conversion, the AP1 Y row after it. The per-view constant buffer publishes	//! this to the pixel shaders (as a delta from 709); the metering histogram's compute pass,	//! which has no per-view binding, reads it from here.	static Vec3        GetWorkingLuminanceWeights();	//! The exposure meter's saturation point, in stops above mid grey, and the pre-exposed	//! luminance that works out to. A real incident meter's dome saturates; so does this one, and	//! for the same reason - past about fourteen stops over the exposure anchor a texel is a	//! specular or solar highlight whose exact value says nothing about how much light is falling	//! on the scene. It is also what keeps the meter's own fp16 arithmetic finite: 0.18*2^14 is	//! 2949 and the illuminance estimate derived from it is 51473, both comfortably inside the	//! 65504 that R16G16F and the pre-exposed HDR target can hold.	//! Only ever consulted on the scene-referred path; 0 is published off it.	static const float kSceneReferredMeterCeilingStops;	static float       GetSceneReferredMeterCeiling();	//! The ceiling every scene-referred writer is held to and the 709->AP1 conversion enforces,	//! in exposed engine units (r_SceneReferredWriterClamp, default 2^15). A FORMAT bound, not a	//! look: the HDR target is fp16 and an Inf in it poisons every average downstream.	//! 0 off the switch, and 0 when the cvar is set to 0, which is what the shaders read as	//! "no clamp" - the branches fold away and the stock path is byte-identical.	static float       GetSceneReferredWriterClamp();	//! The sky pass's ceiling for a given pre-exposure, in EXPOSED engine units. The physical	//! solar disc (r_SceneReferredSkyPeakLuminance, cd/m2) converted to engine units and	//! pre-exposed, then held under the writer clamp so the fp16 format can never be overrun.	//! Unlike the bare writer clamp this means the same real-world luminance at every aperture.	//! 0 off the switch, and the plain writer clamp when the peak cvar is 0.	static float       GetSceneReferredSkyPeakClamp(float sceneExposure);	//! A highlight-extraction threshold, in whatever unit is in force this frame	//! (SceneReferredSpec.md D11, Stage S7).	//!	//! OFF the scene-referred switch this is the identity: the parameter is an absolute value in	//! engine light units, exactly as every one of these effects has always read it, and not one	//! bit of the stock path moves.	//!	//! ON it the same stored number is read as STOPS ABOVE MID GREY. That is the physical unit -	//! how far above a correctly exposed grey card a highlight has to be before the glass, the	//! emulsion or the sensor does something visible with it - and it is the unit that makes the	//! setting survive a change of exposure, which an absolute number under a moving pre-exposure	//! cannot. It is also what a photographer already thinks in.	//!	//! The clamp is on the STOPS, so a slider whose range was chosen for absolute values (ghosts	//! and the front filter both go to 64) cannot ask for 2^64 and produce a constant that means	//! nothing. 16 stops above mid grey is already "never" for any real frame.	static float       ResolveThreshold(float stored);public:	static bool m_bLoadedSystem;	// Global renderer resources	static void CreateHDRMaps(int resourceWidth, int resourceHeight);	static bool CreatePostFXMaps(int resourceWidth, int resourceHeight);	static void CreateSystemTargets(int resourceWidth, int resourceHeight);	static void ResizeSystemTargets(int renderWidth, int renderHeight);	static void DestroyHDRMaps();	static void DestroyPostFXMaps();	static void DestroySystemTargets();	static void LoadDefaultSystemTextures();	static void UnloadDefaultSystemTextures(bool bFinalRelease = false);	static void ShutDown();	static void OnRenderResolutionChanged(int renderWidth, int renderHeight);	static void OnOutputResolutionChanged(int outputWidth, int outputHeight);	static void OnDisplayResolutionChanged(int displayWidth, int displayHeight);	static int         s_resourceWidth, s_resourceHeight;	static int         s_renderWidth, s_renderHeight, s_renderMinDim, s_renderArea;	static int         s_outputWidth, s_outputHeight;	static int         s_displayWidth, s_displayHeight;	static ETEX_Format s_eTFZ;	// ==============================================================================	// Capabilities	static SPixFormatSupport s_hwTexFormatSupport;	// ==============================================================================	// Memory/Texture pools	static SEnvTexture* FindSuitableEnvTex(Vec3& Pos, Ang3& Angs, bool bMustExist, int RendFlags, bool bUseExistingREs, CShader* pSH, CShaderResources* pRes, CRenderObject* pObj, bool bReflect, CRenderElement* pRE, bool* bMustUpdate, const SRenderingPassInfo* pPassInfo);	// ==============================================================================	static CTexture* s_ptexDisplayTargetScaledPrev;           // CSonarVision, ?, 2x	static CTexture* s_ptexMipColors_Diffuse;                 // ?	static CTexture* s_ptexMipColors_Bump;                    // ?	static CTexture* s_ptexFromRE[8];                         // ?	static CTexture* s_ptexShadowID[8];                       // ?	static CTexture* s_ptexRT_2D;                             // CREWaterOcean, ?	static CTexture* s_ptexFromRE_FromContainer[2];           // ?	static CTexture* s_ptexFromObj;                           // ?	static CTexture* s_ptexHDRMeasuredLuminanceDummy;         // ?	static CTexture* s_ptexCloudsLM;                          // ?	static CTexture* s_ptexColorChart;                        // ?	static CTexture* s_pTexNULL;	// From file loaded textures ===============================================================================	TMP_RENDER_API static CTexture* s_ptexNoTexture;	static CTexture* s_ptexNoTextureCM;	static CTexture* s_ptexWhite;	static CTexture* s_ptexWhite3D;	static CTexture* s_ptexGray;	static CTexture* s_ptexMedian;	static CTexture* s_ptexBlack;	static CTexture* s_ptexBlackAlpha;	static CTexture* s_ptexBlackCM;	static CTexture* s_ptexDefaultProbeCM;	static CTexture* s_ptexDefaultMergedDetail;	static CTexture* s_ptexFlatBump;#if !defined(_RELEASE)	static CTexture* s_ptexMipMapDebug;	static CTexture* s_ptexColorBlue;	static CTexture* s_ptexColorCyan;	static CTexture* s_ptexColorGreen;	static CTexture* s_ptexColorPurple;	static CTexture* s_ptexColorRed;	static CTexture* s_ptexColorWhite;	static CTexture* s_ptexColorYellow;	static CTexture* s_ptexColorMagenta;	static CTexture* s_ptexColorOrange;#endif	static CTexture* s_ptexPaletteDebug;	static CTexture* s_ptexPaletteTexelsPerMeter;	static CTexture* s_ptexIconShaderCompiling;	static CTexture* s_ptexIconStreaming;	static CTexture* s_ptexIconStreamingTerrainTexture;	static CTexture* s_ptexIconNavigationProcessing;	static CTexture* s_ptexShadowJitterMap;	static CTexture* s_ptexEnvironmentBRDF;	static CTexture* s_ptexScreenNoiseMap;	static CTexture* s_ptexDissolveNoiseMap;	static CTexture* s_ptexNoise3D;	static CTexture* s_ptexGrainFilterMap;	static CTexture* s_ptexFilmGrainMap;	static CTexture* s_ptexVignettingMap; // todo: create at runtime based on viggneting cvars	static CTexture* s_ptexAOJitter;	static CTexture* s_ptexAOVOJitter;	static CTexture* s_ptexNormalsFitting;	static CTexture* s_ptexPerlinNoiseMap;	static CTexture* s_ptexLTC1;	static CTexture* s_ptexLTC2;	// Static resolution [independent] targets =================================================================	static CTexture* s_ptexWindGrid;                                             // CSceneGBufferStage, CSceneForwardStage, CSceneCustomStage	static CTexture* s_ptexHDRAdaptedLuminanceCur[8];                            // CAutoExposureStage	static int       s_nCurLumTextureIndex;                                      // CAutoExposureStage	static CTexture* s_ptexCurLumTexture;                                        // CAutoExposureStage, CToneMappingStage, CPostAAStage	static CTexture* s_ptexHDRToneMaps[NUM_HDR_TONEMAP_TEXTURES];                // CAutoExposureStage, CBloomStage	static CTexture* s_ptexFarPlane;                                             // shadow map representing the far plane (all tests pass)	static CTexture* s_ptexWaterOcean;                                           // CWaterStage, CREWaterOcean, water ocean vertex texture	static CTexture* s_ptexWaterVolumeTemp[2];                                   // CWaterStage, water volume heightmap	static CTexture* s_ptexWaterVolumeDDN;                                       // CWaterStage, water volume heightmap	static CTexture* s_ptexFlaresOcclusionRing[MAX_OCCLUSION_READBACK_TEXTURES]; // CFlareSoftOcclusionQuery	static CTexture* s_ptexFlaresGather;                                         // CFlareSoftOcclusionQuery	static CTexture* s_ptexVolumetricFog;                                        // CVolumetricFogStage	// Pools ===================================================================================================	static SEnvTexture         s_EnvTexts[MAX_ENVTEXTURES];                      // FX_DrawToRenderTarget, SHRenderTarget::GetEnv2D	static TArray<SEnvTexture> s_CustomRT_2D;                                    // FX_DrawToRenderTarget, SHRenderTarget::GetEnv2D, CREWaterOcean	static TArray<CTexture>    s_ShaderTemplates;	static bool                s_ShaderTemplatesInitialized;};