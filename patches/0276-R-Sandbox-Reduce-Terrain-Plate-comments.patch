From 22a04d61ecd14a86619dc9459758664ec5177ca9 Mon Sep 17 00:00:00 2001
From: z060142 <z060142@yahoo.com.tw>
Date: Wed, 2 Sep 2026 10:27:01 +0800
Subject: [PATCH 276/307] !R (Sandbox) Reduce Terrain Plate comments.

---
 Code/Sandbox/EditorQt/GameEngine.cpp          |   7 +-
 Code/Sandbox/EditorQt/Terrain/Heightmap.cpp   |  13 +-
 .../EditorQt/Terrain/TerrainCommands.cpp      | 197 +++++++-----------
 .../Terrain/TerrainPlateColourOverlay.cpp     |  93 ++++-----
 .../Terrain/TerrainPlateColourOverlay.h       |  70 ++-----
 .../EditorQt/TerrainTexturePainter.cpp        |   7 +-
 Code/Sandbox/EditorQt/TerrainTexturePainter.h |  14 +-
 .../EditorQt/Vegetation/VegetationMap.cpp     |  16 +-
 .../EditorQt/Vegetation/VegetationMap.h       |   7 +-
 9 files changed, 158 insertions(+), 266 deletions(-)

diff --git a/Code/Sandbox/EditorQt/GameEngine.cpp b/Code/Sandbox/EditorQt/GameEngine.cpp
index d6a4f0ae..e225113f 100644
--- a/Code/Sandbox/EditorQt/GameEngine.cpp
+++ b/Code/Sandbox/EditorQt/GameEngine.cpp
@@ -1542,10 +1542,9 @@ void CGameEngine::Update()
 		if (bUpdateAIPhysics)
 			updateFlags |= ESYSUPDATE_EDITOR_AI_PHYSICS;
 
-		// Decision 08: the live terrain colour overlay of the terrain plates. It only marks terrain
-		// texture sectors dirty; the paint itself happens inside the engine's own pull, later in this
-		// Update, through CHeightmap::GetColorAtPosition. Editor branch only - the overlay is an editor
-		// preview by nature (the game loads terraintexture.pak). @see Terrain/TerrainPlateColourOverlay.h.
+		// Only marks terrain texture sectors dirty; the paint itself happens later in this Update, inside
+		// the engine's own pull through CHeightmap::GetColorAtPosition. Editor only by nature - the game
+		// loads terraintexture.pak.
 		TerrainPlateColourOverlay::Update();
 
 		GetIEditorImpl()->GetSystem()->Update(updateFlags);
diff --git a/Code/Sandbox/EditorQt/Terrain/Heightmap.cpp b/Code/Sandbox/EditorQt/Terrain/Heightmap.cpp
index a53dfcf0..c2b6d81b 100644
--- a/Code/Sandbox/EditorQt/Terrain/Heightmap.cpp
+++ b/Code/Sandbox/EditorQt/Terrain/Heightmap.cpp
@@ -2868,14 +2868,11 @@ ColorB CHeightmap::GetColorAtPosition(const float x, const float y, ColorB* colo
 			}
 		}
 
-		// Decision 08, the live terrain colour of an integrated terrain plate. This is the ONLY caller of
-		// this function: CTerrainNode::UpdateNodeTextureFromEditorData builds one terrain colour tile row
-		// by row out of it and DXT compresses the result into the atlas slot (terrain_node.cpp:528-583).
-		// So compositing here puts the plate's colour into what the engine renders and leaves the level's
-		// own colour alone - m_terrainRGBTexture is read above and never written. That matters: CRGBLayer
-		// flushes every dirty tile to disk on every level save (RGBLayer.cpp:1088), so a "virtual" write
-		// there would become permanent at the next Ctrl+S. Untick, move or delete the plate and the same
-		// pull repaints this tile from the untouched source. @see TerrainPlateColourOverlay.h.
+		// The live terrain colour of an integrated terrain plate. Compositing HERE - the only caller is
+		// CTerrainNode::UpdateNodeTextureFromEditorData, which builds one colour tile out of these rows -
+		// keeps the plate's colour out of m_terrainRGBTexture, which is only read above. That matters:
+		// CRGBLayer flushes every dirty tile to disk on level save, so writing there would make the
+		// overlay permanent at the next Ctrl+S.
 		TerrainPlateColourOverlay::ApplyToRow((uint32*)colors, colorsNum,
 		                                      CLAMP(x, 0, fLevelSize - 1.f), xStep,
 		                                      CLAMP(y, 0, fLevelSize - 1.f), texelSize * fLevelSize);
diff --git a/Code/Sandbox/EditorQt/Terrain/TerrainCommands.cpp b/Code/Sandbox/EditorQt/Terrain/TerrainCommands.cpp
index adf4f71a..b88cf58e 100644
--- a/Code/Sandbox/EditorQt/Terrain/TerrainCommands.cpp
+++ b/Code/Sandbox/EditorQt/Terrain/TerrainCommands.cpp
@@ -15,10 +15,8 @@
 #include "TerrainTexturePainter.h"
 #include "IEditorImpl.h"
 
-// Commit Bake (decision 07 step 1). EditorQt already has the plugin's Module directory on its include
-// path (CMakeLists.txt:1508-1513) and this is a header only downcast onto a pure virtual interface, so
-// nothing here needs a symbol out of CryDefaultEntities. Same shape as
-// Plugins/EditorParticle/EntityObjectWithParticleComponent.cpp:6 and LensFlareUtil.cpp:10.
+// Header only downcast onto a pure virtual interface, so nothing here needs a symbol out of
+// CryDefaultEntities.
 #include <DefaultComponents/Geometry/TerrainPlateComponent.h>
 
 #include <BoostPythonMacros.h>
@@ -242,10 +240,9 @@ static void PyRefineTerrainTiles()
 
 
 //! Brings painted vegetation instances (and merged-mesh grass) back onto the current ENGINE terrain.
-//! This is the opt-in repair for any engine side terrain change that the editor CHeightmap never saw,
-//! a terrain plate bake in particular: instance Z is sampled once at paint time and stored absolutely,
-//! and the only stock re-sampler (CVegetationMap::OnHeightMapChanged) runs at mission load only and
-//! skips auto-merged instances.
+//! Instance Z is sampled once at paint time and stored absolutely, and the stock re-sampler
+//! (CVegetationMap::OnHeightMapChanged) runs at mission load only and skips auto-merged instances, so
+//! an engine side change such as a terrain plate bake needs this opt-in repair.
 static void PyRepositionVegetation()
 {
 	CVegetationMap* pVegetationMap = GetIEditorImpl()->GetVegetationMap();
@@ -276,7 +273,7 @@ static void PyRepositionVegetation()
 	int moved = 0;
 	int skipped = 0;
 	{
-		// One undo step for the whole run: Ctrl+Z puts every moved instance back where it was.
+		// One undo step for the whole run.
 		CUndo undo("Reposition Vegetation on Terrain");
 		pVegetationMap->RepositionInstancesOnTerrain(box, moved, skipped);
 	}
@@ -292,9 +289,8 @@ static void PyRepositionVegetation()
 	}
 }
 
-//! The terrain plates in the current selection, as the pair the commit commands both want: the entity
-//! (for its name in the log) and the component, reached through the pure virtual ITerrainPlateCommit.
-//! Empty and already warned about when there is nothing to work on.
+//! The terrain plates in the current selection, entity (for the log) plus component. Empty and already
+//! warned about when there is nothing to work on.
 static std::vector<std::pair<IEntity*, Cry::DefaultComponents::CTerrainPlateComponent*>>
 CollectSelectedTerrainPlates(const char* szCommandName)
 {
@@ -336,16 +332,11 @@ CollectSelectedTerrainPlates(const char* szCommandName)
 	return plates;
 }
 
-//! Commit Bake, step 1: stamps the LIVE bake of every selected terrain plate into the level's own
-//! heightmap, so that the relief stops being a plate's runtime footprint and becomes ordinary terrain -
-//! saved in Heightmap.dat, pushed back into the engine on every load (CryEditDoc.cpp:122) and therefore
-//! there after the plate is deleted. Decision 07 step 1; the traced facts behind every line below are in
-//! research/12-commit-bake.md.
-//!
-//! The block is read straight back out of the live engine terrain rather than recomputed, which is what
-//! makes it the SAME block the bake produced: every batch, cascade, priority and rim shape that went into
-//! the live footprint is already in the ground. The loss is the engine's 12 bit per sector quantisation,
-//! a few mm, against the heightmap's own 1.6 cm save quantisation (research 12 section 0.4).
+//! Stamps the LIVE bake of every selected terrain plate into the level's own heightmap, so the relief
+//! becomes ordinary terrain and survives the plate being deleted.
+//! The block is read straight back out of the live engine terrain rather than recomputed, so it is the
+//! SAME block the bake produced - every batch, cascade, priority and rim shape is already in the ground.
+//! The loss is the engine's 12 bit per sector quantisation, a few mm, against the heightmap's own 1.6 cm.
 static void PyCommitTerrainPlate()
 {
 	using Cry::DefaultComponents::CTerrainPlateComponent;
@@ -359,19 +350,16 @@ static void PyCommitTerrainPlate()
 		return;
 	}
 
-	// The plates first, so that a refusal below can be reported before a single height is written.
+	// The plates first, so a refusal can be reported before a single height is written.
 	const std::vector<std::pair<IEntity*, CTerrainPlateComponent*>> plates = CollectSelectedTerrainPlates("terrain.commit_plate");
 	if (plates.empty())
 		return;
 
 	const float unitSize = pHeightmap->GetUnitSize();
 	// The heightmap is stored transposed against the world: heightmap X is the world Y unit and heightmap
-	// Y is the world X unit, with the linear index x + y * width (Heightmap.h:82,
-	// CHeightmap::WorldToHmap/HmapToWorld, Heightmap.cpp:557-566, and the same swap in every consumer -
-	// the sculpt brush at TerrainBrushTool.cpp:461-462, the texture painter at
-	// TerrainTexturePainter.cpp:691-692). The plate's own rect is in ENGINE units, which are not swapped
-	// (SetTerrainElevation reads pTerrainBlock[worldXunit * stride + worldYunit], terran_edit.cpp:266), so
-	// the mapping here is  hmapX = worldYunit,  hmapY = worldXunit.
+	// Y is the world X unit, linear index x + y * width (CHeightmap::WorldToHmap, and the same swap in
+	// every consumer). The plate's rect is in ENGINE units, which are not swapped, so the mapping is
+	// hmapX = worldYunit, hmapY = worldXunit.
 	const int hmapUnitsY = (int)pHeightmap->GetWidth();    // heightmap X axis: world Y
 	const int hmapUnitsX = (int)pHeightmap->GetHeight();   // heightmap Y axis: world X
 	if (unitSize <= 0.f || hmapUnitsX <= 0 || hmapUnitsY <= 0)
@@ -383,9 +371,8 @@ static void PyCommitTerrainPlate()
 	const CTimeValue startTime = gEnv->pTimer->GetAsyncTime();
 	int committed = 0;
 
-	// One undo group for the whole run: Ctrl+Z puts the terrain of every committed plate back at once.
-	// The undo does NOT reach the components' Committed flag - it is runtime state, not a terrain edit -
-	// so an undo leaves disarmed plates over restored ground; the log line says how to re-arm them.
+	// One undo group for the whole run. It does NOT reach the components' Committed flag (runtime state,
+	// not a terrain edit), so an undo leaves disarmed plates over restored ground.
 	CUndo undo("Commit Terrain Plate");
 
 	for (const std::pair<IEntity*, CTerrainPlateComponent*>& entry : plates)
@@ -402,8 +389,8 @@ static void PyCommitTerrainPlate()
 			continue;
 		}
 
-		// The engine block covers units rectX1 .. rectX1 + rectSize INCLUSIVE (terran_edit.cpp:264), so the
-		// square is rectSize + 1 cells on a side.
+		// The engine block covers units rectX1 .. rectX1 + rectSize INCLUSIVE, so the square is
+		// rectSize + 1 cells on a side.
 		const int dim = rectSize + 1;
 		const int hx0 = rectY1;   // heightmap X, world Y
 		const int hy0 = rectX1;   // heightmap Y, world X
@@ -424,9 +411,8 @@ static void PyCommitTerrainPlate()
 		{
 			for (int uy = 0; uy < cols; ++uy)  // along world Y
 			{
-				// GetTerrainZ is CTerrain::GetZ, which truncates to units and returns the stored node height
-				// with no interpolation; the half unit offset only defends against a float multiply landing
-				// just under the integer. Same sampler the plate's own baseline capture uses.
+				// GetTerrainZ truncates to units and does not interpolate; the half unit offset only defends
+				// against a float multiply landing just under the integer. Same sampler the baseline uses.
 				const float z = p3DEngine->GetTerrainZ(((float)(rectX1 + ux) + 0.5f) * unitSize,
 				                                       ((float)(rectY1 + uy) + 0.5f) * unitSize);
 				block[(size_t)ux * cols + uy] = z;
@@ -435,11 +421,9 @@ static void PyCommitTerrainPlate()
 			}
 		}
 
-		// THE showstopper check (research 12 section 0.5). CHeightmap::Serialize saves heights as
-		// uint16 scaled by 65535 / m_fMaxHeight and clamps to 0xFFFF (Heightmap.cpp:2682-2695), and a
-		// negative height wraps through the same clamp and comes back as the ceiling. Either way the level
-		// looks right until it is reloaded, which is the worst failure mode there is - so refuse, loudly,
-		// and write nothing.
+		// CHeightmap::Serialize saves heights as uint16 scaled by 65535 / m_fMaxHeight and clamps, and a
+		// negative height wraps through the same clamp to the ceiling. Either way the level looks right
+		// until it is reloaded, so refuse loudly and write nothing.
 		const float maxHeight = pHeightmap->GetMaxHeight();
 		if (blockMax > maxHeight || blockMin < 0.f)
 		{
@@ -453,11 +437,9 @@ static void PyCommitTerrainPlate()
 
 		pHeightmap->RecordUndo(hx0, hy0, cols, rows, /*bInfo*/ false);
 
-		// A cell the bake never moved reads back as the baseline plus at most one engine quantisation step,
-		// and the sector aligned square reaches well outside the plate. Writing those cells back would stamp
-		// that noise into a pristine heightmap for no gain, so a cell is only written when it moved by more
-		// than the readback is worth. The step is the coarsest one any touched sector can have
-		// (12 bits over the block's own span, terran_edit.cpp:281-286), with a floor for a flat block.
+		// The sector aligned square reaches well outside the plate, and an untouched cell still reads back
+		// one engine quantisation step off. Only write a cell that moved by more than that readback noise.
+		// The step is the coarsest any touched sector can have (12 bits over the block's span).
 		const float epsilon = max(0.0001f, (blockMax - blockMin) / 4095.f);
 
 		int cellsWritten = 0;
@@ -481,7 +463,7 @@ static void PyCommitTerrainPlate()
 		}
 
 		// UpdateEngineTerrain ignores its 4th argument and always pushes a SQUARE of side areaSize snapped
-		// out to whole sectors (Heightmap.cpp:2461-2472), so it gets the larger of the two sides.
+		// out to whole sectors, so it gets the larger of the two sides.
 		const int side = max(cols, rows);
 		pHeightmap->UpdateEngineTerrain(hx0, hy0, side, side, CHeightmap::ETerrainUpdateType::Elevation);
 		GetIEditorImpl()->GetTerrainManager()->SetModified(hx0, hy0, hx0 + cols, hy0 + rows);
@@ -490,9 +472,9 @@ static void PyCommitTerrainPlate()
 		              Vec3((float)(rectX1 + rows) * unitSize, (float)(rectY1 + cols) * unitSize, blockMax + 1.f));
 		GetIEditorImpl()->UpdateViews(eUpdateHeightmap, &worldBox);
 
-		// The disarm, INSIDE the undo group and only now that the heights are in: the plate adopts the
-		// committed ground as its baseline before Bake Into Terrain goes off, so the untick that follows
-		// cannot write the old ground back (research 12 section 2.6).
+		// The disarm, INSIDE the undo group and only now that the heights are in: the plate must adopt the
+		// committed ground as its baseline before Bake Into Terrain goes off, or the untick writes the old
+		// ground back.
 		pCommit->OnCommitted();
 		++committed;
 
@@ -514,12 +496,10 @@ static void PyCommitTerrainPlate()
 	       committed, (int)plates.size(), elapsedMs);
 }
 
-//! Blends one heightmap cell's surface type weights towards szLayer by weight, without copying the two
-//! bugs in the stock CImagePainter::LerpTerrainSurfaceType (ImagePainter.cpp:123 indexes we[3] on a three
-//! element array, and :202 seeds a 15 scale value into a 255 scale field). Same idea as the stock one:
-//! unroll both sides into a per layer histogram, keep the three heaviest, renormalise - because the
-//! engine cell can only carry three types (SSurfaceTypeItem::kMaxSurfaceTypesNum, and 4 bit weights and a
-//! 16 entry palette per sector below that, research 12 section 5.3).
+//! Blends one heightmap cell's surface type weights towards szLayer by weight. Same idea as
+//! CImagePainter::LerpTerrainSurfaceType - unroll into a per layer histogram, keep the three heaviest,
+//! renormalise, because a cell carries only SSurfaceTypeItem::kMaxSurfaceTypesNum types - but without its
+//! two bugs (an out of range we[3] index, and a 15 scale value seeded into a 255 scale field).
 static SSurfaceTypeItem BlendSurfaceTypeTowards(const SSurfaceTypeItem& src, uint32 layerId, float weight)
 {
 	float histogram[e_layerIdHole] = { 0.f };
@@ -559,9 +539,8 @@ static SSurfaceTypeItem BlendSurfaceTypeTowards(const SSurfaceTypeItem& src, uin
 		histogram[best] = 0.f;
 	}
 
-	// Renormalise the tail, then give the dominant slot whatever is left: we[0] is IMPLICIT in the engine
-	// encoding (terrain_sector.h:65-94 derives it as 15 - we[1] - we[2]), so it is the one that must
-	// absorb the rounding, not the ones that survive the trip.
+	// we[0] is IMPLICIT in the engine encoding (derived as 15 - we[1] - we[2]), so it is the slot that
+	// must absorb the rounding.
 	if (total > 0.f)
 	{
 		int tail = 0;
@@ -576,20 +555,16 @@ static SSurfaceTypeItem BlendSurfaceTypeTowards(const SSurfaceTypeItem& src, uin
 	return out;
 }
 
-//! Commit Bake, step 2: stamps the APPEARANCE of every selected terrain plate - terrain colour and the
-//! plate's Commit Layer surface weights - into the level's own data over the plate's footprint, fading
-//! into whatever was there over the SAME rim band the height bake uses.
+//! Stamps the APPEARANCE of every selected terrain plate - terrain colour and the plate's Commit Layer
+//! surface weights - into the level's own data over the plate's footprint, fading into what was there
+//! over the SAME rim band the height bake uses.
 //!
-//! This is a separate operation from terrain.commit_plate on purpose (user requirement, 2026-09-01): its
-//! own button, its own undo group, no height precondition and no effect at all on Bake Into Terrain or on
-//! the plate's baselines. It can be run before the height commit, after it, or instead of it - everything
-//! it needs (the footprint and the rim weight) comes from the plate's CURRENT transform and knobs, not
-//! from the live bake, so none of those orders changes what it writes.
+//! Deliberately independent of terrain.commit_plate: its own undo group, no height precondition and no
+//! effect on Bake Into Terrain or the baselines. Everything it needs comes from the plate's CURRENT
+//! transform and knobs, so it can run before, after or instead of the height commit.
 //!
-//! Where the colour goes: CRGBLayer, the level's own terrain colour store. Nothing regenerates it - the
-//! game export reads it straight (GameExporter.cpp:1068-1070) and CTerrainTexGen is preview only - so the
-//! stamp is permanent, ships, and is reversible only by this command's undo or by repainting. That is
-//! why the undo is the stock painter's own object rather than something invented here.
+//! The colour goes into CRGBLayer, the level's own store, which nothing regenerates - so the stamp is
+//! permanent, ships with the level, and is reversible only by this command's undo or by repainting.
 static void PyCommitTerrainPlateColour()
 {
 	using Cry::DefaultComponents::CTerrainPlateComponent;
@@ -618,8 +593,8 @@ static void PyCommitTerrainPlateColour()
 		return;
 	}
 
-	// Hazard 9 (research 12 section 7): with this on, the terrain colour is synthesised from the detail
-	// materials at paint time and a written RGB layer is ignored, so the stamp would appear to do nothing.
+	// With this on the terrain colour is synthesised from the detail materials and a written RGB layer is
+	// ignored, so the stamp would appear to do nothing.
 	ICVar* pAutoBaseTexture = (gEnv->pConsole != nullptr) ? gEnv->pConsole->GetCVar("e_TerrainAutoGenerateBaseTexture") : nullptr;
 	if (pAutoBaseTexture != nullptr && pAutoBaseTexture->GetIVal() != 0)
 	{
@@ -635,9 +610,8 @@ static void PyCommitTerrainPlateColour()
 	const CTimeValue startTime = gEnv->pTimer->GetAsyncTime();
 	int committed = 0;
 
-	// ONE undo group, and it covers this operation only. The height commit has its own; the two never
-	// share a step, so a Ctrl+Z after a colour commit puts the colour and the layer weights back and
-	// leaves the committed heights alone, and vice versa.
+	// ONE undo group, covering this operation only - the height commit has its own, so the two never
+	// share a step.
 	CUndo undo("Commit Terrain Plate Colour");
 
 	for (const std::pair<IEntity*, CTerrainPlateComponent*>& entry : plates)
@@ -662,9 +636,8 @@ static void PyCommitTerrainPlateColour()
 			continue;
 		}
 
-		// The footprint in world metres and in the RGB layer's normalised measure. Note the axes: the
-		// normalised RGB / heightmap coordinate fpx is the world Y and fpy is the world X (research 12
-		// sections 1.2 and 3.1), which is why every pair below reads (Y, X).
+		// The footprint in world metres and in the RGB layer's normalised measure. The normalised
+		// coordinate fpx is the world Y and fpy the world X, which is why every pair below reads (Y, X).
 		const float worldXMin = (float)rectX1 * unitSize;
 		const float worldYMin = (float)rectY1 * unitSize;
 		const float worldXMax = (float)(rectX1 + rectSize) * unitSize;
@@ -675,9 +648,8 @@ static void PyCommitTerrainPlateColour()
 		const float normXMax = clamp_tpl(worldXMax / terrainSize, 0.f, 1.f);
 		const float normYMax = clamp_tpl(worldYMax / terrainSize, 0.f, 1.f);
 
-		// The layer, if one was asked for, plus the palette pre-flight the painter does before every dab:
-		// a sector whose 16 entry surface type palette is full would swallow the write silently
-		// (research 12 section 5.3).
+		// The layer, plus the palette pre-flight the painter does before every dab: a sector whose 16 entry
+		// surface type palette is full would swallow the write silently.
 		CLayer* pLayer = nullptr;
 		uint32  layerId = 0;
 		if (appearance.szLayerName != nullptr)
@@ -710,16 +682,13 @@ static void PyCommitTerrainPlateColour()
 			}
 		}
 
-		// ONE undo object per plate, snapshotting the RGB tiles AND the layer id blocks the footprint
-		// touches, taken BEFORE anything is written. Same objects the terrain texture painter records for
-		// a dab, so undo and redo already know how to put both halves back and re-push the engine.
+		// One undo object per plate, snapshotting the RGB tiles AND the layer id blocks, taken BEFORE
+		// anything is written. Same objects the texture painter records for a dab.
 		RecordTerrainAppearanceUndo((normYMin + normYMax) * 0.5f, (normXMin + normXMax) * 0.5f,
 		                            max(normYMax - normYMin, normXMax - normXMin) * 0.5f);
 
-		// ---------------------------------------------------------------- layer weights, at CELL rate
-		// Sampled at the terrain node positions ComputeBakeBlock uses - (rectX1 + ux) * unitSize, no half
-		// cell offset - and with the sample footprint set to one terrain cell, so the weight here is bit
-		// for bit the weight the height bake gave that same cell.
+		// Layer weights, at CELL rate. Sampled at the terrain node positions ComputeBakeBlock uses (no half
+		// cell offset) with a one cell footprint, so the weight is bit for bit the height bake's.
 		int layerCells = 0;
 		const int hx0 = rectY1;   // heightmap X, world Y
 		const int hy0 = rectX1;   // heightmap Y, world X
@@ -747,7 +716,7 @@ static void PyCommitTerrainPlateColour()
 			}
 		}
 
-		// ---------------------------------------------------------------- colour, at TEXEL rate
+		// Colour, at TEXEL rate.
 		int colourTexels = 0;
 		int albedoMisses = 0;
 
@@ -765,9 +734,9 @@ static void PyCommitTerrainPlateColour()
 			{
 				for (int tx = tileX0; tx <= tileX1; ++tx)
 				{
-					// GetTileImage is the write handle research 12 section 3.1 points at: it loads the tile
-					// if needed and marks it dirty, unlike SetValueAt(float, float, ...) which writes through
-					// a private pointer and leaves the tile clean (RGBLayer.cpp:378, hazard 4).
+					// GetTileImage is the write handle: it loads the tile if needed and marks it dirty, unlike
+					// SetValueAt(float, float, ...), which writes through a private pointer and leaves the
+					// tile clean.
 					CImageEx* pTile = pRGBLayer->GetTileImage(tx, ty, true);
 					if (pTile == nullptr || pTile->GetWidth() <= 0 || pTile->GetHeight() <= 0)
 						continue;
@@ -775,8 +744,8 @@ static void PyCommitTerrainPlateColour()
 					const int resX = pTile->GetWidth();
 					const int resY = pTile->GetHeight();
 
-					// One texel's own footprint in metres, which is what the rim weight's coverage term is
-					// measured over here. 0.5 m by default (2 texels per metre, Heightmap.cpp:369-382).
+					// One texel's own footprint in metres, the area the rim weight's coverage term is measured
+					// over here. 0.5 m by default.
 					const float texelSize = max(terrainSize / (float)(resX * (int)tileCountX),
 					                            terrainSize / (float)(resY * (int)tileCountY));
 
@@ -799,12 +768,9 @@ static void PyCommitTerrainPlateColour()
 							if (weight <= 0.002f)
 								continue;
 
-							// THE colour, and the only one: the plate material's diffuse texture under this
-							// world position, at the resolution this texel size asks for. The live overlay
-							// makes the identical call with the identical texel size
-							// (TerrainPlateColourOverlay::ApplyToRow), which is what makes this command
-							// mean "make what you are looking at permanent". A texel the plate cannot
-							// colour is left as it was - there is no invented fallback colour.
+							// The live overlay makes the identical call with the identical texel size, which is
+							// what makes this command mean "make what you are looking at permanent". A texel
+							// the plate cannot colour is left as it was; there is no fallback colour.
 							ColorF albedo;
 							if (!pCommit->GetCommitAlbedoAtWorld(worldX, worldY, texelSize, albedo))
 							{
@@ -812,20 +778,12 @@ static void PyCommitTerrainPlateColour()
 								continue;
 							}
 
-							// The blend is the painter's, line for line (ImagePainter.cpp:310-335): both sides
-							// to linear, lerp there, back to gamma. Blending gamma values directly would make
-							// the rim band read too dark.
-							//
-							// It lives in TerrainPlateColourOverlay because the LIVE overlay (decision 08)
-							// runs the very same call on the very same rim weight, one line of
-							// TerrainPlateColourOverlay::ApplyToRow. That shared call is what makes the two
-							// tiers WYSIWYG: this command means "make what you are looking at permanent",
-							// and neither tier can drift from the other without breaking the other.
-							//
-							// Tile pixel layout: blue | green << 8 | red << 16, alpha unused (RGBLayer.h:63),
-							// and the image's first axis is fpx, i.e. world Y (CRGBLayer::GetValueAt,
-							// RGBLayer.cpp:273-281). The overlay's destination texels are in that same
-							// layout, because they came out of this very RGB layer.
+							// The blend is the painter's, line for line: both sides to linear, lerp there, back
+							// to gamma - blending gamma values directly reads too dark. It lives in
+							// TerrainPlateColourOverlay because the live overlay runs the very same call on
+							// the very same rim weight, so neither tier can drift from the other.
+							// Tile pixel layout: blue | green << 8 | red << 16, alpha unused, first axis fpx
+							// (world Y) - the layout these destination texels already have.
 							uint32& destination = pTile->ValueAt(px, py);
 							destination = TerrainPlateColourOverlay::BlendPlateColourIntoTexel(
 							  destination, albedo, appearance.tint, weight);
@@ -849,9 +807,8 @@ static void PyCommitTerrainPlateColour()
 			continue;
 		}
 
-		// ---------------------------------------------------------------- refresh
-		// Colour into the viewport, per touched terrain texture sector - the painter's own recipe
-		// (TerrainTexturePainter.cpp:727-760), including the (iY, iX) / (minY, minX) axis order.
+		// Refresh. Colour into the viewport, per touched terrain texture sector - the painter's own recipe,
+		// (iY, iX) axis order included.
 		if (colourTexels > 0)
 		{
 			const int texSectorSize = p3DEngine->GetTerrainTextureNodeSizeMeters();
@@ -874,7 +831,7 @@ static void PyCommitTerrainPlateColour()
 		}
 
 		// Surface bits down to the engine. UpdateEngineTerrain always pushes a square of side areaSize
-		// snapped out to whole sectors and ignores its 4th argument (Heightmap.cpp:2461-2472).
+		// snapped out to whole sectors and ignores its 4th argument.
 		if (layerCells > 0 && cols > 0 && rows > 0)
 		{
 			pHeightmap->UpdateEngineTerrain(hx0, hy0, max(cols, rows), max(cols, rows), CHeightmap::ETerrainUpdateType::Paint);
diff --git a/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.cpp b/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.cpp
index 56d8ddd2..ccf48d7f 100644
--- a/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.cpp
+++ b/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.cpp
@@ -6,9 +6,8 @@
 #include "Terrain/Heightmap.h"
 #include "IEditorImpl.h"
 
-// Decision 08. EditorQt already has the plugin's Module directory on its include path
-// (CMakeLists.txt:1508-1513) and this is a header only downcast onto a pure virtual interface, so
-// nothing here needs a symbol out of CryDefaultEntities - same shape as TerrainCommands.cpp:21.
+// Header only downcast onto a pure virtual interface, so nothing here needs a symbol out of
+// CryDefaultEntities.
 #include <DefaultComponents/Geometry/TerrainPlateComponent.h>
 
 #include <CryEntitySystem/IEntitySystem.h>
@@ -22,20 +21,18 @@ namespace
 using Cry::DefaultComponents::CTerrainPlateComponent;
 using Cry::DefaultComponents::ITerrainPlateCommit;
 
-//! Debounce floor. One dirty terrain texture sector costs a whole tile DXT recompress plus an upload, so
-//! a plate that keeps changing (a drag with Update During Drag on, a slider being scrubbed) must not get
-//! one per frame. A plate that reports itself settling is held back entirely and repaints on release;
-//! this interval is the backstop for the ones that do not.
+//! Debounce floor: one dirty terrain texture sector costs a whole tile DXT recompress plus an upload.
+//! A plate that reports itself settling is held back entirely; this is the backstop for the others.
 const uint32 kRepaintFrameInterval = 6;
 
-//! A weight below this contributes less than half a quantisation step and is skipped, exactly as
-//! terrain.commit_plate_colour skips it. Keeping the same epsilon keeps the two footprints identical.
+//! Below this a weight contributes less than half a quantisation step. terrain.commit_plate_colour uses
+//! the same epsilon, which is what keeps the two footprints identical.
 const float  kWeightEpsilon = 0.002f;
 
 struct SLivePlate
 {
-	//! The entity, not the component pointer: ApplyToRow re-resolves through the entity system so a
-	//! plate deleted between Update() and the engine's pull cannot leave a dangling interface pointer.
+	//! The entity, not the component pointer: ApplyToRow re-resolves it, so a plate deleted between
+	//! Update() and the engine's pull cannot dangle.
 	EntityId             id = INVALID_ENTITYID;
 
 	// Footprint in world metres, XY only.
@@ -47,13 +44,13 @@ struct SLivePlate
 	//! The user's optional filter over the sampled diffuse. White by default.
 	ColorB               tint = ColorB(255, 255, 255, 255);
 
-	//! Everything the look depends on, folded into one value. When it changes the sectors under the OLD
-	//! and the NEW footprint are marked dirty.
+	//! Everything the look depends on, folded into one value. On a change the sectors under the OLD and
+	//! the NEW footprint are marked dirty.
 	uint32               signature = 0;
 	uint32               lastRepaintFrame = 0;
 
-	//! The plate has deferred transform work pending, i.e. it is being dragged. The change detector holds
-	//! its fire until this clears, so a drag repaints once, on release.
+	//! A drag is in progress. The change detector holds its fire until this clears, so a drag repaints
+	//! once, on release.
 	bool                 bSettling = false;
 };
 
@@ -72,14 +69,10 @@ uint32 HashFloat(uint32 seed, float value)
 	return HashCombine(seed, bits);
 }
 
-//! Marks every terrain texture sector the box touches, through the editor's own refresh path. This is
-//! the recipe the terrain texture painter uses for a dab (TerrainTexturePainter.cpp:727-760) and the one
-//! terrain.commit_plate_colour reuses, including the (iY, iX) / (minY, minX) axis order:
-//! CHeightmap::UpdateSectorTexture -> CTerrainGrid::LockSectorTexture -> I3DEngine::SetTerrainSectorTexture,
-//! which walks the node and every ancestor and sets eTES_SectorIsModified_AtlasIsDirty
-//! (terran_edit.cpp:456-475, terrain_node.cpp:518-526). Going straight to SetTerrainSectorTexture would
-//! be wrong: the engine only arms the dirty flag for a node whose editor texture id is non zero, and
-//! LockSectorTexture is what creates that id.
+//! Marks every terrain texture sector the box touches, through the editor's own refresh path - the same
+//! recipe the texture painter uses for a dab, (iY, iX) axis order included. Calling
+//! I3DEngine::SetTerrainSectorTexture directly would be wrong: the engine only arms the dirty flag for a
+//! node whose editor texture id is non zero, and CHeightmap::UpdateSectorTexture is what creates it.
 void MarkSectorsDirty(float worldXMin, float worldYMin, float worldXMax, float worldYMax)
 {
 	CHeightmap* pHeightmap = GetIEditorImpl()->GetHeightmap();
@@ -115,9 +108,7 @@ void MarkSectorsDirty(float worldXMin, float worldYMin, float worldXMax, float w
 	}
 }
 
-//! Walks the entity system and collects the plates that currently want the overlay. Once per editor
-//! frame, from Update(). Plates are rare and GetComponentByTypeId is a short scan over one entity's
-//! components, so this is a vector walk, not a search.
+//! Collects the plates that currently want the overlay. Once per editor frame, from Update().
 void ScanPlates(std::vector<SLivePlate>& out)
 {
 	out.clear();
@@ -167,9 +158,8 @@ void ScanPlates(std::vector<SLivePlate>& out)
 		plate.worldYMax = (float)(rectY1 + rectSize) * unitSize;
 		plate.tint = tint;
 
-		// The look depends on the footprint, the tint and - through GetCommitAlbedoAtWorld - on which
-		// material the plate is wearing. A material EDIT (a different texture assigned inside the same
-		// material) is not covered; nudging the plate repaints it.
+		// A material EDIT (a different texture inside the same material) is not covered by this hash;
+		// nudging the plate repaints it.
 		uint32 signature = 0x811c9dc5u;
 		signature = HashFloat(signature, plate.worldXMin);
 		signature = HashFloat(signature, plate.worldYMin);
@@ -191,9 +181,8 @@ uint32 BlendPlateColourIntoTexel(uint32 destination, const ColorF& linearAlbedo,
 {
 	const float recip255 = 1.f / 255.f;
 
-	// The albedo is already linear - the sampler converted it there, where the texture's own colour space
-	// was known (@see ITerrainPlateCommit::GetCommitAlbedoAtWorld). Only the tint arrives in gamma,
-	// because it is a colour picker value.
+	// The albedo is already linear (the sampler converted it where the texture's colour space was known);
+	// only the tint arrives in gamma, being a colour picker value.
 	ColorF colourSource = linearAlbedo;
 
 	ColorF filter((float)tint.r * recip255, (float)tint.g * recip255, (float)tint.b * recip255);
@@ -228,8 +217,8 @@ void Update()
 	if (gEnv == nullptr)
 		return;
 
-	// No terrain (no level, or a level being torn down): drop everything without marking anything. The
-	// next level repaints from its own CRGBLayer, which this module never wrote to.
+	// No terrain: drop everything without marking anything. The next level repaints from its own
+	// CRGBLayer, which this module never wrote to.
 	if (gEnv->p3DEngine == nullptr || gEnv->p3DEngine->GetITerrain() == nullptr)
 	{
 		g_plates.clear();
@@ -260,8 +249,8 @@ void Update()
 			continue;
 		}
 
-		// Mid drag. Carry the last repainted state forward so the change stays pending: the drag costs no
-		// tile recompress at all and the ground repaints once, when the plate is dropped.
+		// Mid drag: carry the last repainted state forward so the change stays pending and the ground
+		// repaints once, on release.
 		if (pPrevious != nullptr && plate.bSettling)
 		{
 			plate.signature = pPrevious->signature;
@@ -275,10 +264,8 @@ void Update()
 
 		if (pPrevious != nullptr && (frameId - pPrevious->lastRepaintFrame) < kRepaintFrameInterval)
 		{
-			// Too soon. Carry the OLD state forward so the change is still pending and gets picked up on a
-			// later frame - this is the throttle, not a drop. One dirty sector is a whole tile DXT
-			// recompress plus an upload, so this is the floor even for a plate that never reports itself
-			// settling (Update During Drag on, or a slider being scrubbed).
+			// Too soon: carry the OLD state forward so the change stays pending. This is the throttle,
+			// not a drop.
 			plate.signature = pPrevious->signature;
 			plate.worldXMin = pPrevious->worldXMin;
 			plate.worldYMin = pPrevious->worldYMin;
@@ -298,9 +285,8 @@ void Update()
 		plate.lastRepaintFrame = frameId;
 	}
 
-	// Plates that went away: deleted, unticked, or Bake Into Terrain switched off. Repaint the ground
-	// they used to cover; the pull reads CRGBLayer, which never held their colour, so it comes back
-	// exactly as it was.
+	// Plates that went away. Repaint the ground they covered; the pull reads CRGBLayer, which never held
+	// their colour, so it comes back exactly as it was.
 	for (const SLivePlate& previous : g_plates)
 	{
 		bool bStillLive = false;
@@ -333,10 +319,8 @@ void ApplyToRow(uint32* pRow, int count, float worldYStart, float worldStepY, fl
 	const float rowYMax = max(worldYStart, worldYEnd);
 
 	// The rim weight is an area measure and sampleSizeMeters is the area it is measured over. The colour
-	// commit measures it over ONE RGB TEXEL (reports/29 section 3), and the leaf terrain colour tile is
-	// finer than that, so the floor here makes the leaf tile - the one seen up close - agree with the
-	// commit texel for texel. Ancestor tiles are coarser and use their own, wider footprint, which is
-	// what antialiases the rim at distance.
+	// commit measures it over one RGB texel, so this floor makes the leaf tile agree with the commit
+	// texel for texel; coarser ancestor tiles keep their own wider footprint and antialias the rim.
 	const float sampleSize = max(fabs_tpl(worldStepY), rgbTexelSizeMeters);
 
 	for (const SLivePlate& plate : g_plates)
@@ -347,9 +331,8 @@ void ApplyToRow(uint32* pRow, int count, float worldYStart, float worldStepY, fl
 			continue;
 		}
 
-		// Re-resolve through the entity id rather than trusting the cached pointer: the pull runs inside
-		// the engine update, and an entity deleted between Update() and here would leave a dangling
-		// interface pointer. GetEntity is an array lookup and this is once per row, not once per texel.
+		// Re-resolve through the entity id: the pull runs inside the engine update, and an entity deleted
+		// between Update() and here would leave a dangling interface pointer.
 		IEntity* pEntity = gEnv->pEntitySystem->GetEntity(plate.id);
 		if (pEntity == nullptr)
 			continue;
@@ -366,17 +349,13 @@ void ApplyToRow(uint32* pRow, int count, float worldYStart, float worldStepY, fl
 			if (worldY < plate.worldYMin || worldY > plate.worldYMax)
 				continue;
 
-			// THE rim weight, the same call terrain.commit_plate_colour makes
-			// (TerrainCommands.cpp, PyCommitTerrainPlateColour): identical curve, identical knobs,
-			// identical epsilon, so the preview and the commit cannot drift.
+			// The same rim weight call terrain.commit_plate_colour makes, so the two cannot drift.
 			const float weight = pCommit->GetCommitWeightAtWorld(worldX, worldY, sampleSize);
 			if (weight <= kWeightEpsilon)
 				continue;
 
-			// THE colour: the plate material's diffuse texture under this world position. The commit
-			// makes the identical call with the identical texel size, so the two tiers sample the same
-			// resolution of the same texture at the same place. A plate whose material cannot be sampled
-			// at all stamps nothing rather than falling back to some invented colour.
+			// The commit makes the identical call with the identical texel size, so both tiers sample the
+			// same mip at the same place. An unsamplable material stamps nothing rather than inventing.
 			ColorF albedo;
 			if (!pCommit->GetCommitAlbedoAtWorld(worldX, worldY, rgbTexelSizeMeters, albedo))
 				continue;
diff --git a/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.h b/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.h
index 5efec19a..15b5082f 100644
--- a/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.h
+++ b/Code/Sandbox/EditorQt/Terrain/TerrainPlateColourOverlay.h
@@ -4,59 +4,33 @@
 
 #include <CryMath/Cry_Color.h>
 
-//! Decision 08 - the plate's diffuse as the terrain's macro colour, LIVE and non destructive.
+//! The terrain plate's diffuse as the terrain's macro colour, live and non destructive. The source is
+//! ITerrainPlateCommit::GetCommitAlbedoAtWorld, the same call terrain.commit_plate_colour makes at the
+//! same resolution, so Commit Colour means "make what you are looking at permanent".
 //!
-//! WHAT COLOUR. The plate material's DIFFUSE TEXTURE, sampled at the world position projected top down
-//! through the plate's own UV mapping, times the material's diffuse colour - i.e. what the plate mesh
-//! itself shows. That is the only source; the flat tint that used to stand in for it is gone, because a
-//! plate has no colour of its own apart from its material (user, 2026-09-01). The sampler is
-//! ITerrainPlateCommit::GetCommitAlbedoAtWorld and it is the SAME call terrain.commit_plate_colour makes,
-//! at the same resolution, which is what makes Commit Colour mean "make what you are looking at
-//! permanent".
+//! The engine's editor side macro colour is a PULL: a dirty node makes
+//! CTerrainNode::UpdateNodeTextureFromEditorData build its tile row by row out of
+//! CHeightmap::GetColorAtPosition. The overlay is applied to that OUTGOING row buffer and CRGBLayer, the
+//! level's actual colour, is never written - CRGBLayer::Serialize flushes every dirty tile to disk on
+//! every level save, so a write there would become permanent at the next Ctrl+S.
 //!
-//! Where this sits. The engine's runtime macro colour is a pooled atlas whose editor half is a PULL:
-//! CTerrainNode::SetupTexturing sees a node marked eTES_SectorIsModified_AtlasIsDirty and calls
-//! CTerrainNode::UpdateNodeTextureFromEditorData (terrain_node.cpp:117-142, :528-583), which builds the
-//! tile row by row out of C3DEngine::m_pEditorHeightmap->GetColorAtPosition and DXT compresses it into
-//! the atlas slot. That callback IS CHeightmap. So the overlay is applied to the row buffer CHeightmap
-//! hands back (Heightmap.cpp, GetColorAtPosition) - the OUTGOING buffer only. CRGBLayer, the level's
-//! actual colour, is never written: CRGBLayer::Serialize flushes every dirty tile to disk on every level
-//! save (RGBLayer.cpp:1088), so anything written there would silently become permanent at Ctrl+S
-//! (research 13 section B / section 3).
-//!
-//! Consequences that fall out of that choice, all of them wanted:
-//!  * nothing to restore. The atlas is a cache; its truth is CRGBLayer. Untick, move or delete a plate
-//!    and the same pull repaints the tile from the untouched source. There is no baseline array and no
-//!    restore bug class (research 13 section 1.3-1.4).
-//!  * it survives streaming for free: CTerrainSectorTexture's StreamOnComplete re-arms the dirty flag
-//!    (terrain_sector_tex.cpp:170-173), so the next pull re-applies the overlay.
-//!  * EDITOR ONLY, by nature. The game loads terraintexture.pak, which is produced from CRGBLayer by
-//!    CGameExporter::ExportSurfaceTexture (GameExporter.cpp:1068-1070). Nothing written into the atlas
-//!    can reach it. Shipping the colour is what terrain.commit_plate_colour (Commit Colour) is for, and
-//!    the two tiers share BlendPlateColourIntoTexel and ITerrainPlateCommit::GetCommitWeightAtWorld, so
-//!    "Commit Colour" means literally "make what you are looking at permanent".
+//! Consequences, all wanted: nothing to restore (the atlas is a cache, its truth is CRGBLayer); it
+//! survives streaming, because StreamOnComplete re-arms the dirty flag; and it is editor only, because
+//! the game loads terraintexture.pak, which is exported from CRGBLayer.
 namespace TerrainPlateColourOverlay
 {
 
 //! THE blend, shared by the live overlay and by terrain.commit_plate_colour. Both sides to linear, lerp
-//! there, back to gamma - the terrain painter's own recipe (ImagePainter.cpp:310-335); blending gamma
-//! values directly makes the rim band read too dark.
-//!
+//! there, back to gamma - the terrain painter's own recipe; blending gamma values directly makes the rim
+//! band read too dark.
 //! destination and the return value are one terrain colour texel in the RGB layer's packed layout,
-//! blue | green << 8 | red << 16 (RGBLayer.h:63), which is also the layout CHeightmap::GetColorAtPosition
-//! hands to the engine.
-//!
-//! linearAlbedo is what ITerrainPlateCommit::GetCommitAlbedoAtWorld returned - the plate material's
-//! diffuse texture times its diffuse colour, already LINEAR, which is why it arrives as a ColorF and not
-//! as a byte triple. There is no flat colour path anywhere any more: a plate has no colour of its own
-//! other than its material. tint is the user's optional filter, a gamma space picker value, white by
-//! default; it multiplies the albedo the way a gel multiplies a lamp.
+//! blue | green << 8 | red << 16. linearAlbedo comes from GetCommitAlbedoAtWorld and is already LINEAR.
+//! tint is the user's optional gamma space filter over it, white by default.
 uint32 BlendPlateColourIntoTexel(uint32 destination, const ColorF& linearAlbedo, ColorB tint, float weight);
 
-//! Once per editor frame. Re-enumerates the plates whose Terrain Colour From Plate is on, and marks the
-//! terrain texture sectors under a plate that appeared, moved, changed or went away, so that the engine
-//! pull repaints them. Debounced: a plate that reports itself settling (a drag in progress) is left
-//! alone until it settles, and no plate repaints more often than kRepaintFrameInterval.
+//! Once per editor frame: re-enumerates the plates asking for the overlay and marks the terrain texture
+//! sectors under any that appeared, moved, changed or went away. Debounced - a plate that reports itself
+//! settling is left alone, and no plate repaints more often than kRepaintFrameInterval.
 void Update();
 
 //! True when at least one plate is currently asking for the overlay. Cheap; the stock path must stay
@@ -65,11 +39,9 @@ bool IsActive();
 
 //! Composites the live plates over one row of terrain colour texels, in place. Called from
 //! CHeightmap::GetColorAtPosition with the row it is about to return.
-//!
-//! The engine asks for a row along WORLD Y at a fixed world X (terrain_node.cpp:562 passes m_nOriginY
-//! first and m_nOriginX second, and CHeightmap's first argument is the fpx of the RGB layer, which is
-//! the world Y - research 12 sections 1.2 and 3.1). worldStepY is the node's own texel size in metres,
-//! so ancestor tiles automatically get a wider filter footprint and the rim antialiases at every level.
+//! The engine asks for a row along WORLD Y at a fixed world X (CHeightmap's fpx is the RGB layer's, i.e.
+//! the world Y axis). worldStepY is the node's own texel size, so ancestor tiles get a wider filter
+//! footprint and the rim antialiases at every level.
 void ApplyToRow(uint32* pRow, int count, float worldYStart, float worldStepY, float worldX, float rgbTexelSizeMeters);
 
 }
diff --git a/Code/Sandbox/EditorQt/TerrainTexturePainter.cpp b/Code/Sandbox/EditorQt/TerrainTexturePainter.cpp
index 25bcf35b..05a7bc68 100644
--- a/Code/Sandbox/EditorQt/TerrainTexturePainter.cpp
+++ b/Code/Sandbox/EditorQt/TerrainTexturePainter.cpp
@@ -321,7 +321,6 @@ public:
 	}
 
 	//! Takes ownership of an element a caller outside the painter tool filled in.
-	//! @see RecordTerrainAppearanceUndo.
 	explicit CUndoTexturePainter(CUndoTPElement* pElement)
 	{
 		m_pUndo = pElement;
@@ -358,10 +357,8 @@ void RecordTerrainAppearanceUndo(float fpx, float fpy, float radius)
 	if (!CUndo::IsRecording())
 		return;
 
-	// AddSector snapshots both halves of the appearance in one go: the RGB tile regions the square
-	// touches, and the 64 x 64 blocks of the layer id bitmap under it. CUndoTPElement::Paste puts both
-	// back and re-pushes the engine surface bits, so one Ctrl+Z restores colour and layer weights
-	// together, which is precisely what the commit needs.
+	// AddSector snapshots both halves of the appearance - the RGB tile regions and the layer id blocks -
+	// so one Ctrl+Z restores colour and layer weights together.
 	CUndoTPElement* pElement = new CUndoTPElement;
 	pElement->AddSector(fpx, fpy, radius);
 
diff --git a/Code/Sandbox/EditorQt/TerrainTexturePainter.h b/Code/Sandbox/EditorQt/TerrainTexturePainter.h
index 32f39315..c8359972 100644
--- a/Code/Sandbox/EditorQt/TerrainTexturePainter.h
+++ b/Code/Sandbox/EditorQt/TerrainTexturePainter.h
@@ -50,15 +50,11 @@ struct CTextureBrush
 	}
 };
 
-//! Snapshots the terrain COLOUR tiles and the surface type (layer id) blocks that the normalised square
-//! (fpx +- radius, fpy +- radius) touches, and records them as ONE undo object in the currently open undo
-//! group. fpx / fpy / radius are in the same 0..1 whole-terrain measure the painter uses, i.e. fpx is the
-//! world Y axis and fpy is the world X axis (the CHeightmap swap).
-//!
-//! It exists so that a caller which writes CRGBLayer and the layer bitmap by hand - the terrain plate
-//! appearance commit - gets exactly the undo the texture painter gets for a dab, out of exactly the same
-//! objects (CUndoTPElement / CUndoTexturePainter, this file), instead of inventing a second one. Must be
-//! called BEFORE the writes and inside an open CUndo scope.
+//! Snapshots the terrain colour tiles and the layer id blocks the normalised square
+//! (fpx +- radius, fpy +- radius) touches as ONE undo object in the open undo group, so a caller that
+//! writes CRGBLayer and the layer bitmap by hand gets the same undo a painter dab gets.
+//! fpx / fpy / radius use the painter's 0..1 whole-terrain measure, i.e. fpx is the world Y axis and fpy
+//! the world X axis (the CHeightmap swap). Must be called BEFORE the writes, inside an open CUndo scope.
 void RecordTerrainAppearanceUndo(float fpx, float fpy, float radius);
 
 //////////////////////////////////////////////////////////////////////////
diff --git a/Code/Sandbox/EditorQt/Vegetation/VegetationMap.cpp b/Code/Sandbox/EditorQt/Vegetation/VegetationMap.cpp
index 6f8aef1b..40016e97 100644
--- a/Code/Sandbox/EditorQt/Vegetation/VegetationMap.cpp
+++ b/Code/Sandbox/EditorQt/Vegetation/VegetationMap.cpp
@@ -2723,8 +2723,7 @@ void CVegetationMap::RepositionInstancesOnTerrain(const AABB& box, int& outMoved
 	{
 		for (int sectX = sx1; sectX <= sx2; ++sectX)
 		{
-			// RegisterInstance() never changes the XY position, so the sector list stays valid while
-			// it is being iterated.
+			// RegisterInstance() never changes XY, so the sector list stays valid while iterated.
 			SectorInfo& sector = GetVegSector(sectX, sectY);
 			for (auto pInst : sector)
 			{
@@ -2733,14 +2732,13 @@ void CVegetationMap::RepositionInstancesOnTerrain(const AABB& box, int& outMoved
 
 				if (!pInst->object || pInst->object->IsAffectedByBrushes() || !pInst->object->IsAffectedByTerrain())
 				{
-					// Anchored to a brush or explicitly detached from the terrain: same rule as
-					// OnHeightMapChanged().
+					// Anchored to a brush or detached from the terrain: same rule as OnHeightMapChanged().
 					++outSkipped;
 					continue;
 				}
 
-				// The live engine terrain, not the editor CHeightmap: this is what picks up an
-				// engine side SetTerrainElevation (e.g. a terrain plate bake).
+				// The live engine terrain, not the editor CHeightmap: only this sees an engine side
+				// SetTerrainElevation such as a terrain plate bake.
 				const float terrainZ = p3DEngine->GetTerrainElevation(pInst->pos.x, pInst->pos.y);
 				if (fabs(terrainZ - pInst->pos.z) < kZTolerance)
 				{
@@ -2751,10 +2749,8 @@ void CVegetationMap::RepositionInstancesOnTerrain(const AABB& box, int& outMoved
 				RecordUndo(pInst);
 				pInst->pos.z = terrainZ;
 
-				// Destroys and re-adds the render node. For auto-merged objects this re-adds the
-				// merged-mesh sample (CTerrain::AddVegetationInstance -> CMergedMeshesManager::AddInstance)
-				// with a freshly sampled terrain alignment, which is the only way to move merged-mesh
-				// grass. It also rebuilds the ground decal.
+				// Destroys and re-adds the render node; for auto-merged objects this re-adds the
+				// merged-mesh sample with a fresh terrain alignment, the only way to move merged grass.
 				RegisterInstance(pInst);
 				++outMoved;
 			}
diff --git a/Code/Sandbox/EditorQt/Vegetation/VegetationMap.h b/Code/Sandbox/EditorQt/Vegetation/VegetationMap.h
index 3450d80d..70e7c979 100644
--- a/Code/Sandbox/EditorQt/Vegetation/VegetationMap.h
+++ b/Code/Sandbox/EditorQt/Vegetation/VegetationMap.h
@@ -139,10 +139,9 @@ public:
 	void RepositionArea(const AABB& box, const Vec3& offset = Vec3(0, 0, 0), int nRot = 0, bool isCopy = false);
 
 	//! Re-sample the Z of every vegetation instance inside \p box from the CURRENT ENGINE terrain and
-	//! re-register it. Unlike OnHeightMapChanged(), auto-merged (merged-mesh) instances are handled as
-	//! well, because the re-registration goes through RegisterInstance(), which destroys and re-adds the
-	//! render node resp. the merged-mesh sample.
-	//! XY positions are never changed, so no sector re-bucketing and no duplicate removal is needed.
+	//! re-register it. Unlike OnHeightMapChanged() this also handles auto-merged instances, because
+	//! RegisterInstance() destroys and re-adds the render node resp. the merged-mesh sample.
+	//! XY is never changed, so no sector re-bucketing and no duplicate removal is needed.
 	//! \param box   World space area to process. Only XY is used, Z is ignored.
 	//! \param outMoved   Number of instances whose Z was changed.
 	//! \param outSkipped Number of instances inside the area that were left untouched.
-- 
2.47.1.windows.1

