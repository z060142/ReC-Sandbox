[project]
name = "exr-check"
version = "0.1.0"
description = "Render a scene-referred EXR through the same ACES display/view the engine uses, and compare"
requires-python = ">=3.10"
dependencies = [
    "opencolorio>=2.4",
    "numpy>=1.24",
    "openexr>=3.2",
    "pillow>=10.0",
]

[tool.uv]
package = false
