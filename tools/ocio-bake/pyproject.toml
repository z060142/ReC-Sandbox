[project]
name = "ocio-bake"
version = "0.1.0"
description = "Bake the ACES 2.0 output transform to a .cube for the scene-referred pipeline"
requires-python = ">=3.10"
dependencies = [
    "opencolorio>=2.4",
    "numpy>=1.24",
    "tomli>=2.0; python_version < '3.11'",
]

[tool.uv]
package = false
